# DSH 便携版 3

此目录可以整体放在 U 盘、移动硬盘或可移动 SSD 中。启动时会按当前盘符重新计算路径，不依赖制作时的 `G:` 盘符。

## 目录职责

| 目录 | 内容 | 官网更新时 |
| --- | --- | --- |
| `App/` | 初始桌面程序兼容槽 | 保留，统一更新器不会原位覆盖 |
| `Data/DSH/` | 配置、凭据、Profile、插件登记、会话和对话 | 保留 |
| `Data/Electron/` | 桌面设置、Cookie、Local Storage、缓存和崩溃记录 | 保留 |
| `Data/Runtime/` | 官方 DSH 运行时和离线插件仓库 | 保留，新版本会按哈希自修复 |
| `Data/Development/` | npm/pnpm、构建器及常见开发工具缓存 | 保留 |
| `工作空间/` | 用户项目与工作文件 | 保留 |
| `Customize/` | 用户定制素材与说明 | 保留，不自动覆盖已验证槽 |
| `Data/Updates/Desktop/` | 更新状态、审计事件、下载、A/B 槽和指针 | 保留，可自动回滚 |
| 源码与 Git 历史 | 用户定制、比较和重新构建 | 保留 |

## 使用

1. 双击 `DSH便携版3.exe` 启动（推荐，无控制台窗口，图标为 DSH 官方鲸鱼标志；若应用已在运行会自动切回其窗口）。也可以双击 `Start-DSH-Portable.cmd` 启动。
2. 点击顶部“更新”图标或“帮助 → 检查更新”，在可视化页面查看检测、下载、验证、构建、部署、完成的总进度与分阶段进度。
3. 候选完成构建后，由用户确认“部署并重启”。当前 DSH 会先优雅退出；启动器切换到不可变候选槽并执行真实启动验证，失败自动恢复上一槽。
4. 修改源码后，运行 `Build-DSH-Portable.cmd`。脚本会在 `Tools/` 自备 Node.js 和 pnpm，依赖缓存也只写到 `Data/Development/`。
5. 拉取官网源码前先提交自己的定制，再使用标准 Git fetch/rebase 或合并流程；解决冲突并通过质量门禁后才构建，不再使用会直接替换文件的旧更新脚本。
6. 更新或自建后可运行 `powershell -ExecutionPolicy Bypass -File .\scripts\smoke-portable.ps1`，执行强制离线首启验收。

> 启动器说明：`DSH便携版3.exe` 与 `DSH便携版3.ico` 均由 `scripts\build-launcher.ps1` 生成——图标取自官网鲸鱼标志（与第一版同款），exe 嵌入该图标并无窗口调用启动脚本。重新生成时若图标文件已存在会直接复用；删除 `DSH便携版3.ico` 后重跑脚本才会重新绘制。

低速 U 盘首次解压数万小文件可能需要 5–15 分钟；看到初始化界面时不要拔盘或强制结束。后续启动会复用带 SHA256 标记的盘内运行时。

## 迁移与备份

- 彻底退出托盘中的 DSH 后，直接复制整个目录即可迁移。
- 最重要的备份是 `Data/DSH/`、`Data/Electron/UserData/`、`工作空间/`、`Customize/` 和已提交的源码分支。
- 凭据也在便携盘内。盘丢失等同于数据和密钥可能泄露，建议使用 BitLocker To Go 或硬件加密盘。
- Windows 本身仍可能保留“最近打开”、安全日志、DNS/网络缓存等操作系统级痕迹；应用可控的数据和运行环境已收口到便携盘，但普通 Windows 程序无法承诺宿主系统绝对零痕迹。

## 更新与恢复

- 检测、下载、摘要验证和候选构建都不要求退出 DSH，也不会修改正在运行的槽。
- 只有用户确认部署桌面应用时才会优雅退出；插件安装/更新与 Harness 子运行时更新不会退出桌面应用。
- Release ZIP 必须来自允许的 GitHub 仓库，并同时通过 Release SHA256、候选文件闭包、归档 sidecar、产品版本和槽清单校验。
- 桌面启动器只切换 `Data/Updates/Desktop/pointer.json` 指向的槽；候选未在时限内生成健康证明时自动回滚。
- 审计记录位于 `Data/Updates/Desktop/events.jsonl`，启动与回滚记录位于 `Data/Updates/Desktop/launcher.log`。

## 插件安装与启动自愈

- 安装、升级、启用或禁用插件时不需要退出 `DSH Codex Desktop`。安装器先在同一磁盘暂存并校验文件，再原子切换插件目录和 Profile 清单，最后只让 DSH 子服务热重载。
- 启动前会校验社区插件入口。损坏插件会从启用列表中隔离，但依赖和插件文件保留，桌面会继续启动。
- 隔离审计位于 `Data/DSH/profiles/web/.dsh-recovery/quarantined-bundles.json`，对应清单备份位于同目录下的 `manifest-backups/`。
- 修复或重新安装插件后，健康指纹发生变化，旧隔离会自动解除并允许插件重新启用。
- 只有桌面程序自身更新、替换 `App/` 时才需要从托盘彻底退出。

项目的正式变更与答复门禁见 `docs/01-当前工作/便携版3-变更与答复质量门禁.md`。
