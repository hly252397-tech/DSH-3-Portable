# DSH 便携版 3

此目录可以整体放在 U 盘、移动硬盘或可移动 SSD 中。启动时会按当前盘符重新计算路径，不依赖制作时的 `G:` 盘符。

## 目录职责

| 目录 | 内容 | 官网更新时 |
| --- | --- | --- |
| `App/` | 当前桌面程序 | 替换，旧版移入 `Data/Updates/Backups/` |
| `Data/DSH/` | 配置、凭据、Profile、插件登记、会话和对话 | 保留 |
| `Data/Electron/` | 桌面设置、Cookie、Local Storage、缓存和崩溃记录 | 保留 |
| `Data/Runtime/` | 官方 DSH 运行时和离线插件仓库 | 保留，新版本会按哈希自修复 |
| `Data/Development/` | npm/pnpm、构建器及常见开发工具缓存 | 保留 |
| `Workspace/` | 用户项目与工作文件 | 保留 |
| `Customize/` | 更新后覆盖层与可选钩子 | 保留并重新应用 |
| 源码与 Git 历史 | 用户定制、比较和重新构建 | 保留 |

## 使用

1. 双击 `Start-DSH-Portable.cmd` 启动。
2. 首次部署或只想使用官网成品，运行 `Update-DSH-Portable.cmd`；它从 MichengAI GitHub Releases 下载最新 Windows x64 ZIP。
3. 修改源码后，运行 `Build-DSH-Portable.cmd`。脚本会在 `Tools/` 自备 Node.js 和 pnpm，依赖缓存也只写到 `Data/Development/`。
4. 用 `Sync-Upstream-Source.ps1` 检查官网源码更新；确认工作区干净后加 `-Apply` 才会快进，避免覆盖定制。
5. 更新或自建后可运行 `powershell -ExecutionPolicy Bypass -File .\scripts\smoke-portable.ps1`，执行强制离线首启验收。

低速 U 盘首次解压数万小文件可能需要 5–15 分钟；看到初始化界面时不要拔盘或强制结束。后续启动会复用带 SHA256 标记的盘内运行时。

## 迁移与备份

- 彻底退出托盘中的 DSH 后，直接复制整个目录即可迁移。
- 最重要的备份是 `Data/DSH/`、`Data/Electron/UserData/`、`Workspace/`、`Customize/` 和已提交的源码分支。
- 凭据也在便携盘内。盘丢失等同于数据和密钥可能泄露，建议使用 BitLocker To Go 或硬件加密盘。
- Windows 本身仍可能保留“最近打开”、安全日志、DNS/网络缓存等操作系统级痕迹；应用可控的数据和运行环境已收口到便携盘，但普通 Windows 程序无法承诺宿主系统绝对零痕迹。

## 更新与恢复

- 更新前必须从托盘彻底退出应用。
- `Update-DSH-Portable.cmd` 只替换 `App/`，并把旧版完整移动到 `Data/Updates/Backups/`。
- 如果新版本不适合，可退出应用，把当前 `App/` 改名，再把对应备份目录改回 `App/`。
- 官网 ZIP 会覆盖源码定制构建；需要持续保留的源码改动应提交到 Git 分支并重新执行构建，或放入 `Customize/App-Overlay/`。
