# 验证记录 — 2026-09-05

## 工作边界

只新增 `Check-DSH-Health.cmd` 和 `scripts/portable-health/`。未修改主进程、前端、Profile、更新指针或公共构建输出；没有启动、重启、停止 DSH。另一任务的隔离 UI 进程被排除。

## 基线及仓库门禁

- 官方兼容预检 `verify-dsh-official-baseline.mjs --online` 通过：内置 `0.1.2-alpha.3`；上游 `0.1.3-alpha.1@d347e703908d0406b7a7ef80e3a0e594d86b2215`。本工具不调用插件接口。
- 使用便携 Node `24.20.0`，TypeScript 输出到独立 `.health-check-dist`（不覆盖 `dist`），编译通过。
- 此独立编译目录内仓库测试：309 项，308 通过，1 跳过，0 失败。编译产物可丢弃，不属于桌面部署候选。
- PowerShell 工具使用 Windows PowerShell 5.1 验证；中文源码带 UTF-8 BOM。
- 独立测试覆盖正式/隔离进程区分、待激活与失败区分、旧日志、路径越界/目录联接、非法/超大 JSON、缺失安装、空间警告、损坏槽、认证响应、重定向禁用、请求超时、模拟运行时端到端以及指针并发变化。

## 真实只读检查

2026-09-05 08:50 左右（北京时间）的检查：当前桌面槽 `1.0.46-local-056cb80fe2077395`；候选 `1.0.46-local-9ce60671cf7bd302`。两者关键文件存在、清单哈希匹配，候选仍在等待激活，记录进度 94%。

当时没有正式用户数据目录的桌面主进程，仅有另一任务的隔离实例，因此没有探测它的 Harness。配置运行时指针为 `0.1.2-alpha.5`，没有将它冒充“实际运行版本”。便携盘可用空间约 133.48 GiB。

这些是检查时刻的证据，不是未来状态保证。实际正式实例运行期间的服务检查尚未现场验证；匹配子进程、读取运行时版本、HTTP 401 和并发状态处理由隔离夹具验证。不为验证此工具而打断另一任务或启动正式桌面。
