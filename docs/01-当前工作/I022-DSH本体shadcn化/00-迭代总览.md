# I022 · DSH 应用本体 shadcn 化 — 迭代总览

> 执行依据：`00-任务规格.md`（§0–§6）。改动范围：仅 `assets/theme.css`；未触碰 `--chrome-*` 令牌块、`--dsw-alias-*` 桥、其他 assets 文件、`src/`、`Data/DSH/profiles/**`、`cordis.patch.yml`；未执行 `git commit`；未启动应用。

## 1. 改动选择器清单

### 1.1 令牌块（仅改值，未改任何选择器）

- `:root`（暗色回退基础块）：`--dsh-bg-*`、`--dsh-text-*`、`--dsh-border-*` 中性色对齐 zinc。
- `:root[data-color-scheme="light"]`（浅色回退块）：同上。
- `:root[data-dsh-preset="qoder"]`（本体暗色实际生效块）：同上。
- `:root[data-color-scheme="light"][data-dsh-preset="qoder"]`（浅色 qoder 预设块）：中性色对齐 zinc + `--dsh-success` 去绿。
- accent 系（`--dsh-accent`/`--dsh-accent-soft`）及 lake/verde/vermilion/slate/gold 预设：一律不动。

### 1.2 浅色本体重绘块（`body[data-color-scheme="light"][data-dsh-preset="qoder"]`）

框架 / 侧栏 / 官方面板槽位：
- `.pI_x6G_frame`（bg）、`.pI_x6G_sidebarCol`（bg）、`.pI_x6G_centerCol`（border+border-radius）
- `.hHd-Xa_root`（bg）、`.hHd-Xa_brand`（border/border-radius/bg/color）、`.hHd-Xa_newSession`（border-radius）
- `.bhn1Oq_sectionHeader`（border-radius）、`.bhn1Oq_sectionLabel`（color）
- `.hHd-Xa_footArea`（border-top）

首页 hero / 详情面板：
- `.pXSMma_headline::after`（color）
- `.uV2eYG_card`（border+border-radius）
- `.pI_x6G_frame:has(.dss-root) .pI_x6G_detailsCol`（border+border-radius）
- `.dss-root` 令牌映射（sunken/line/line-strong/accent/accent-soft）

增强侧栏（michengai dsh-codex-ui）：
- `.dcu-root` 变量（primary/secondary/tertiary/navigation/icon/hover/border/bg）
- `.dcu-brand`（border/border-radius/bg/color）、`.dcu-icon`（border-radius）
- `.dcu-menu button` / `.dcu-footer-link`（border-radius）
- `.dcu-head-actions > button:last-child`（color）
- `.dcu-workspaces`（border-top-color）、`.dcu-foot`（border-top/bg）
- `.dcu-footer-actions > *` / `.dcu-settings-seat > button`（border-radius）
- `.dss-workbench-nav`（border-radius/color/hover）、`.dcu-settings-seat > button:hover`（bg）

## 2. 新旧值对照表

### 2.1 暗色 `--dsh-*` 令牌（§3.1）

| 令牌 | 旧值 | 新值 |
|---|---|---|
| `--dsh-bg-canvas` | `#181818` | `#09090b` |
| `--dsh-bg-surface-1` | `#1f1f1f` | `#18181b` |
| `--dsh-bg-surface-2` | `#252526` | `#27272a` |
| `--dsh-bg-surface-3` | `#2d2d2d` | `#3f3f46` |
| `--dsh-text-primary` | `#d4d4d4` | `#fafafa` |
| `--dsh-text-secondary` | `#a3a3a3` | `#a1a1aa` |
| `--dsh-text-tertiary` | `#7f7f7f` | `#71717a` |
| `--dsh-border-subtle` | `#303030` | `#27272a` |
| `--dsh-border-strong` | `#454545` | `#3f3f46` |

`:root` 暗色回退块同样对齐 zinc（canvas `#0f1316→#09090b`、surface-1 `#161c20→#18181b`、surface-2 `#1d262b→#27272a`、surface-3 `#253138→#3f3f46`、text-primary `#f1f6f8→#fafafa`、text-secondary `#a6b3ba→#a1a1aa`、text-tertiary `#788991→#71717a`、border-subtle `#2b383e→#27272a`、border-strong `#3b4c54→#3f3f46`）。

`:root[data-color-scheme="light"]` 回退块：canvas `#f4f8fa→#ffffff`、surface-2 `#eaf1f4→#f4f4f5`、surface-3 `#e0eaee→#e4e4e7`、text-primary `#11181c→#18181b`、text-secondary `#5b6970→#52525b`、text-tertiary `#7b8a91→#71717a`、border-subtle `#d4e0e4→#e4e4e7`、border-strong `#b9c9cf→#d4d4d8`。

浅色 qoder 预设块（79–96）：canvas `#f6f6f6→#fafafa`、surface-2 `#f6f6f6→#fafafa`、surface-3 `#efefef→#e4e4e7`、text-primary `#141414→#18181b`、text-secondary `#636261→#52525b`、text-tertiary `#838280→#71717a`、border-subtle `#e6e6e6→#e4e4e7`、border-strong `#dddddd→#d4d4d8`、success `#316e50→#18181b`；accent/accent-soft 保留。

### 2.2 浅色重绘块中性色（§3.2）

| 旧值 | 新值 | 出现位置 |
|---|---|---|
| `#f6f6f6` | `#fafafa` | frame、sidebarCol、hHd-Xa_root、dcu-root、dcu-foot、dss-sunken |
| `#e6e6e6` | `#e4e4e7` | centerCol/detailsCol 边框、--dss-line |
| `#dddddd` | `#d4d4d8` | brand 边框、footArea 顶线、uV2eYG_card 边框、dcu 各边框、--dss-line-strong |
| `#141414` | `#18181b` | sectionLabel、dcu-sidebar-primary |
| `#636261` | `#52525b` | dcu-sidebar-secondary/icon |
| `#838280` | `#71717a` | 文字三级（浅色预设块 text-tertiary） |
| `#292827` | `#18181b` | dcu-head-actions 搜索项、dss-workbench-nav |
| `#9b9a98` | `#71717a` | dcu-sidebar-tertiary |
| `#e7e7e7` | `#e4e4e7` | dcu-sidebar-hover、workbench-nav/settings-seat hover |

### 2.3 绿色残留清除（§3.2）

| 旧值 | 新值 | 出现位置 |
|---|---|---|
| `#edf5f1` | `var(--tint,#f4f4f5)` | hHd-Xa_brand、dss-root `--dss-accent-soft` |
| `#edf5f1` | `#f4f4f5` | dcu-brand |
| `#316e50` | `var(--accent,#18181b)` | hHd-Xa_brand、dss-root `--dss-accent` |
| `#316e50` | `#18181b` | 79–96 块 `--dsh-success` |
| `#285c43` | `#18181b` | dcu-brand |
| `#aaa39d` | `#71717a` | pXSMma_headline::after |

### 2.4 圆角（§3.3）

| 旧值 | 新值 | 位置 |
|---|---|---|
| `4px` | `6px` | hHd-Xa_newSession、bhn1Oq_sectionHeader |
| `5px` | `6px` | dcu-icon、dcu-menu button、dcu-footer-link、dcu-footer-actions/settings-seat、dss-workbench-nav |
| `9px` | `8px` | pI_x6G_centerCol、detailsCol |
| `20px` | `8px` | uV2eYG_card |
| `16px` | `999px` | hHd-Xa_brand、dcu-brand |

几何类属性（宽高、padding、margin、inset、transform、grid、order、flex）及全部 `content:"…"` 文本注入未改动。

## 3. 门禁三步结果

| 步骤 | 命令 | 结果 |
|---|---|---|
| 1. 类型检查 | `./App/resources/node/node.exe node_modules/typescript/bin/tsc` | ✅ 通过（exit 0） |
| 2. 全量测试 | `./App/resources/node/node.exe --test dist/test/*.test.js` | ✅ 通过：371 测试，0 fail（1 skipped） |
| 3. 重新打包 | `powershell -NoProfile -ExecutionPolicy Bypass -File Build-DSH-Portable.ps1` | ✅ **通过**（transactionId `f21e989c-7b15-4940-bb54-2153c192879a`） |

### 门禁 3 过程（含并发会话协议 §0.5）

- 首次打包在 A/B 候选暂存阶段被工具中断（未出 transactionId，未生成新槽）。
- 随后两次重跑被**无关失败**阻断：第 2 次 `tsc` 报 `test/work-mode.test.ts(395,45): TS2322`；第 3 次 `dist/test/work-mode.test.js:485` 运行时断言失败。经核实为并发会话（PID 12412，`opencode.exe run` 执行 I022-模式切换任务）正在实时改写 `test/work-mode.test.ts`（末次写入 2026-09-07 21:58:51）。按 §0.5 等待 90 秒重跑两次仍失败，按协议停下并报告"打包被无关失败阻断"，未代改他人测试/插件源。
- 并发会话结束后重跑第 4 次：一次 `electron-builder` 阶段网络超时（`connect ETIMEDOUT 20.205.243.166:443`，外部资源下载瞬时失败，与改动无关）；第 5 次重跑成功完成。
- **最终成功**：`Build-DSH-Portable.ps1` 完成 pnpm install + tsc + 全量测试（0 fail）+ pack + A/B 候选暂存；`Data/Updates/Desktop/state.json` 进入 `deploying` 阶段（`overallProgress: 94`），输出 **transactionId `f21e989c-7b15-4940-bb54-2153c192879a`**，槽位 `Data/Updates/Desktop/slots/1.0.46-local-b7ed028093d63fc3`。
- 已验证候选槽内 `resources/theme.css` 含本次全部新值（`--dsh-bg-canvas:#09090b/#fafafa`、`--dsh-success:#18181b`、`--dss-accent:var(--accent,#18181b)`、`border-radius:999px` 等）。
- 打包后按 §5 停止：未重启应用、未做端到端，部署/验证/回滚由用户重启触发。

## 4. 待用户目视确认项（重启应用后）

候选已暂存（transactionId `f21e989c-7b15-4940-bb54-2153c192879a`），请从托盘正常退出或"重启应用"触发启动器部署与验证，再按下列项目视：

深色（qoder preset）下：
- 侧栏、聊天区、首页 hero、工作台导航中性色对齐 zinc 冷灰（画布 `#09090b`、侧栏 `#18181b`、边框 `#27272a`/`#3f3f46`），无暖灰残留。
- 7 个强调色预设（lake/verde/vermilion/slate/gold/qoder）继续生效。

浅色（qoder preset）下：
- 侧栏背景 `#fafafa`、卡片 `#fff` + `#d4d4d8` 边框 + 8px 圆角、brand 胶囊 999px 中性化（不再绿字绿底）。
- 首页 hero 副标题 `#71717a`；工作台导航悬停 `#e4e4e7`；dcu 增强侧栏变量全面 zinc 化。
- 绿色残留（`#edf5f1`/`#316e50`/`#285c43`/`#aaa39d`）全部清除。

回归项：
- 外壳 chrome 观感不变（工具栏/设置/关于/启动/快捷键/浏览器面板）——`--chrome-*` 块未动，`--dsw-alias-*` 桥映射未动。

## 5. 合规声明

- 未 `git commit`，全部改动留在工作树。
- 仅改 `assets/theme.css`；未同步任何测试断言（受控测试均未锁定本规格改动的值）。
- 未启动/重启 DSH 应用；打包候选已暂存（`deploying`），重启由用户触发，失败自动回滚。