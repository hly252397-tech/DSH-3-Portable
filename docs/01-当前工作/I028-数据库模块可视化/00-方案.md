# I028 — 数据库模块可视化 · 方案（v1，按规范重做）

> **v0 作废声明**：v0 未执行 GCC 前置检查、未查功能清单、未读现有实现，提出的三视图（概览/单列分析/结构关系）中前两个**与既有实现重复**。本文为按规范重做的版本。

## 1. GLOBAL CONTEXT CHECK（12 项）

1. **模块**：`Data/DSH/profiles/web/local/dsh-sidebar-spaces`（02-代码地图：侧栏空间插件）。
2. **职责**：会话侧栏工作台（知识库/数据库/任务管理/Files 等 space）与便携存储。
3. **上游**：DSH 工作台 tab 点击 → `openTab({type:'space-database'})`（client.src.js:504）。
4. **下游**：`DatabaseApp` → `DatabaseContent` → `usePortableState(DB_KEY)` → `/sidebar-spaces/api/store` → `portable-store.js` → 便携 SQLite。
5. **调用点**：client.src.js:1572/1580/2432（tab 注册与渲染）、store API（同源+session 校验）。
6. **公共能力**：echarts（已捆绑）、`PersistenceGate`、`SavedInput`、dss-* 样式体系。
7. **已有类似实现**：✅ **已存在完整可视化**——表格/图表双视图、按字段类型自动选型（条形/饼图/折线/直方/散点，KIND_LABEL:1047）、聚合方式（sum 等）、条数限制（chartLimit）、echarts canvas 渲染（:753-754）。功能清单第 64 行已登记（含 I023 实测记录）。
8. **可复用组件**：上述全部；echarts 初始化模式（ref+canvas）。
9. **公共 API/Schema**：database space schema（version/fields/records）——只读不碰。
10. **历史约束**：portable-store 写路径不可动（写队列/修订冲突保护）；同源+session 校验必须复用。
11. **影响面**：若增强，仅 DatabaseContent 图表分支内改动；表格视图与存储层零接触。
12. **禁改区**：App/resources、运行时槽、node_modules、portable-store 写路径。

## 2. 现状盘点（DatabaseApp 已有能力）

表格视图（增删改字段/记录、导入、查询定位）+ 图表视图（单字段选型 × 类型自动适配 × 聚合 × Top-N）。

## 3. 真实缺口（对照 GitHub 参考，待用户点选后实施）

| 缺口 | 参考 | 价值判断 |
|---|---|---|
| 图表导出 PNG | chartdb/datasette 的分享能力 | 低（右键另存已可行） |
| 概览仪表（字段填充率/类型分布汇总行） | drawDB 卡片布局 | 中——锦上添花 |
| 大记录量性能（现有实现无抽样） | datasette 只读安全模型 | 仅当记录 >2k 时有意义 |
| 多字段组合看板 | — | 需求未出现，不做 |

## 4. 结论

**不做新功能**。现有实现已覆盖「数据库模块可视化」的合理范围且经过 I023 实测。本文保留 GCC 记录与缺口清单，作为未来增强的依据；任何增强须由用户明确点选后按三项对齐流程实施。
