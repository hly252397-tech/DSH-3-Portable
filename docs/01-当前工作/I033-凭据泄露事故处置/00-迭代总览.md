# I033 · 凭据泄露事故处置（搜索令牌时误打印 API Key）

> 事件时间：2026-09-14 23:0x。**由我（本会话智能体）造成**，非用户操作失误。
> 一句话：为排查「带令牌的 `dsh web` URL」，我用**裸关键词 `token`** 去 grep 便携盘数据目录，
> 把 `Data\DSH\.credentials.yaml` 里的 `QWEN_TOKEN_PLAN_CN_API_KEY` **明文打进了会话**；
> 按铁律 1「模型可见即已记录」，该文本随之进入会话日志并随每轮请求发给模型供应商。

## 1. 处置（已做 / 未做）

| 动作 | 状态 | 说明 |
|---|---|---|
| 轮换 `QWEN_TOKEN_PLAN_CN_API_KEY` | **待用户执行** | **唯一真正的补救**——明文已离开本机（进模型上下文），删本地文件不能追回；轮换需供应商控制台登录，智能体做不到 |
| 删除 `Data\Temp\diag-home\.credentials.yaml` | **已做（经用户确认）** | 551 B、写于 **09-11 13:30**、含 5 把 Key 明文（`DEEPSEEK` / `ZAI_CODING_CN` / `QWEN_TOKEN_PLAN_CN` / `SENSENOVA` / `OLLAMA_LOCAL`，仅记键名不记值）。**刻意不做备份**——备份等于把明文再留一份，正好违背处置目的；审计信息记在本文件 |
| 会话日志（`Data\DSH\sessions\…\session.v3.jsonl.zstd*`） | **未动** | 它确实含该事件（压缩存储）；动它=删/改写用户会话历史并可能打断运行中的会话，代价大于收益 |
| 投影缓存（`session_projcache_archive_manager_v2`） | **未动** | 首次深度扫描曾命中 1 处，插件重建后已无 `sk-sp-` 明文（可再生） |
| `Data\Temp\diag-home` 其余 8986 文件 | **未动** | 09-11 一次性诊断 home，属另一件事的残留，清理可另行授权 |

## 2. 教训（供后续 agent，比处置本身值钱）

1. **禁止用裸关键词 grep 凭据类路径。** 搜"令牌"必须匹配带赋值符的模式（`token=`），并**先排除**
   `.credentials.yaml` / `.npmrc` / `.env` / `*token*` 这类文件；输出一律脱敏（前 24 字符 + 长度）。
   本次的失败形态是"匹配太宽 + 直接打印整行"。
2. **压缩格式不能用明文 grep 证明"不存在"。** 会话日志是 `session.v3.jsonl.zstd`
   （zstd 魔数 `28 B5 2F FD`）：**任何明文都 grep 不到**。凡用 grep 做否定性结论，必须先跑
   **已知存在的对照字符串**验格式——本次对照 `9c27219f3b6264c7`（当天确定打印过）同样 0 命中，
   由此当场判定"未命中"结论无效并撤回。**没有对照的"未命中"不是证据。**
3. **进程内令牌在设计上不可发现。** `dsh web` 的令牌是 `randomBytes(32)` 进程启动时现生成、
   只存内存、**从不落盘**（进程命令行只有 `--port 0 --no-open`）。"找一个必然存在的令牌"本身就是
   **错的问题定义**——正确做法是**自己起一个实例**用它自己打印的令牌：
   `<外壳槽>\resources\node\node.exe <外壳槽>\resources\bootstrap.mjs <运行时槽>\node_modules\@deepseek-ai\dsh\lib\bin.js web --port <p> --no-open`
   ＋ `DSH_HOME` / `DSH_PROFILE_DIR` / `DSH_PROFILE_NAME=web` / `DSH_RUNTIME_DIR` 四个环境变量，
   stdout 会打印 `dsh web: http://127.0.0.1:<p>/?token=…`。
4. **自起实例量不到桌面窗口的几何。** 自带浏览器里的视口尺寸与桌面窗口内那个
   WebContentsView 不同，数字不可比：要判"占位更宽还是视图放置"必须在桌面窗口内量
   （截图按像素量 / 把浏览器窗口调成与桌面视图同尺寸 / 由外壳开 DevTools 读 `innerWidth`）。

## 3. 与既有结论的关系

- 本事故**不影响** I031/I032 的交付与验收（`1.0.66-local-9c27219f3b6264c7` 已实机通过）。
- 本次为排查令牌而做的扫描**未发现**本地其它位置的该 Key 明文（`Data\` 深度扫描 6.2 万候选文件 × 320 秒
  ＋ 存储/会话/用户数据/Temp 定向扫描不限时间），但**如上所述，压缩文件内的结论无效**，
  故此处不声称"本地已无明文"，只声明：**已删除一处确认存在的明文副本，其余按用户决定保留**。
