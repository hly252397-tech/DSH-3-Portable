# 接受 profile 插件批量升级：随包矩阵对齐 + 补丁重钉 + 基线重记（2026-09-16）

## 起因（事实，非推测）

- 2026-09-16 **18:58:51–18:59:20**，live profile 里发生一次**插件批量更新 + pnpm 安装**：
  `pnpm-lock.yaml` / `pnpm-workspace.yaml` / `node_modules/.modules.yaml` / `.package-map.json` 全被重写，
  `package.json` 于 18:59:09 被改写。**不是本会话所为**（本会话未运行任何 pnpm/插件命令；
  本会话在 profile 内唯一的写入是 09-15 22:59 注册 `dsh-ui-tweaks`）。
- 后果：**14 项**随包矩阵 pin 与实装不符（12 项社区插件升版 + better-sidebar/cordis-plugin-group 属预期差异），
  且 `src/dsh-view-preload.cts`（09-15 本仓库设置页让位修复）也未重记基线。
- 构建的 UI 基线门禁**正确地**拦下了候选构建（`not ok 477 UI drift: @michengai/dsh-automation/lib/client.js`）。
  **未在未授权情况下改写基线**——先取证、再向用户请示，用户从 A/B/C 中**选 A：接受新版**。

## 采纳的新版（实装 = 新的随包矩阵）

| 包 | 旧 pin | 新 pin（=实装） |
| --- | --- | --- |
| `@michengai/dsh-codex-ui` | 1.1.7 | **1.1.10** |
| `@michengai/dsh-im-connect` | 0.1.49 | **0.1.50** |
| `@michengai/dsh-automation` | 0.1.42 | **0.1.44** |
| `@michengai/dsh-skills-manager` | 0.1.50 | **0.1.52** |
| `@michengai/dsh-archive-manager` | 0.1.42 | **0.1.43** |
| `@michengai/dsh-agency-agents` | 0.1.42 | **0.1.43** |
| `@michengai/dsh-codex-pet` | 0.1.5 | **0.1.6** |
| `@michengai/dsh-btw` | 0.1.7 | **0.1.8** |
| `@michengai/dsh-simplify` | 0.1.4 | **0.1.5** |
| `@michengai/dsh-code-review` | 0.1.0 | **0.1.2** |
| `@michengai/dsh-pua` | 0.3.11 | **0.3.13** |
| `dshmarket` | 1.46.1 | **1.47.0** |

`dsh-better-sidebar` 仍锁 **0.18.0**（本地 link 定制分发，升上游会把定制件换成 npm 包——不动）；
`@deepseek-ai/cordis-plugin-group` 1.0.2 为官方作用域，不参与社区矩阵对比。

## 改法与文件

1. `src/bundled-plugins.ts`：12 项版本 pin 对齐实装（精确匹配、逐项校验命中 1 次）。
2. `test/bundled-plugins.test.ts`：同步期望表（含无引号键 `dshmarket` 与单条 `.version` 断言）与注释。
3. `Customize/Automation-Workbench/build.mjs`：**补丁重钉** `upstreamHash`
   `b4839315…`(0.1.42) → **`cd0bfb30…`(0.1.44)**。
   - 重钉前按 0.1.42 同等的核对：三锚点（`apply` / `runtime` / 原生页面返回行）**各出现 1 次**、
     三个退役标记（`PORTABLE_AUTOMATION_WORKBENCH` / `installPortableAutomationWorkbench` /
     `data-daw-launcher`）**零残留**、`import_react11` 别名在位（6 处）。
   - 重钉后实跑补丁对真文件：**exit 0**，且产物哈希 = 源哈希 `cd0bfb30…`（退役后只校验不改内容）。
   - 不重钉的直接后果：构建会在该步 `throw 'Unsupported automation client'`（补丁以哈希把门）。
4. `customizations/ui/`：按契约以 `--record --note <本文> --evidence <证据 JSON>` 追加新基线。

## 验收证据

- **插件加载（硬门槛）**：以真实 profile 启动诊断实例 → 端口监听 + 打印就绪 URL + **stderr 全空**
  （无 `failed to apply` / 无模块解析失败）。日志：`Data/Temp/diag-web13.log` / `.err`。
- **真机 UI**：`Data/Temp/app-after-upgrade.png`（App 实拍）——automation 0.1.44 的「任务管理」新 UI
  （主代理运行中 + 后台任务列表）正常渲染；右侧 Files 与 任务管理 双标签共存；
  左侧底部用量圆钮与设置齿轮上下分开（09-16 收起态修复在真机生效）；
  输入区右上角 `平价 ¥40.6` + 模型贴右（09-16 布局改动生效）。
- **声明 vs 实机**：semver 求值显示 `agency-agents`（`dsh-client-runtime@0.1.0-rc.8 || 0.1.1-rc.2`）与
  `code-review`（`dsh-agent@0.1.6-alpha.1`）的 peer 范围**不含**我们的 `0.1.5-rc.2`，
  但**实机加载成功、无报错**——按"以实机为准"处理，作为已知风险记录在案。

## 覆盖边界（如实标注）

- 已验证：插件全量加载无失败、真机主界面（左栏/输入区/右侧面板双标签/automation 新 UI）视觉与交互正常。
- **未逐项执行**契约 §5 的完整清单（Files 拖窄/树拖宽/预览、首页同宽同高、暗色状态、
  80%/125%/150% 三档缩放、底部重启按钮隔离桥测试）。这些项如需严格覆盖，应单独一轮；
  本轮以"矩阵对齐 + 补丁重钉 + 加载与主界面验收"为界。

## 镜像源滞后与处置（2026-09-16 记录在案）

- **现象**：构建在离线插件仓装配步失败 —— `ERR_PNPM_NO_MATCHING_VERSION: @michengai/dsh-simplify@0.1.5`；
  装配上游（`DSH_UPSTREAM_REGISTRY`，默认 `https://registry.npmmirror.com/`）当时最新只有 **0.1.4**。
- **核对**：12 个目标版本中 **11 个两源都有**，仅 `dsh-simplify@0.1.5` **官方源有、镜像未同步**
  ⇒ **矩阵 pin 无误**，是镜像滞后，不是版本不对。
- **处置**：**仅对那次构建**把上游源临时指向 `https://registry.npmjs.org/`（源码既有开关；**未改代码、未降版本**）。
  构建 exit 0，随包仓以新版重建（142.8 MB，19:39），部署后六项核验全绿。
- **安装确认**：profile 实装 `@michengai/dsh-simplify 0.1.5`（18:58 批量升级那次装入），声明与实装一致；
  该插件 `exports` 仅 `.`、无 `dsh.client` ⇒ **本就无 `lib/client.js`**（纯宿主侧，只管 `/simplify` 命令）。
- **遗留决定（用户 2026-09-16「先不动」）**：是否让构建在镜像缺版本时**自动回退官方源**
  （改 `scripts/staging-registry-proxy.mjs` / `prepare-runtime.ts`，需一次完整构建验证）——**暂不做**；
  下次再遇镜像滞后仍需手动指源。改动点与环境变量记于本节，便于后续接手。
