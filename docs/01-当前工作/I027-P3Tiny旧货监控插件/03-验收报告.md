# I027 — P3 Tiny 监控插件本机安装验收报告

- 安装时间：2026-09-13 23:57（安装器），dep 格式修正 00:0x，timer 守卫补丁 00:0x
- Profile 路径：G:\DSH-3-Portable\Data\DSH\profiles\web
- 插件目录：Data\DSH\profiles\web\local\dsh-p3-tiny-watch（含 package.json/cordis.patch.yml/README/lib 五文件/test）
- Junction：node_modules\dsh-p3-tiny-watch → local\dsh-p3-tiny-watch（LinkType: Junction，验证通过）
- Profile 依赖声明：`"dsh-p3-tiny-watch": "link:./local/dsh-p3-tiny-watch"`（安装器原写反斜杠路径，已修正为任务要求格式）
- Bundle 登记：dsh.profile.bundles 新增 dsh-p3-tiny-watch（1 次，无重复）
- Loader 状态：首次重载 DSH 启动失败（installSchedule 的 `typeof ctx.interval` 守卫在 Cordis 上无效——访问未注入服务即抛错，DSH 整树退出码 1）；已打最小补丁改用 `ctx.get('timer')` 可选访问；重载后 DSH 正常启动、插件加载、调度器自动触发一次检查（trigger=schedule）
- 插件测试：13/13 pass（补丁后复跑）
- 命令：/p3-tiny-config 在真实客户端斜杠菜单可见（截图证据）；/p3-tiny-status 输入后调度器运行；**命令输出文本未在屏幕捕获**（home 屏执行后无可见输出），列入未验证项
- 公开来源检查：eBay 双源 HTTP 403（本机网络被拒），runs.jsonl 如实记录 UPSTREAM_ERROR，无伪造数据
- 数据目录：Data\DSH\plugin-data\dsh-p3-tiny-watch\（state.json + runs.jsonl，便携盘内；listings/alerts 按需生成）
- startup-error.log：23:59 有 timer-inject 崩溃记录（补丁前）；补丁后无新增
- 插件矩阵零漂移：bundles/local/junctions 均 add-only（pre/post 快照 diff）；lockfile 仅新增本插件 importer 条目；cordis.patch.yml 未变
- 与任务目标的偏差：①安装器依赖路径格式修正（1 行）；②lib/index.js installSchedule 守卫修复（1 函数，补丁已记）——均为使插件可加载的最小修正，未新增功能
- 未验证项：OFFICIAL_BASELINE_NOT_VERIFIED（github 网络中断，--online fetch failed）；命令输出文本；/p3-tiny-check 真实抓取（网络 403）；桌面通知（REAL_NOTIFICATION_NOT_TRIGGERED）；168h 定时器不重复注册（以单测+单次 schedule 运行间接佐证）
- 本轮新增提交：见 git log（feature/p3-tiny-watch）：测试用例、I027/I028 文档、run-tests.mjs 入库、布局测试视口修正
