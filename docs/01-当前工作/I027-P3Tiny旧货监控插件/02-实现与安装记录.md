# I027：实现与安装记录

## 新增文件

```text
plugins/dsh-p3-tiny-watch/**
scripts/install-p3-tiny-watch.mjs
Install-P3-Tiny-Watch.cmd
Test-P3-Tiny-Watch.cmd
```

## 用户入口

```text
/p3-tiny-check
/p3-tiny-status
/p3-tiny-list [数量]
/p3-tiny-config
```

## 安装事务

安装器执行以下步骤：

1. 验证仓库插件源码与真实 Web Profile。
2. 备份 `package.json`、`pnpm-lock.yaml`、`cordis.patch.yml` 及旧插件目录。
3. 复制到暂存目录并验证包名、入口和 bundle。
4. 原子替换 `local/dsh-p3-tiny-watch`。
5. 原子更新 Profile manifest：依赖写为 `link:./local/dsh-p3-tiny-watch`，bundle 只追加一次。
6. 创建 `node_modules/dsh-p3-tiny-watch` Junction。
7. 复查 manifest、入口、bundle 与链接，并写入安装结果。

## 本轮验证

在隔离环境完成：

- `node --check`：核心、来源、存储、编排、插件入口、安装器全部通过。
- `node --test test/*.test.mjs`：13 项通过，0 fail。
- 模拟 Profile 安装：manifest 依赖和 bundle 正确追加；Junction 指向 local 插件；备份与结果文件生成。
- 自动测试未访问真实外部网站。
