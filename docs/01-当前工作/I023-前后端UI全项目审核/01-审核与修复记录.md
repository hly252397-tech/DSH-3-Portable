# I023 审核与修复记录

审核日期：2026-09-08。范围为功能清单 1–74、桌面 IPC、用户入口及本地插件。以下“通过”只适用于明确列出的操作；页面能打开不等于该模块全部业务通过。

后续用户明确要求移除左侧模式切换、模式筛选和底部知识与数据快捷入口，已执行；这些入口的历史恢复结论以 [框选入口移除](03-侧栏框选入口移除.md) 为准，右侧侧边卡片继续保留。

## 环境及验证边界

- 桌面实际运行槽为 `1.0.46-local-b7ed028093d63fc3`。修复前后不强制终止该桌面进程，不覆盖当前槽。候选状态以迭代总览中的最终记录为准。
- 实际 DSH 运行时为 Harness 槽中的 `0.1.2-alpha.5`；`Data/Runtime/dsh-runtime` 中的 `alpha.3` 是保底版本。功能清单的 bundle 版本不代表当前已安装版本。
- 使用本项目 Node 24.20.0、Electron 44.0.0。真实 Profile/Loader 验证在 `artifacts/alignment-audit-20260908/sandbox` 的隔离数据目录完成，使用实际运行时及 Profile 副本，未复制用户会话、账号密钥及全局设置。新建的测试知识库、数据库记录仅在隔离目录中。
- 设置故障验证使用真实 Electron 页面和模拟 IPC 拒绝；浏览器扩展验证使用真实 Electron 扩展会话。前者不算真实主进程 IPC 端到端通过，后者不代表任意第三方扩展都兼容。
- 现有桌面窗口两次取证分别被前台保护及无可见主窗口阻止，未绕过保护，也未将进程存在算作 UI 验收。`startup-error.log` 的原有错误时间为 07:44:08，不能误报成本次新增故障。
- 打包候选首次使用缺少运行时槽/预热缓存的隔离目录时，未在检查脚本的 90 秒期限内就绪，记录为冷启动未通过该期限，不等同于初始化崩溃。随后补齐独立的 alpha.5 运行时槽，并用项目原有缓存预热 API 完成隔离插件缓存；预热日志为 `sandbox-prewarm.log`，首次超时证据为 `packaged-cold-start.log`。这与正式候选已完成预热的更新条件对应，不能据此宣称全新便携盘冷启动通过。

## 已复现及修复

| 问题 | 前端入口与调用 | 后端及结果 | 修复、复验 |
|---|---|---|---|
| 工作模式侧栏切换、过滤缺失，8 项原有测试失败 | `local/dsh-work-mode/lib/client.js` → settingsScope `work-mode`；GET `/work-mode/api/session-modes` | 同包 `lib/index.js` 的 settings 与存储域 | 恢复编程/通用切换及全部/编程/通用过滤；等待设置提交，失败反馈；读取分类失败时取消过滤。真实切换、重载恢复通过，15 项原有测试通过 |
| 知识与数据只有源码，正式 Profile 未启用；启用后等待已不存在的 betterSidebar 服务，导致插件启动失败 | `local/dsh-sidebar-spaces/lib/client.src.js` → `sidebar.footer.action` 的唯一“知识与数据”入口 | 同包宿主 `/sidebar-spaces/api/store`、Inventor 路由 | 改为声明当前实际存在的 slots/locale/sessions/workspaces 服务，旧 betterSidebar 仅可选访问；官方 CLI 安装后同步相对 file 依赖。真实 Loader、创建知识库/条目、读盘及刷新恢复通过 |
| 数据库编辑后立即添加字段，第一次点击被未结束的保存丢弃 | DatabaseContent / SavedInput → usePortableState | POST `/sidebar-spaces/api/store`，SQLite revision 事务 | 保存操作串行排队，使用最新已提交 revision，冲突停止后续写入。增加两项行为回归，真实单次添加字段、记录编辑及 ECharts 图表复验 |
| 知识与数据暗色主题中页签栏及卡片残留白底；小窗口侧栏收起后工作台仍固定左移 240 px | StandaloneWorkbench / CSS / ResizeObserver | 纯前端，无需后端 | 工作台和卡片采用宿主主题变量；所有工作台按真实侧栏尺寸对齐；窄窗口导航和卡片调整。使用暗色、1000×760、减少动画及创建弹窗验证 |
| 桌面设置的异步失败缺少统一反馈，部分按钮不可重试；主题写盘失败仍显示新主题 | `assets/settings.html`、`assets/theme.js` → shell 更新、通知、主题 IPC | `src/main.ts` 对应 handler 及持久化 | 进行中禁用，捕获错误，恢复可操作状态及已保存值；主题等待原生持久化成功后再更新 UI/cache。真实设置页故障注入与主题行为回归通过 |
| 浏览器扩展单项/全局开关只改配置，当前会话仍保留原加载状态 | `assets/browser-workspace.js` → browserToggleExtension/browserSaveSettings | `src/main.ts` → `browser-library.cjs` → Electron Session.extensions | 开关完成前同步原生扩展状态，加载串行化、避免重复加载，只卸载资料库管理的扩展；失败可见。真实 Electron 启用/关闭/重启用/全关/全开序列通过，故障恢复测试通过 |
| 主题按钮在真实宿主状态广播时反复被重建，点击中断 | settings.html 的 renderThemes/localize | 实际主进程 bootstrap 广播 | 相同语言保留节点，只同步选择状态；真实外壳设置页点击湖蓝、主进程读回、恢复 Qoder 通过 |
| 原生浏览器入口隐藏且绘制被关闭；替代插件仅有工具能力 | shell.html 菜单旁浏览器按钮→受限 toggle IPC→browser-panel.html | main.ts 原生视图、资料库和下载 | 恢复现有面板布局；主内容保持全宽，管理页/更多菜单/DSH 弹窗显示时处理原生网页遮挡；主外壳仅新增开关权限，其余管理操作仍限定 browser-panel。工作台可见性单独反馈，避免重新启用主 HTML 中旧的重复浏览器界面 |
| 功能清单把 bundle、源码、已安装和运行验收混作一类 | 功能清单 | 当前 Profile manifest/包 manifest/运行时指针 | 补充当前差异及本次证据索引，未安装和未验证项不计入通过 |
| 只修改外部 UI 资源时打包复用旧候选，更新后看不到修复 | stage-local-desktop-candidate → stageLocalDesktopBuild | portable-desktop-update.ts 本地内容标识 | 改为对完整包的路径和物理文件内容计算确定性摘要，包含 extraResources；回归证明只改 settings.html 产生新槽、旧槽保持原内容、相同内容仍复用 |

## 全项目对应检查

静态清点发现 76 个外壳 IPC 通道、57 个 invoke handler，预加载层的 invoke 未发现缺失 handler。输出为 `artifacts/alignment-audit-20260908/inventory.json`，包含通道、预加载调用方、页面控件及本地插件。静态结果不证明业务数据、点击反馈和异常处理全部正确。

下表覆盖全部 1–74 编号。每行的后端、前端、UI 结论依次列出；“部分通过”表示已列出的操作通过，剩余范围未验证，不计为整个模块验收通过。

| 功能编号 | 真实入口/调用关系 | 后端；前端；UI | 证据与剩余范围 |
|---|---|---|---|
| 1–5 | 启动器→主进程/启动页/窗口及托盘 | 单测及已列启动通过；部分通过；部分通过 | 完整源码及预热的打包候选在隔离环境启动通过；全新环境冷启动、真实用户槽切换及托盘自然退出尚须单独验收 |
| 6–14 | 设置中 DSH 更新→harness/runtime/解压及槽管理 | 单测通过；设置异常反馈通过；部分通过 | 运行时路径已实查；不为复验而升级用户运行时，真实更新事务未验证 |
| 15–21 | 插件安装及文件监控→seed/repair/quarantine/updates | 部分通过；部分通过；部分通过 | 正式安装、隔离 Loader 通过；损坏恢复仅测试，不破坏用户 Profile 制造故障 |
| 22–29 | 桌面桥接、DSH 子进程、预加载及导航 | 单测/Loader 通过；静态通过；未验证 | 当前桌面跨 WebContentsView 的焦点、双击 Logo、进程回收需候选实测 |
| 30–34 | 窗口恢复、内外链导航→主进程策略 | 单测通过；静态通过；未验证 | 不把 URL 白名单/窗口计算单测当作真实点击通过 |
| 35–40 | shell/browser HTML→preload→IPC→main | 契约与实测部分通过；已列入口通过；部分通过 | 隔离完整源码及最终打包候选的启动、设置/浏览器/快捷键子窗口通过，含窄窗口更多菜单；用户当前运行槽及其余焦点/菜单组合未复验 |
| 41 | 设置→主题 IPC→主题存储及页面绘制 | 真实 IPC 保存读回通过；异常恢复通过；已列界面通过 | 完整源码桌面设置点击湖蓝并读回、恢复 Qoder；重复广播保留按钮节点；设置故障注入、知识工作台暗色检查通过，其他页面不扩大计入 |
| 42 | 设置通知开关→偏好持久化→Windows Toast | 单测通过；故障恢复通过；未验证 | 系统通知显示、快速回复未真实发送 |
| 43–48 | 更新设置、资料路径及插件市场批量操作→内部模块 | 单测通过；部分通过；部分通过 | 不绕过 INCOMPATIBLE_RELEASE；真实升级/回滚和整盘写审计未验证 |
| 49–53 | Runtime/Cordis/scope/timeout/invariants→所有用户插件 | 真实 Loader 通过；不适用；不适用 | 属内部框架，由用户插件调用，不应强行生成独立页面 |
| 54 | Codex 侧栏→扩展/设置/任务入口 | 部分通过；已列入口通过；部分通过 | 侧栏与原生设置截图；归档、聊天、跨视图焦点不在通过范围 |
| 55 | IM 助理→渠道设置 | 页面读取通过；入口通过；页面通过 | 未配置外部账号，消息收发、重连未验证 |
| 56 | 定时任务→自动化设置 | 页面读取通过；入口通过；页面通过 | 没有创建会触发外部动作的任务，真实调度未验证 |
| 57 | 技能→技能管理 | 页面读取通过；入口通过；页面通过 | 未实际安装/删除技能或运行技能 |
| 58 | 批量归档/已归档设置→archive-manager | 单测范围通过；入口静态存在；未验证 | 不操作用户真实会话制造验收数据，隔离批量归档待补 |
| 59 | 专家→agency-agents 设置 | 页面读取通过；入口通过；页面通过 | 专家启用与实际模型调用未验证 |
| 60 | 会话上下文视图→dsh-context | 已安装/Loader 通过；静态检查；未验证 | 缺少本次有内容的隔离会话场景 |
| 61 | 设置→侧边卡片；会话右侧/底部工作台→dsh-better-sidebar | 已恢复加载；设置与注册通过；已列界面通过 | 原先“可选替代侧栏/不适用”的判断错误：这是与 Codex 左导航共存的右侧卡片能力。已恢复截图对应版本，详情见 [侧边卡片恢复](02-侧边卡片恢复.md)；终端、Git、会话侧聊等全部业务仍不扩大计为通过 |
| 62 | 连接器→MCP 配置 | 页面读取通过；入口通过；页面通过 | 真实远端服务及工具调用未验证 |
| 63 | 插件→扩展目录→已安装管理→dshmarket | 部分通过；入口通过；部分通过 | 页面首次加载截图不是网络目录成功证据；外部目录/任意安装事务不计通过 |
| 64 | 右侧卡片→知识库/数据库/图表/Inventor/自动化 | 已列操作通过；已列操作通过；已列界面通过 | 左侧底部快捷入口已按用户要求移除，右侧卡片保留；历史真实 SQLite 读写及重载、字段/记录、图表、Inventor 检测通过，未验收 CAD 自动建模 |
| 65 | 侧栏重启按钮→桌面桥接重启 | 单测范围通过；入口静态存在；未验证 | 保留用户当前桌面，不强制触发重启 |
| 66 | 设置→模式配置→work-mode | 设置能力保留；侧栏切换与筛选按用户要求下线 | 旧筛选清理行为测试及真实重载通过；未动用户会话与模式设置，详见后续移除记录 |
| 67 | plan-quota | 未验证；未验证；未验证 | 当前 Profile 未安装，记录路径无源码；I020/I021 明确为在途未挂载原型，不能沿用已交付标记 |
| 68 | 浏览器→资料库/扩展开关→Electron Session | 已列真实操作通过；已列入口通过；部分通过 | 真实浏览器入口/网页/资料库、书签和首页保存读回、合成凭据填充、本机 HTTP 原生下载及文件内容、扩展开关通过；批量导入导出、任意第三方扩展等未全量实测 |
| 69–72 | 构建、启动、便携环境、健康检查脚本 | 测试及构建见总览；不适用；未验证 | CLI 内部脚本无需重复 UI；自然重启健康验收与跨盘冷启动另记 |
| 73 | 类型及测试套件 | 测试结果见总览；不适用；不适用 | 测试基础设施，不是用户界面 |
| 74 | 模型配置→ollama-local→本机 Ollama | 完整链路通过（2026-09-11 隔离 headless 实测）；配置缺陷已修复；UI 选项待重启后查看 | 隔离 DSH_HOME + headless 一次性任务两次真实生成（qwen3:8b，Ollama /api/ps 服务端确认加载）；链路= settings 解析→apiKeyEnv 凭据→openai-completions 适配→reasoning off→none 映射→本地 Ollama。**发现并修复缺陷**：ollama-local 原缺 `apiKeyEnv` 声明与凭据占位，任何真实对话都会 `PI_AI_ERROR: No API key`（本地端点免鉴权但适配器强制要求）；已在真实 settings.yaml 插入 `apiKeyEnv: OLLAMA_LOCAL_API_KEY` 并在 .credentials.yaml 追加占位引用（备份 `Data/Updates/OllamaFix-backup-20260911/`）。模型回复被系统提示中的技能说明带偏属 qwen3:8b 能力面，不计入链路失败 |

## 证据位置与复验

证据根目录为 `artifacts/alignment-audit-20260908/`：

- `baseline-tests.log`：初始 380 项，371 通过、8 失败、1 跳过。
- `build.log`：规范构建及当次测试日志；最新补充测试结果以总览为准。
- `final-ui.log`、`verified-ui.log`：11 条初始真实链路及后续主题检查；`final-core-ui.log`：数据库修复及主题/布局修复后的核心复验。不能将较早失败截图当成最终结果。
- `database-diagnosis.log`：第一次添加字段无结果、第二次成功的原始复现；`test/sidebar-spaces-write-queue.test.ts` 覆盖丢操作及 revision 冲突。
- `settings-regression.cjs`、`settings-error-retry.png`、`settings-theme-committed.png`：真实 Electron 设置页的模拟 IPC 故障验证。
- `extension-electron.log`：真实 Electron 原生扩展开关状态 `[true,false,true,false,true]`。
- `shell-real.log`、`shell-results.json`：完整源码 Electron + 隔离实际 DSH 的 5 组真实 IPC 检查，含设置主题、浏览器、书签/首页、合成凭据、原生下载及快捷键子窗口；对应截图为 `settings-real-ipc.png`、`browser-real-library.png`、`browser-real-download.png`、`shortcuts-real.png`。
- `slot-identity-tests.log`：UI 单独变更的候选身份回归及现有 A/B 更新测试全部通过。最终完整测试和候选交付以 `delivery-build.log` 为准。
- `delivery-verification.json`：12 个关键 UI/主进程文件的源码、打包与最终候选摘要一致；4 个本地插件客户端/宿主文件与实际安装一致。
- `packaged-shell.log`、`packaged-shell-results.json`：最终候选程序在预热的隔离环境中的 5 组真实检查全部通过；`packaged-*.png` 为候选程序截图。检查脚本按窄窗口真实布局从“更多”进入资料和下载，不强制点击隐藏控件。
- `final-visual.log`、`knowledge-dark-compact.png`、`knowledge-dialog-dark-compact.png`：最终非加载态的暗色工作台及弹窗复验。
- `profile-install.log`、`profile-hashes.json`：正式 Profile 安装与源/安装文件一致性；相对 file 依赖防止新增固定盘符依赖。
- `before/`：修改前受影响文件及 Profile manifest/锁文件备份；`initial-hashes.json` 记录既有工作树。

剩余验收按此表执行：新桌面候选正常重启并检查状态与健康日志；当前用户桌面的焦点、通知及尚未覆盖的设置/浏览器组合；隔离混合会话和归档；外部账号/服务/模型对话。任何未验证项继续保留，不能把本轮修复写成“74 项全部验收通过”。

## 2026-09-11 补充验收记录

- **白色界面统一回归与修复（41 号，用户截图反馈"UI没统一"）**：根因链——插件市场侧早已把 profile 依赖版本提升到 1.1.1 矩阵但未实际安装，link 迁移的 `pnpm install` 物化了整个矩阵（codex-ui 0.2.101→1.1.1、agency 0.1.36、skills 0.1.44 等）；codex-ui 1.1 把侧栏配色收敛到自有 `--dcu-sidebar-*` 变量（默认 `#eef7f5` 淡绿），theme.css 的 `.dcu-root` 覆盖块只压了 `background`/hover/border 三个属性，设置页左栏直接引用 `var(--dcu-sidebar-background)` 回退淡绿。修复：theme.css 浅色块补齐 `--dcu-sidebar-*` 整组 8 个变量映射到便携 zinc 调色板 + 防回归测试（codex-ui-style-ownership 新增断言：任一变量缺失即失败）。
- **连带故障两起（同根因，已修复）**：①旧矩阵提升的裸 `schemastery` 在新依赖图中不存在、被 install 剪枝——sidebar-spaces/better-sidebar/work-mode 三个本地插件 `import 'schemastery'` 失败，10:18 一次 DSH 启动因此中断（bridge 一并缺失，自愈恢复）；已 `pnpm add schemastery@3.18.0` 显式入 manifest。②DSH 自愈把加载失败的三个本地插件从 `dsh.profile.bundles` 摘除（侧栏卡片 provider 一度禁用）；已按 LinkMigration 备份恢复 20 项原序。`sidebar-service-lifecycle` 测试此前正是防这类摘除的门禁，现已恢复通过。
- **43–48 更新链路真机证据（2026-09-09 晚，事件链 `Data/Updates/Desktop/events.jsonl`）**：上游 1.0.51 Release 无便携契约 → `incompatible`（已阻止）保持当前槽（不绕过 INCOMPATIBLE_RELEASE 的设计内拒绝真实发生）；本地构建 1.0.51 经真实启动器（双击 `DSH便携版3.exe`）完成槽切换与健康提交 → `completed`；slot-gc 提交点自动回收 1 槽；版本追平后启动自动检查 → `none` 已是最新；`startup-error.log` 零新增。真实升级事务至此已有一组完整通过证据（成功路径）；故意失败自动回滚演练仍未执行，保留未验证。
- **6–14 补充**：活动运行时槽已为 `0.1.2-rc.1`（与桌面清单一致，`Data/Runtime/Harness/current.json`）；隔离 headless 启动两次成功复用该槽 + pnpm 离线仓装配，作为运行时槽只读启动的真实证据。
- **74 完成**：见上表行。附带产出：隔离 headless 验证环境模板（可复用于后续 0.1.5 迁移的隔离 Profile 验证）。
- **待用户重启后一并验收**（合并清单）：①新候选（含主题修复）部署与健康；②本地插件经 junction + schemastery 恢复后真实加载（侧栏知识库/卡片/重启按钮/工作模式）；③65 号侧栏重启按钮真实点击链路；④设置页 Ollama provider 卡片（apiKeyEnv 修复）；⑤**设置页左栏应为白色（本次主题修复的直接验收项）**。
