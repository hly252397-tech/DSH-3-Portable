# I032 · 浏览器面板宽度策略统一（用户截图里那条"右侧白区"的修复）

> 触发：用户发来 8 张实机截图 + 一句「**全是问题**」。逐张取证后，其中一张的**红框正好框住
> 浏览器卡片右侧那片空白**（用红像素栅格程序化提取，确认是用户手绘的闭合矩形，非渲染噪点）。
> 结论：这**不是渲染故障**，而是**两套宽度策略各写一套、而且单位不同**。

## 0. 根因（代码 + 实渲染双重证据）

| 侧 | 位置 | 策略 | 单位 |
|---|---|---|---|
| 页面卡片 | `assets/theme.css` → `body .nArs4W_panel{max-width:calc(100vw - 1040px)}` | 视口 − **1040**（官方内容下限 680px 推得；被 `test/workbench-panel-clamp.test.ts` 钉住） | **CSS px** |
| 外壳原生视图 | `src/browser-panel-layout.ts` → `MAXIMUM_PANEL_WIDTH_MARGIN` | 视口 − **900**（按 ~560 保底；被 `test/browser-panel-layout.test.ts` 钉住） | **DIP** |

两侧**都各有测试钉着、都自称"对话列保底"**，但数值不同、单位还不同 ⇒ 网页缩放 ≠ 1 时偏差为
`margin/zoom − margin`：
- **卡片比原生视图宽** ⇒ 差出来的那一条露出卡片白底 = 用户红框圈住的**右侧白区**；
- 反过来（另一侧偏大）⇒ 原生视图**压住对话列**（不可见覆盖，那段区域的对话点不到）。

## 1. 改动（策略只剩一处）

| 文件 | 改法 |
|---|---|
| `src/browser-panel-layout.ts` | `MAXIMUM_PANEL_WIDTH_MARGIN` **900 → 1040**（统一到页面侧那条、依据官方 680 下限）并导出；新增 `browserPanelMaxWidthCss(panelWidthDip, zoom)` 把上限**折成 CSS px** |
| `src/main.ts` | 新增 `publishBrowserPanelMaxWidth()`：布局时把上限写成页面根上的 CSS 变量 `--dsh-browser-panel-max-width`；导航后 DOM 重置，由 `applyDshDesktopTheme` 复位标记并按同一把尺子重发 |
| `assets/theme.css` | `max-width: var(--dsh-browser-panel-max-width, calc(100vw - 1040px)) !important` —— **单一来源**，兜底值与外壳常量同值 |
| `test/browser-panel-layout.test.ts` | 新增「CSS px 折算」用例 + **跨模块一致性用例**（CSS 兜底常量必须 `===` 外壳常量，防止再各写一套）；面板钳制用例的期望数按新策略更新 |
| `test/workbench-panel-clamp.test.ts` | 改钉新契约（`var(...)` + 兜底 1040），并写明它现在与外壳同源 |

> 原则：**不是把测试改绿，而是把"两把尺子"改成"一把"** —— 一致性用例把这条钉死，
> 以后再单边改数就会直接红。

## 2. 证据

### 2.1 实渲染验收（便携盘 Chrome 152 + puppeteer-core，加载**真实 `assets/theme.css`**）

`artifacts/browser-panel-width-20260914/verify.mjs` → `results.json`：

| zoom | 视口(DIP) | 修复前 卡片→DIP / 视图(DIP) | 修复前错位 | 修复后 卡片→DIP / 视图(DIP) | 修复后错位 |
|---|---|---|---|---|---|
| 1 | 2560 | 1520 / 1520 | 0 | 1520 / 1520 | 0 |
| 1 | 1600 | 560 / 560 | 0 | 560 / 560 | 0 |
| **0.8** | 2560 | **1728 / 1660** | **68** ← 复现用户白区 | 1520 / 1520 | 0 |
| **0.8** | 1600 | **768 / 700** | **68** | 560 / 560 | 0 |
| 1.25 | 2560 | 1260 / 1260 | 0 | 1520 / 1520 | 0 |
| 1.25 | 1600 | 300 / 300 | 0 | 560 / 560 | 0 |

脚本判定：**负向对照复现错位 = 是 ✅；修复后全部对齐 = 是 ✅**（exit 0）。

### 2.2 门禁

| 门禁 | 结果 |
|---|---|
| `tsc` | exit 0 |
| 定向（`browser-panel-layout` + `workbench-panel-clamp`） | 15/15 pass |
| 全量 | 491 tests / 477 pass / 2 fail（**仅** UI 基线两条，改 `assets/theme.css` 的预期漂移，已按契约 `--record`）/ 12 skip |
| `ui-baseline.mjs` | 见 §3（记录后 PASS） |
| `Build-DSH-Portable.ps1` | 见 §4 |

## 3. 基线记录

- 改动文件：`assets/theme.css`（受保护）；记录：本文；证据：`artifacts/browser-panel-width-20260914/results.json`。
- 记录命令：`App/resources/node/node.exe scripts/ui-baseline.mjs --record --note docs/01-当前工作/I032-浏览器面板宽度策略统一/00-迭代总览.md --evidence artifacts/browser-panel-width-20260914/results.json`

## 4. 构建与候选

- 入口：`powershell -NoProfile -ExecutionPolicy Bypass -File Build-DSH-Portable.ps1`
  （沿用镜像覆盖 `DSH_STAGING_REGISTRY` / `DSH_UPSTREAM_REGISTRY=https://registry.npmjs.org/`）。
- 结果：**`build exit=0`**（23:0x → 23:25）。候选槽
  **`Data/Updates/Desktop/slots/1.0.66-local-9c27219f3b6264c7`**，事务 `33dac282-451f-4870-9329-8d0aebe0f596`；
  `pointer.json` 的 `pending` 指向该槽 → 用户下次正常重启部署的就是含本修复的那一版。
- 产物核对：槽内 `resources/theme.css` 命中 `--dsh-browser-panel-max-width`；
  `dist/src/browser-panel-layout.js` 含 `MAXIMUM_PANEL_WIDTH_MARGIN = 1040` 与 `browserPanelMaxWidthCss`。
- 活动槽未被覆盖（A/B 语义保持）。

## 5. 未覆盖 / 未做（如实声明）

- **真实窗口复验需重启**：`src/` 改动只能经 A/B 候选槽生效，本轮不打断正在运行的会话；
  用户下次正常重启后应由启动器部署并健康验证。
- 本次 harness 复现的是**单位/常量不一致**这条链（缩放 0.8 下 68 DIP 错位）。若用户实机缩放不是 0.8，
  偏差量按 `|margin/zoom − margin|` 缩放，结论不变（同一根因）。
- 用户 8 张截图里的**其余元素本轮未动**：右下角 IM 渠道图标浮层、左下 `¥` 计费卡、宠物小卡、
  任务管理面板。它们是插件 UI / 用户定制（`@michengai/dsh-im-connect`、`@kenz1117/dsh-ui-usage-billing`、
  `dsh-codex-pet`、`dsh-sidebar-spaces` 等），**改动等于动用户定制或卸插件，需用户拍板**——已在汇报里列为待确认项。

## 6. 追记（2026-09-14 23:39 用户重启后实机验收）

- **部署链**：`pointer.current` 已切到 `1.0.66-local-9c27219f3b6264c7`（`pending` 清空、`previous` 保留
  `…54d1c533b96a3297` 可回滚）；运行进程命令行指向新槽；`startup-error.log` **零新增**。
- **运行健康**：插件宿主 `[boot] apply 完成 6ms`；**8 个 `link:local/*` 定制链接 8/8 在且可读**；
  `dsh-better-sidebar` 仍 `0.18.0-alpha.0`；profile 清单未被改写（21:35:20）；`local/` 325 文件不变。
- **视觉验收（用户目视确认）：右侧白区消失、面板边缘贴住窗口** ✅ → 本修复**实机闭环**。
- **仍未取到的证据（如实声明）**：本次重启**未触发插件补种**（`plugin-seed.log` 仍为 21:31 旧记录），
  故 I031 的补种护栏本轮未获活体验证——它是防御性改动，要等下次真有补种动作才上场；至少无新增错误。

## 7. 追记二（2026-09-14 深夜）：用户「缩小后又出现了」→ 补齐两处缺口

> 用户重启后确认白区消失（§6 ✅）。随后**把窗口缩小**，白区**复现**。这一轮说明第一版只修了一半：
> 我验的 6 组全是"宽视口"，**窄视口与缩放切换这两条路径根本不在我的用例集里**。

### 7.1 复现与定位（用户截图 + 红框区外的实物）

- 用户新截图（缩小后 1360×900）：**右侧 210×860 的竖条整条纯白**，GitHub 页面内容只到约 1/3 宽就断。
- 量出来的形态：**原生视图 ≈280px**（正好是外壳的 `MINIMUM_PANEL_WIDTH` 下限）、**页面那张卡片宽度为 0** ——
  ⇒ 不是"宽度算错"，而是**页面已经把面板藏了，外壳还在画**。

### 7.2 两处缺口（都在外壳侧）

| # | 缺口 | 机制 | 用户可见形态 |
|---|---|---|---|
| ① | **外壳没镜像页面的隐藏阈值** | `theme.css` 在 CSS 视口 < 1100px 时把 `body .nArs4W_panel` 设 `display:none`（"面板先让位、对话独占"），外壳不知道 → 仍按最小宽度 280px 画原生视图 | **一条 280px 浏览器 + 右边一片白**（本次复现） |
| ② | **缩放变化没有重发布** | 外壳的缩放动作走**程序化** `setZoomFactor`（`id === 'zoom-in/out/reset'`），**不触发** `webContents` 的 `zoom-changed`；而 `publishBrowserPanelMaxWidth` 只在布局时调用 → 缩放一变，下发的 CSS px 上限就作废（同一 DIP 在不同缩放下对应不同 CSS px） | 缩放一次后宽度又开始错位 |

### 7.3 改法（仍是"同一把尺子"）

| 文件 | 改法 |
|---|---|
| `src/browser-panel-layout.ts` | 新增 `MINIMUM_PANEL_VIEWPORT_CSS = 1100`（与 theme.css 的媒体查询同值）+ `shouldHideBrowserPanel(viewportDip, zoom)`：在 **CSS px** 坐标系里判断"页面会不会藏面板"（坏输入一律不隐藏） |
| `src/main.ts` | ① `layoutDshView` 里 `visible = !shouldHideBrowserPanel(...) && …` —— **镜像隐藏**；② 新增 `syncBrowserPanelForZoom()`：作废已下发值 → 重发布 → 重排版；③ 在 `zoom-in/out/reset` 三个动作后显式调用它（程序化路径），并挂 `webContents.on('zoom-changed')` 兜住键盘/默认路径 |
| `test/browser-panel-layout.test.ts` | 新增用例：`shouldHideBrowserPanel` 真值表（含坏输入）+ **跨模块一致性**（外壳常量必须 `===` theme.css 媒体查询的阈值） |

> 本轮**未改** `assets/theme.css`（页面侧规则本就是对的）⇒ 不产生 UI 基线漂移，`ui-baseline` 仍 PASS。

### 7.4 证据

`artifacts/browser-panel-width-20260914/verify-hide.mjs` → `results-hide.json`（真实 theme.css + 直接 import
应用编译产物 `dist/src/browser-panel-layout.js` 做判定）：

| zoom | 视口(DIP) | CSS 视口 | 兜底宽 | 下发变量后 | 页面隐藏 | 外壳隐藏 | 修复前错位 | 修复后错位 |
|---|---|---|---|---|---|---|---|---|
| 1 | 1360 | 1360 | 320 | 320 | 否 | 否 | 0 | **0** |
| 1 | 2560 | 2560 | 900 | 900 | 否 | 否 | 0 | **0** |
| 0.8 | 1360 | 1700 | 660 | 400 | 否 | 否 | **208** | **0** |
| 0.8 | 1600 | 2000 | 900 | 700 | 否 | 否 | **160** | **0** |
| **1.25** | **1360** | **1088** | 0 | 0 | **是** | **是** | **280** | **0** |
| 1.25 | 1600 | 1280 | 240 | 448 | 否 | 否 | 0 | **0** |
| …（共 9 组） | | | | | | | | 全部 **0** |

- 脚本判定：**页面/外壳判定一致 ✅ · 负向对照复现 ✅ · 修复后错位全为 0 ✅**（exit 0）。
- 顺带坐实了"为什么必须有那个变量"：不变量、只吃 CSS 兜底时，缩放 0.8 下两侧差 **208 DIP**（页面 CSS px vs 外壳 DIP）。
- 门禁：`tsc` 0；全量 **492 tests / 480 pass / 0 fail / 12 skip**（+1 条新用例）；`ui-baseline` PASS（无漂移）。

### 7.5 未做 / 待验证

- **实机确认仍需用户重启**：`src/` 改动经候选槽生效；本轮候选槽见 §7.6。
- 我的用例集仍不是穷尽的：跨屏 DPI 缩放（125%/150% 系统缩放）与超宽屏组合未逐一枚举，但判据已统一到
  "CSS px 同一把尺子"，同类偏差按 `|margin/zoom − margin|` 缩放，机理一致。

### 7.6 候选槽（第二版）

- 候选槽 **`Data/Updates/Desktop/slots/1.0.66-local-3509b8cbe8741c12`**（2026-09-15 00:21:51 暂存，
  `pointer.pending` 已指向它；`current` 仍为用户 23:39 部署的 `…9c27219f3b6264c7`）。
- 槽内核对：`dist/src/browser-panel-layout.js` 含 `MAXIMUM_PANEL_WIDTH_MARGIN = 1040` 与
  `MINIMUM_PANEL_VIEWPORT_CSS = 1100`；`resources/theme.css` 仍是 `var(--dsh-browser-panel-max-width, …)` 契约；
  `app.asar` 已更新。
- ⚠ **过程事故（已处置，教训入库）**：本轮第一次构建的后台任务**被我的轮询中断连带杀死**，
  日志停在 `prepare-runtime` 的 `[proxy] GET …`；改为**脱离进程**方式重启构建（`Start-Process`，
  PID 记入 `Data/Temp/build-i032b2.pid`）后一次成功 ⇒ **长构建必须脱离进程启动**，
  不能让它的存活依赖某一次工具调用。
- **更正一处我先前的误判**：我曾按 `Data/Runtime/runtime-*` 的名字判断"被杀构建删掉了产物"并据此提醒
  用户"先别重启"；但当前布局里这些名字**本来就不存在**（真产物是 `Data/Runtime/dsh-runtime` 与打包资源里的
  `runtime-*`）——**没有丢产物，重启无风险**。教训：判断"缺件"前先确认名字与布局对得上。

## 8. 第三处（2026-09-15）：单网页收起顶部标签条（用户「只有浏览器有」）

### 8.1 用户指出的实物

面板顶部叠了**三层**：① DSH 面板标签条（`Files / 知识中心 / 浏览器`）② **网页标签条**（`GitHub - MichengAI… ×`）
③ 导航条（地址栏那一行）。**只有浏览器面板**多出第 ② 层 —— 只开一个网页时它纯冗余、白吃约 40px。
这正是文档 40 §0 的「**优化 1 · 单标签隐藏网页标签条**」，当时因"要连 `src` 几何一起改、成本高于 ②③"
被列为**未实施**（用户 2026-09-15 截图把它指了出来 ⇒ 该决策落地）。

### 8.2 改法（单一来源，仍是同一把尺子）

| 文件 | 改法 |
|---|---|
| `src/browser-panel-layout.ts` | 新增 `shouldShowPageTabBar(tabCount)`（0/1 → 收起；≥2 → 显示；坏数据保持旧行为）+ `resolveBrowserPageTop(tabCount, tabsBar, navBar)` |
| `src/main.ts` | **三处**原生页面视图的垂直偏移统一改用 `resolveBrowserPageTop(...)`；`browserShellState()` 增发 `pageTabBarVisible` |
| `src/shell-contract.ts` | `BrowserShellState` 增 `pageTabBarVisible` |
| `assets/browser-panel.html` | 新增 `.browser.single-page-tab .browser-tabs{display:none}`；`render()` 按 `browser.pageTabBarVisible===false` 切类，并把"+"搬进导航条（地址栏之前）；≥2 时搬回 |
| `test/browser-panel-layout.test.ts` | 新增用例：判定真值表 + 几何（42 / 82）+ 页面必须消费该布尔值并带隐藏规则 |

**功能零损失**：多标签行为完全不变；单标签时"+"仍在（在导航条里）。

### 8.3 证据（真实 assets 实渲染，只打桩外壳 IPC）

`artifacts/browser-panel-width-20260914/verify-singletab.mjs` → `results-singletab.json`：

| 场景 | 标签条 display | 标签条高 | 导航条高 | chrome 总高 | "+"位置 |
|---|---|---|---|---|---|
| 1 个网页 | **none** | 0 | 42 | **42** | 导航条内 ✓ |
| 2 个网页 | flex | 40 | 42 | **82** | 标签条内 ✓ |
| 2→1 往返 | **none** | 0 | 42 | **42** | 导航条内 ✓ |

脚本判定：单网页收起 ✅ · 多网页不变 ✅ · 往返仍正确 ✅（exit 0）。
门禁：`tsc` 0；全量 **495 tests / 483 pass / 0 fail / 12 skip**；`ui-baseline` 已按契约记录
（`customizations/ui/history/2026-09-14T16-28-13-651Z.json`，含三份实渲染证据）并 PASS。

### 8.4 候选槽（第三版）

见 §9。

## 9. 候选槽（第三版）与待验证

- 构建：`Build-DSH-Portable.ps1`（**脱离进程**启动，PID 记 `Data/Temp/build-i032c.pid`）。
- 结果：**`build exit=0`**，候选槽 **`Data/Updates/Desktop/slots/1.0.66-local-820f0eb425651705`**
  （2026-09-15 00:40:05 暂存，`pointer.pending` 已指向它；`current` 仍为用户 23:39 部署的 `…9c27219f3b6264c7`）。
  槽内核对：`resources/theme.css` 含面板宽度变量契约 ✓、`resources/browser-panel.html` 含 `single-page-tab` ✓、
  `dist/src/browser-panel-layout.js` 含 `shouldShowPageTabBar` ✓。
- ⚠ 该槽**同时包含第二版**（窄视口隐藏镜像 + 缩放重发布）**与第三版**（单网页收起标签条）
  ⇒ 用户**一次重启两处都生效**。
- **待用户重启验收**：① 只开一个网页时顶部只剩"标签条？"——应为 **① DSH 标签条 + ③ 导航条**两层（第 ② 层消失、约省 40px），"+"出现在导航条里；② 开第二个网页时第 ② 层回来、行为与从前一致。
