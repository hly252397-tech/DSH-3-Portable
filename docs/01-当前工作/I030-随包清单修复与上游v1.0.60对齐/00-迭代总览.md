# I030 · 随包插件清单修复与上游 v1.0.60 对齐

> 触发：用户问「上游 v1.0.60 那两条要点更新了吗？」→ 实测两条都没做，且发现清单长期陈旧。
> 用户指令：**「修复，然后更新，但是我的定制不能动」**；就 automation 定制的冲突另行明确**「这个可以动」「升到最新版」**。

## 0. 结论

| 项 | 上游 v1.0.60 | 处理 |
|---|---|---|
| 随包插件清单 | — | **修复**：原清单严重陈旧（Codex UI 记 `0.2.102` 而实机已是 `1.1.x`），会让离线包/全新安装把插件装回老版本；现对齐为 16 项并补齐 4 个此前缺失项 |
| Codex UI / IM Connect / 归档 / 智能体 / 技能 / 宠物 / BTW / Simplify / Code Review / PUA / 市场 | 1.1.7 / 0.1.49 / 0.1.42 / 0.1.42 / 0.1.50 / 0.1.5 / 0.1.7 / 0.1.4 / 0.1.0 / 0.3.11 / 1.46.1 | 实机**已达标**（含 Code Review、PUA 已装），清单同步记录 |
| Context | 0.52.1 | 实机 0.50.0 → 由新增的「按随包基线升级」在下次启动补齐 |
| MCP Connector | 0.2.47 | 实机 0.2.45 → 同上 |
| usage-billing | 1.3.0 | 实机未装 → 同上（补种缺失项） |
| 自动化 | 0.1.42 | 实机已 0.1.42；**用户定制补丁原锚定 0.1.40**，经用户授权重钉到 0.1.42（见 §3） |
| better-sidebar | 0.19.1 | **保持 0.18.0 不动**：本仓库以 `link:local/dsh-better-sidebar` 分发用户定制件，按基线升级会把它换成 npm 包＝覆盖定制 |
| 运行环境 | Electron 44.3.0 / Node 24.21.0 / pnpm 11.26.0 | **已升**（含五平台 sha256、@types/node 26.5.1、electron-builder 26.16.1） |
| DSH | 保持 0.1.5-rc.2 | 本来就成立（活动运行时槽即 0.1.5-rc.2） |

## 1. 更正我先前的一处误报

我最初报「你的实际安装偏旧、缺 4 项」是**错的**：当时比对的是 profile 的**声明文件**（`package.json`），而不是**实际安装的包**。以实机 `node_modules` 为准逐个核对后：16 项里 **13 项已达标**，仅 Context / MCP Connector 偏旧、usage-billing 未装。教训与前一轮同源——**要比对运行件本体，不要比对声明**。

## 2. ①修复：随包清单

`src/bundled-plugins.ts` 的 `BUNDLED_PLUGINS` 由 10 项陈旧版本改为 16 项对齐版本，并补齐
`@michengai/dsh-codex-pet` / `dsh-btw` / `dsh-simplify` / `@michengai/dsh-code-review` /
`@michengai/dsh-pua` / `@kenz1117/dsh-ui-usage-billing`。

**顺带补上让这份清单真正生效的机制**：`planBundledPluginSeed` 过去只补「缺失」的插件，已装但低于基线的**不会升**——所以清单再怎么改也影响不到已有 profile。现补上游 v1.0.55 的同名行为：
按随包基线升级**低于**基线的已装插件，并加**两道护栏**：

1. **只升不降**：已装版本不低于基线时一律不动；
2. **本地定制免疫**：用户以 `link:` / `file:` / `portal:` 声明的包（本仓库 8 个定制插件全走 `link:local/…`）
   **永不参与基线比较**——照基线「升级」会把本地定制件换成 npm 发布包，等于覆盖定制。

`installedVersions` / `localLinkPackages` 均为可选入参：不传时退化为旧的「只补缺失」，既有调用方行为不变。

## 3. ②更新：自动化补丁的重钉（用户授权的定制文件改动）

- 实机 `@michengai/dsh-automation` 已是 **0.1.42**，而 `Customize/Automation-Workbench/build.mjs`
  的 `upstreamHash` 仍锚定 **0.1.40** → 补丁以「Unsupported automation client」拒绝生效，
  UI 基线因该受保护文件漂移而失败（两条红灯同源）。
- 实测定位：`0.1.40 = 9319c634…`（补丁声明值）、`0.1.42 = b4839315…`（实机值），二者互不匹配。
- **用户明确授权**「这个可以动」「升到最新版」后重钉：逐项核对 0.1.42 的三锚点
  （`apply` / `runtime` / 原生页面返回行）各出现 **1 次**、`import_react11` 别名未漂移、
  退役标记零残留，再把 `upstreamHash` 改为 0.1.42 的哈希，并在文件注释里留档新旧哈希与核对结论。
- 补丁本身的性质未变：I023/31 已把它退役成**校验器**（只核对输入件是否为已审阅的上游版本 + 契约锚点完整，
  **不注入任何 UI**）；本次重钉不改变该性质，也**不会**把已退役的工作台注入带回来。

## 4. 运行环境升级

`package.json`：`electron 44.1.1 → 44.3.0`、`@types/node 26.4.1 → 26.5.1`、
`electron-builder 26.15.7 → 26.16.1`、`engines.node 24.20.0 → 24.21.0`、
`packageManager pnpm@11.24.0 → 11.26.0`、`config.bundledNodeVersion → v24.21.0`
（五平台 `bundledNodeSha256` 取上游 v1.0.60 的官方值，**不自造哈希**）；
`scripts/prepare-runtime.ts` 的 `bundledPnpmVersion → 11.26.0`；`pnpm-lock.yaml` 同步重解。

⚠️ 运维实证：`Build-DSH-Portable.ps1` 用 `Invoke-WebRequest` 下 Node 会报「无法连接到远程服务器」
（本机 PowerShell 通道问题），而同一 URL 用 `curl.exe` 正常（HTTP 200、37.6 MB）。本轮改用
`curl.exe` 下载 → 解压 → 校验 `win32-x64` 哈希一致 → 备份旧 `Tools/node` → 装入 24.21.0，
从而让构建脚本的哈希检查直接跳过下载（**未改构建脚本**）。后续若再遇同类失败，优先查 PowerShell 通道而非网络。

## 5. 测试与门禁

- 三条被旧值钉死的用例按新实况更新（**只改期望值，不放宽断言**）：
  `bundled-plugins.test.ts` 的目录清单/长度与版本映射（含 `@kenz1117` 作用域白名单）、
  `prepare-runtime.test.ts` 的 Linux x64/arm64 Node 哈希。
- 两条 `plugin-seed.test.ts` 的夹具改为**真实写入 node_modules**：新语义下「声明了但没装」会被判为欠装而安装，
  而这两个用例的场景前提本就是「版本齐全」，夹具原先只声明不落地，属夹具失真。
- 新增 3 例：低于基线要升且高于基线不动、`link:` 定制即使低于基线也不替换、
  以及补种路径同时体现「非 link 低于基线要升 + link 定制不动」（集成级，断言里显式写明 link 被替换就等于丢失用户改造）。

## 6. 变更记录与证据

- 变更记录：本文件。
- UI 证据（用于重新登记受保护基线）：
  `artifacts/automation-0.1.42-repin-20260914/results.json`（16 项断言全过），脚本同名 `verify.cjs`。
  含**反向对照**：篡改过的上游版本必须被拒绝，否则说明锚点检查形同虚设。
  覆盖输入件身份、重钉哈希、三锚点各 1 次、零注入、幂等、退役标记零残留、1920px 宿主复刻渲染。
- **退路**：开工前完整备份 `Data/DSH/profiles/web` 与 `customizations` 到
  `Data/Development/backup-before-v1060-<时间戳>`（逐项核对：8 个定制插件源码 325/325、profile 清单、UI 基线）。

## 7. 构建与产物核验

- **首次构建失败于装配阶段**：`prepare-runtime` 的预取代理默认上游是 npmmirror
  （`DSH_UPSTREAM_REGISTRY`，默认 `https://registry.npmmirror.com/`），而
  **`@michengai/dsh-code-review` 在镜像上仍是 404**（新包同步滞后；同一包在 registry.npmjs.org 为 200，
  已实测对比）。用代码里既有的开关把装配源指向官方源后重跑成功：
  `DSH_STAGING_REGISTRY=https://registry.npmjs.org/` 与 `DSH_UPSTREAM_REGISTRY=https://registry.npmjs.org/`。
  ⚠️ 运维提示：**新发布的内置插件可能落后于镜像**，装配失败先查镜像同步而不是先怀疑包名。
- **候选槽**：`Data/Updates/Desktop/slots/1.0.66-local-54d1c533b96a3297`，事务 `aee4c198-6f36-4e6e-b2ae-afb1a749ee6d`。
- **重启后行为的提前实测**（用**真实判定函数** `planBundledPluginSeed` + 实机 profile 数据，在候选构成本上跑）：

  | 判定 | 结果 |
  |---|---|
  | 计划动作 | `add`，恰好 **3 项**：`dsh-context 0.50.0 → 0.52.1`、`dsh-mcp-connector 0.2.45 → 0.2.47`、`@kenz1117/dsh-ui-usage-billing 未安装 → 1.3.0` |
  | 其余 13 项 | 全部「已达标/更高 → 不动」 |
  | `dsh-better-sidebar` | **「link 定制，跳过」**——护栏按设计生效（否则会被换成 npm 包，等于覆盖用户定制） |
  | 受保护的本地定制包 | 8 个 `link:local/…` 插件全部进入护栏清单 |
  | 护栏逻辑是否真的随包 | 新槽 `app.asar` 内可见 `readLocalLinkPackages` / `localLinkPackages` / `installedVersions` / `needsIgnoredBuildRepair` |

  即：重启后的变更面**正好等于上游 v1.0.60 的差距项，没有任何多余改动**。
- **产物核验（逐项实测，非推断）**：
  | 项 | 实测值 |
  |---|---|
  | 随包 Node（槽内 `resources/node/node.exe --version`） | **v24.21.0** |
  | 随包 pnpm（槽内 `pnpm-package/package.json`） | **11.26.0** |
  | 构建用 Electron（`node_modules/electron/dist/version`） | **44.3.0** |
  | 新清单是否进包（app.asar 搜索） | `@kenz1117/dsh-ui-usage-billing` / `dsh-code-review` / `dsh-pua` / `dsh-context` **全部在** |
  | 槽完整性 | `node.exe` / `app.asar` / `plugins-store.tgz` / `dsh-runtime.tgz` 四项清单哈希 **全 match**；槽名后缀＝源包 sha256 前缀 |
  | 部署指针 | `pointer.pending` 已指向该槽，`current` 仍是上一构建 |

## 8. 真机验证（用户从托盘正常退出重启后，实测）

| 核验项 | 实测结果 |
|---|---|
| 运行中的槽 | **`1.0.66-local-54d1c533b96a3297`**（即本轮候选） |
| 部署事务 | `aee4c198-6f36-4e6e-b2ae-afb1a749ee6d`，事件链 **`completed`「桌面端 1.0.66 已部署并通过启动验证」**（13:32:27Z）；`pointer.pending` 已清空，`slot-gc` 回收 1 个历史槽 |
| 随包运行环境 | 槽内实测 Node **v24.21.0**、pnpm **11.26.0**、构建 Electron **44.3.0** |
| 插件对齐 | **16/16 与上游 v1.0.60 目标一致**（逐项实测；全部已声明、全部在 `dsh.profile.bundles`） |
| 用户定制 | 8 个 `link:local/…` 声明全在、全部仍是 Junction、版本未变（含 `dsh-better-sidebar 0.18.0-alpha.0` 原样）；`assets/**`、`customizations/**` 未改 |
| 启动错误 | `startup-error.log` 无新增（仍停在 9/13）、`plugin-update.log` 无新增（仍停在 9/7） |

**用户侧确认**：设置页「配置管理插件」列表 12 项全部显示「已安装」，**无任何「可更新」**——包括此前一直提示
「0.1.40 → 0.1.42」的「定时任务」，说明补丁重钉后该提示消失、定制恢复生效。

### 8.1 发现并处置的一处隐患：启动补种因 pnpm 仓库不匹配而失败

- **现象**：重启后 `plugin-seed.log`（21:31:35）被写入 `[ERR_PNPM_UNEXPECTED_STORE]`：
  profile 的 `node_modules` 链接自 `G:\.pnpm-store\v11`，而该次 pnpm 命令想用
  `Data\Development\pnpm-home\store\v11`。结果：**启动补种失败，Context / MCP Connector / usage-billing 未自动补齐**。
  该项失败由本轮的「启动失败可见化」按设计弹窗提示，因此用户侧是「看得见」的，不再是静默失败。
- **已确认的机制**：环境里 `PNPM_HOME=…\Data\Development\pnpm-home` 使应用自带 pnpm 的**默认仓库**变成
  `pnpm-home\store\v11`，与 profile 实际链接的仓库不同；只要命令没显式带 `--store-dir=<profile 记录的仓库>`，
  pnpm 就必然报此错。**反证实验**：用**同一套参数并带上** `--store-dir=G:\.pnpm-store\v11 --cache-dir=G:\.pnpm-store`
  在实机上执行，一次成功（`dsh-context 0.52.1` / `dsh-mcp-connector 0.2.47` / `usage-billing 1.3.0` 全部装上）。
- **未完全定论的部分（如实声明）**：静态核对下 `resolvePnpmStoreDir(profileDir, …)` **本应**从
  `.modules.yaml` 读出该仓库并带进参数（已用编译产物实测：现在返回 `G:\.pnpm-store\v11`），
  因此"那一次为什么没带上"的触发条件**尚未复现**。旁证：`.modules.yaml` 在失败之后（21:32:15）被重写。
  不做进一步归因，避免在没有复现的前提下改代码。
- **处置**：用应用自身更新插件时的同一条路径（`pnpm add` + profile 记录的仓库）把三项装上 → 16/16 达标。
  由于现在**没有任何低于基线的插件**，启动补种的计划为空、不再执行 pnpm，该症状**自然不再出现**。
- **遗留风险（供下一轮）**：将来基线再次抬升时，若补种又一次拿不到显式仓库，仍会失败。
  建议下一轮：让补种在「profile 已有 node_modules 但解析不到仓库」时**不要发出不带仓库的 pnpm 命令**，
  而是给出明确可执行的日志/提示（而不是把 pnpm 的仓库错误原样抛出）。
- **✅ 该失败模式已结构性消除（2026-09-14 夜，见 §11）**：把活 store 迁到 **pnpm 的默认位置**
  `Data\Development\pnpm-home\store\v11`（在便携盘 `/Data/` 内），使「带 / 不带 `--store-dir`」都指向同一份仓库；
  实测应用自身 `resolvePnpmStoreDir()` 与 pnpm 默认解析**同值**（注意：本条上方"现在返回 `G:\.pnpm-store\v11`"
  是当时的事实，搬迁后返回新路径）。上面那条代码加固建议仍然保留（防御性，`src/` 本轮未改）。

## 9. 未做 / 未验证（如实声明）

- ~~真机验证~~ **已完成**，见 §8。
- **自动化工作台补丁在真实 DSH 设置页里的渲染**：未单独截图核对；但补丁是纯校验器、不注入 UI，
  且「全壳唯一重启入口 / 原生定时任务页」由既有用例与 UI 基线持续覆盖，用户侧亦确认该页正常。
- `pnpm-workspace.yaml` 里 `minimumReleaseAgeExclude: ['@types/node@26.4.0']` 是上一版升 26.4.1 时留的，
  本次改用 26.5.1 后该条已无实际作用；因未配置 `minimumReleaseAge` 本身，留之无害，故未改动。
- **`yaml` 未改，因为本仓库没有它**：上游 `package.json` 有 `dependencies.yaml 2.9.0 → 2.9.1`，
  而本仓库 `package.json` **根本没有 `dependencies` 段**（已 grep 核实：`dependencies|yaml|electron-updater` 零命中），
  属两侧既有差异（本仓库不使用 `electron-updater`，走自研 A/B 更新器）。因此这一项**无对象可升**，不是遗漏。

## 10. 回退路径

1. **应用层**：不重启即不生效；即使重启，启动器在健康验证失败时会自动回滚到上一槽
   （当前槽 `1.0.66-local-3f0f92c63e4920a5`）。
2. **profile / customizations**：`Data/Development/backup-before-v1060-<时间戳>`（开工前完整备份，
   已逐项核对 8 个定制插件源码 325/325、profile 清单、UI 基线）。
3. **仓库层**：本轮改动集中在 `src/bundled-plugins.ts`、`src/plugin-seed.ts`、`package.json`、
   `pnpm-lock.yaml`、`scripts/prepare-runtime.ts`、`Customize/Automation-Workbench/build.mjs`（经授权）、
   若干用例与本文档；`git diff` 可逐项回退。
4. **UI 基线**：`customizations/ui/history/` 保留每次登记的不可变历史，可回到上一版基线。

## 11. 增量（2026-09-14 夜）：pnpm 仓库迁进便携盘 + 孤立 worktree 清理 + 根目录依赖树修复

> 触发：用户就本仓库两项遗留确认「**按你的建议做**」= ① 把盘根 pnpm store 搬进便携目录并把 profile 重指过去；
> ② 删掉已 `worktree prune` 但仍在盘上的孤立 worktree 目录。

### 11.1 为什么必须搬：§8.1 那处隐患的原始证据与死路排查
- 原文证据（`Data/Electron/UserData/plugin-seed.log` 全文一行）：
  `[ERR_PNPM_UNEXPECTED_STORE] … are currently linked from the store at "G:\.pnpm-store\v11".
   pnpm now wants to use the store at "G:\DSH-3-Portable\Data\Development\pnpm-home\store\v11"`。
  即**是应用自己发出的 pnpm 命令没带仓库参数**，而 pnpm 的默认仓库由环境 `PNPM_HOME` 推导。
- 两条"配置修法"实测都是死路（pnpm 11.26 实测）：
  1. 项目级 `.npmrc` 写 `store-dir=` → 被忽略（`pnpm config get store-dir` 仍 `undefined`，`store path` 仍指 `pnpm-home\store\v11`）；
  2. `npm_config_userconfig` 指向的用户级 npmrc → 同样被忽略。
- `Portable-Environment.ps1:55` 导出的 `PNPM_STORE_DIR`（= `Data\Development\pnpm-store`）在 `src/` 里 **没有任何读取方**
  （`grep` 零命中，只有 `DSH_PNPM_STORE_DIR` 被 `desktop-host.ts:193` 读）→ 是残留变量，**不是设计约定**，不能当搬迁目标。
- 结论：只有把**活 store 放到 pnpm 的默认位置**（且该位置在便携盘 `/Data/` 内），才能在不动代码的前提下让
  「带不带 `--store-dir`」都指向同一份仓库，从根上消除该失败模式。

### 11.2 两个 store 的取证（`fsutil hardlink list` 反查 inode 归属）

| 仓库 | 规模 | 谁在用 | 判定 |
|---|---|---|---|
| `G:\.pnpm-store`（盘根） | 26557 文件 / 564.1 MB | profile 的**活 store** | `agent-base`、`@michengai/*`、`@kenz1117/*` 的 inode 都在此 |
| `Data\Development\pnpm-home\store` | 26399 文件 / 575.8 MB | **仓库开发工作区**（`G:\DSH-3-Portable\node_modules`）+ profile 少数旧链接（`ansi-regex`） | 非 profile 主力仓库 |

- **A ⊇ B**：B 独有仅 **375 文件 / 19.2 MB**。
- 盘根那个 `v11` 目录里除 `files/` 外还有 `metadata/`、`metadata-full/`、`projects/`——因为 `src/plugin-toolchain.ts` 的
  `pnpmStoreOptions()` 令 `cacheDir = dirname(storeDir)`，**store 与 cache 故意共用同一个版本目录**。
  所以搬迁必须**整目录搬**，只搬 `files/` 会丢元数据缓存。
- profile 的 `.modules.yaml` 记录 `storeDir = G:\.pnpm-store\v11`（与该日志一致），且 `nodeLinker: hoisted`
  （`node_modules/.pnpm` 近乎空，硬链接直接铺在扁平树里）——这解释了"文件直连 store"的现象。

### 11.3 做了什么（全部同卷 rename，零拷贝、保硬链接）
1. **旧开发 store 留档改名（非删除）**：`pnpm-home\store` → `pnpm-home\store-dev-workspace-20260914`；
2. **活 store 落到 pnpm 默认位置**：`G:\.pnpm-store\v11` → `pnpm-home\store\v11`，
   `lockfile-verified.jsonl` 随迁，盘根空目录删除；
3. **硬链接补差**：把旧 store 独有的 **375 个文件** 以硬链接补进新 store（不复制数据，同卷 metadata 操作）；
4. **改记录**：`profile/node_modules/.modules.yaml` 的 `storeDir` 改写为新路径（`virtualStoreDir` 不动）；
5. **孤立 worktree 清理**：先**非递归**摘掉 `<孤立目录>\node_modules` 这个指向便携版 `node_modules` 的 Junction
   （递归删会穿进目标把仓库依赖删光）→ 校验便携版 `node_modules` 条数不变（10400 → 10400）、
   `typescript`/`electron`/`.pnpm` 均在 → 再删剩余 177 文件 / 1.3 MB。

⚠️ **过程事故（已修复，必须留档）**：本会话的 `pwsh` 实际是 **Windows PowerShell 5.1**，`Set-Content -Encoding utf8`
会写入 **BOM**；带 BOM 的 `.modules.yaml` 让 `JSON.parse` 直接抛错、应用的 `resolvePnpmStoreDir` 退化为 `undefined`，
**会把这个 bug 原地复活**。已按字节剔除 BOM，并用"文件字节数 = 备份 + 40"（新路径比旧路径长 40 字符）证明
**除这一行外没有任何其它改动**。

### 11.4 证据（全部实跑）

| 验收项 | 手段 | 结果 |
|---|---|---|
| 应用自己会拿哪个仓库 | 跑应用构建产物 `dist/src/plugin-seed.js` 的 `resolvePnpmStoreDir(profile)` | 返回新路径，与 `.modules.yaml` 记录**一致**；`shouldUsePackagedStore` = `false` |
| 原报错的判定条件是否还存在 | 应用内置 pnpm `store path`（不带参数）vs 记录值 | 两者**同值** → 该失败模式已不可能触发 |
| store 完整性 | 旧 store 全部 25669 个文件逐个在新 store 里查在否 | **缺失 0** |
| store 能不能独立喂饱整图 | 临时目录 + 同 `package.json`/`pnpm-lock.yaml` + `link:local` Junction，按应用真实参数 `--store-dir` + `--cache-dir` 跑 `install --frozen-lockfile --offline` | `reused 124, downloaded 0, added 124` —— **124 个包 100% 来自搬迁后的 store，零下载** |
| 用户定制是否还在 | Node 直读 8 个 `link:` Junction 的 `package.json` | 8/8 可读，`dsh-better-sidebar` 仍 `0.18.0-alpha.0` |
| 16 项随包插件 | 逐个读实机 `node_modules/<包>/package.json` 版本 | **16/16 与目标一致，偏差 0** |
| 盘根是否清干净 | `Test-Path G:\.pnpm-store` | `False` |

### 11.5 顺带修复的既有红灯：根目录依赖树有 30 处空壳
- 现象：`tsc` **exit 2** —— `scripts/notarize.mts(1,26): TS2307 Cannot find module '@electron/notarize'`；
  全量测试多一条 `dist/test/notarize.test.js` 文件级失败。`node_modules\@electron\notarize` 是**空目录**。
- 精确扫描（修正了首版脚本对 `_supports-color@7.1.0` 这类 peer 后缀条目与作用域目录的误报）：
  `node_modules\.pnpm` 279 个条目里 **30 处缺少自身 `package.json`**（`semver@7.8.5`、`minimist`、`undici`、
  `chalk@4.1.2`、`node-gyp@12.4.0`… 多为 electron-builder 依赖树），顶层无空洞。
- 修复：只删除**确认 0 文件**的空壳条目 → `pnpm install --frozen-lockfile`：`resolved 278, reused 269,
  downloaded 9, added 278, done`（**exit 0**）→ 复扫 **损坏条目 30 → 0**，`@electron/notarize@3.1.1` 真实可用。
- 如实更正：**上一轮交付时"门禁全绿"这一结果对当前这棵树不成立**。该损坏最可能来自吸收上游 v1.0.60
  （新增 `@electron/notarize`）与运行环境升级期间的半成品安装；本轮一并定位并修好，未把它当作"与我无关"放过。

### 11.6 门禁（修复后重跑，2026-09-14 夜）

| 门禁 | 结果 |
|---|---|
| `App/resources/node/node.exe node_modules/typescript/bin/tsc` | **exit 0**（修复前 exit 2） |
| `App/resources/node/node.exe scripts/run-tests.mjs` | **486 tests / 474 pass / 0 fail / 12 skip，exit 0** |
| `App/resources/node/node.exe scripts/ui-baseline.mjs` | **PASS**（`UI baseline, local links and enabled bundles`） |

### 11.7 未做 / 残留风险（如实声明）
- **旧开发 store 575.8 MB 留档未删**：删除属"难回滚"且不在用户本次授权范围；它的 inode 仍被开发工作区
  `node_modules` 硬链接引用，删掉不会丢数据，只会让开发工作区将来的安装需要重新下载 → **等用户拍板**。
- **强制 `--offline` 的全量重装**会在 `metadata-full` 元数据缓存上缺条目而报 `ERR_PNPM_NO_OFFLINE_META`
  （store 内容本身不受影响；在线流程会自动补齐）。根因是历史上有过 `--cache-dir=G:\.pnpm-store` 的运行，
  元数据分别落在两处缓存里；本轮**未做**缓存合并。
- **没有在运行中的 profile 上跑重装**：pnpm 的一致性判据就是 `.modules.yaml.storeDir`，已用"应用自身代码解析"
  与"pnpm 默认解析"双向验证同值；重链 `node_modules`（含 `@electron-internal/extract-zip` 的 `.node`）在应用运行时
  有半途失败风险，故不做。下次用户装/更新插件时 pnpm 才会真正重链，届时 store 已是旧两个仓库的并集。
- `src/` 未改：`PNPM_STORE_DIR` 残留变量、以及"解析不到仓库时不要发出不带仓库的 pnpm 命令"这一加固建议仍在，
  只是本处失败模式已由仓库位置消除。

### 11.8 本次增量回滚
1. `Rename-Item '<pnpm-home>\store' 'G:\.pnpm-store'`（先 `New-Item` 出 `v11` 目录结构）；
2. `Rename-Item '<pnpm-home>\store-dev-workspace-20260914' 'store'`；
3. 还原 `Data/Development/store-relocation-20260914/modules.yaml.before` → `profile/node_modules/.modules.yaml`。
   目录内另有 `搬迁前基线.txt` 与从孤立目录备份出的两个文件（`孤立目录独有-*`）。
   孤立 worktree 无需回滚：其分支 `sync/upstream-1.0.60` 提交 `5931f99` 完整保存在便携版仓库内，且删除前已核对
   （两个中文名文件**确实在该分支提交里**——首版审计脚本因 `git ls-tree` 默认转义中文名而误判为未跟踪，已复核更正）。
