# I017 任务栏图标小尺寸优化

日期：2026-09-05。对应用户指出的“小尺寸细节挤成一团”，不把 I016 的缩放修复等同于视觉优化。

## 已实施

- 从 `G:/DSH-Codex-Portable/customizations/web/hero-whale.svg` 复用第一版矢量外轮廓，保留鲸鱼方向与整体造型。
- 去掉复杂眼部细碎路径，改为单个透明圆孔；简化腹部切口。正方形画布使用明确的安全留白，不再对专用图标重复裁边。
- 新增源文件 `G:/DSH-3-Portable/assets/icons/taskbar-optical.svg`，通过 `scripts/build-taskbar-optical.cjs` 编译为多帧 `taskbar-optical.ico`。各帧直接从矢量以目标尺寸渲染，包含 16/20/24/32/40/48/64/256px。
- `src/app-icon.ts` 的任务栏路径优先选择专用 ICO，缺失时回退原 PNG。托盘明确继续选择原 PNG。首页、通知、安装器素材未改。
- `src/main.ts` 保留 ICO 的文件型 NativeImage，不调用 crop/resize，避免丢失 Windows 原生尺寸选择及留白；普通 PNG 保留 I016 的高清回退修复。
- `package.json` 已加入专用 ICO 打包资源。

## 验证

- 本轮官方在线基线检查通过：bundled 0.1.2-alpha.3，upstream 0.1.3-alpha.1 / d347e703908d0406b7a7ef80e3a0e594d86b2215。
- TypeScript 与完整测试通过：351 项，350 通过、1 跳过、0 失败。
- 编译脚本验证每帧有真实透明背景且主体非空；测试核对 ICO 目录尺寸、帧边界、RGBA PNG 和打包配置、托盘隔离。
- 1:1 像素对比：`G:/DSH-3-Portable/artifacts/taskbar-optical-20260905/actual-size-comparison.png`。上排是只修正缩放的原图，下排是本轮小尺寸修整。
- 实际 Windows 验证使用独立 Electron 测试窗口，PID 43396 / HWND 177606322，单独 AppUserModelID `dsh.icon.optical.preview`，直接加载专用 ICO，不连接 DSH 或读取用户 Profile。
- 在 Shell_TrayWnd 中找到 Electron 预览按钮，Bounds 为 `1789,1392,44,48`，直接截取屏幕像素：`windows-taskbar-icon.png`；完整任务栏现场记录为 `windows-taskbar-live.png`。这是测试窗口真实任务栏效果，**不是正式 DSH 已部署的证据**。测试窗口 60 秒后自动退出。
- 全桌面 UIAutomation 枚举过慢，已取消该诊断进程，改为限定任务栏元素；未影响 DSH。

![实际尺寸对比](G:/DSH-3-Portable/artifacts/taskbar-optical-20260905/actual-size-comparison.png)

![Windows 测试窗口任务栏实际截图](G:/DSH-3-Portable/artifacts/taskbar-optical-20260905/windows-taskbar-icon.png)

## 被拒绝的图像生成稿

按 imagegen 技能先尝试内置编辑工具，提示要求：保留第一版鲸鱼，精简眼部和腹部、均衡留白、真正透明背景；第二次仅要求去掉背景保留形状。两次文件均为 24bpp RGB，角落 Alpha=255，透明格被画进图像，因此均未采用、未写入产品资产。找到可编辑矢量源后按技能的原生图标优先规则改用矢量代码编辑与编译，没有使用 API/CLI 回退或 Python 抠图。

## 运行保护和待完成项

本轮开始时上次构建进程和终端会话 29849 已不存在，桌面指针无 pending，release 仍为旧产物，因此不能声称上次打包成功。重新调用标准 `Build-DSH-Portable.ps1`，日志保存于 `G:/DSH-3-Portable/artifacts/taskbar-optical-20260905/build.log`，本轮构建终端会话 93655。结束前需核对日志与候选指针，不并发重跑。

本轮未重启正式 DSH。用户当前运行槽仍为 `1.0.46-local-9ce60671cf7bd302`；现有进程启动于 18:37，与上午记录不同，不能沿用旧 PID 41396。待运行时升级队列未修改，SHA256 仍为 `247744A90BCC2E1179B4598A2101454B0F5C8DC110845502A6C3F0160290BCEC`。原任务栏/托盘 PNG SHA256 仍为 `70B1BB9BD8C50EE8A5AD5834EEDF7DB5EEDF91DB7F7E5BC80B44B19287A4C4AF`。

重启与队列暂存尚未获得用户确认。本轮小尺寸图标实现和独立 Windows 实测已做；正式桌面候选切换、启动健康和正式任务栏验收尚未完成。

## 本轮最终构建结果

标准构建已退出且退出码为 0，完整门禁再次通过（350 pass / 1 skip / 0 fail）；候选共享插件环境预热完成。A/B 候选为 `Data/Updates/Desktop/slots/1.0.46-local-66a4ce825883430e`，事务 `56591238-18ff-45c4-b20a-14b4fa441602`。pointer.pending 已指向候选，current 仍为原槽，没有切换。

仓库、release 和候选中的 `taskbar-optical.ico` SHA256 均为 `EFED8EB2366989CC49F64DD65178AC5C909A1B53E9046996F1308291D8DEE040`。读取实际 ASAR 目录的 Windows 路径后，确认候选归档包含任务栏 ICO 优先路由和不裁剪 ICO 的加载逻辑。待运行时升级队列哈希再次核对未变。

已完成可在不重启正式 DSH 前执行的图标实现、真实独立任务栏检查和候选构建。下一步仅在用户确认短暂停机与暂存升级队列后，执行正式切换、健康验证和正式任务栏截图；不能直接让用户普通重启而隐瞒队列副作用。
