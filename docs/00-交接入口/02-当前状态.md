# 当前状态

更新时间：2026-09-08（Asia/Shanghai）

## 当前阶段

最新修复为[插件更新后主题保持](../01-当前工作/I023-前后端UI全项目审核/07-插件更新后主题保持.md)：Codex UI 浅色配色迁至桌面主题层，并在页面重载时恢复。当前桌面为 `1.0.46-local-72e4f13a1a0e65b8`，新候选 `1.0.46-local-58514eeac84927e3` 已通过构建和文件核对，真实界面验收见记录；正常退出并重启后切换。

最新完成[首页卡片尺寸对齐](../01-当前工作/I023-前后端UI全项目审核/06-首页卡片尺寸对齐.md)：活动分布与输入框共用宽度、高度比例及间距，38 项真实界面检查通过，插件已同步正式 Profile，重新加载页面后生效。本轮未变更桌面候选。

便携定制版本为 1.0.46，当前进行 [I023 前后端 UI 全项目审核](../01-当前工作/I023-前后端UI全项目审核/00-迭代总览.md)。修复、测试、候选与真实运行验收分别记录；不能把候选暂存当作已更新当前桌面。

此前的[浏览器卡片替换](../01-当前工作/I023-前后端UI全项目审核/05-浏览器卡片替换.md)已包含在当前桌面槽 `1.0.46-local-72e4f13a1a0e65b8` 中：卡片接入顶部入口共用的原生浏览器，旧 iframe 实现移除。

历史上游发布记录（2026-08-20）：

`v1.0.13` 未签名多平台版本已正式发布。六个功能插件与 dshmarket 已纳入离线仓库；评审列出的运行时、插件生命周期、归档完整性问题已完成代码修复。Windows x64、macOS arm64/x64 与 Linux x64 均通过远端构建、打包和桌面启动冒烟。

## 活动迭代

[I023 前后端 UI 全项目审核](../01-当前工作/I023-前后端UI全项目审核/00-迭代总览.md)。其他历史迭代保留原记录，本轮不替它们宣布验收完成。

## 有效风险

- macOS/Linux 尚未在本机验证，必须以对应 CI 打包与冒烟结果为准。
- 当前发布暂不签名；Windows SmartScreen 与 macOS Gatekeeper 可能提示来源不受信任。凭据完整时 CI 仍会自动签名和公证。
- 离线插件仓库和预装官方运行时只在 `prepare-runtime` 后生成；未装配时不会补种，也不阻断启动。
