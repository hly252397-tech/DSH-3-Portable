外壳顶栏改版已落盘并完成离屏验收（**构建/部署待执行**）：改前顶栏右侧 2099px（82%）连续空白并非设计，而是 `#command-center`、`.bar-right`、`.divider` 三处被 `display:none` 关闭、而后端 `find`/`desktop-settings`/`check-updates` 早已实现。按用户拍板改为「左段 返回/前进 ｜ 文件·编辑·视图·帮助（13px+竖线）」+「中段居中定宽 320px 搜索胶囊」+「右段 状态·重启 ｜ 搜索·设置·更新」，重启图标换自绘电源图形并保留两步确认。15/15 断言 PASS（2560/1280 双宽度、深浅两主题、无重叠、最长空白 2099→844px），见 [本轮记录](../01-当前工作/I023-前后端UI全项目审核/39-外壳顶栏改版.md)。**未执行构建**：改前基线即因**外部在途改动**（`assets/theme.css` 17:21 回退 + 黑洞客户端 17:25 同步）报 3 处漂移使 `pnpm test` 变红，且同期另一会话正在同一 A/B 管线上构建部署（09:24:41Z 本地构建入槽 → 09:25:51Z 候选未过启动验证自动回退），按契约不代他人补记基线、不抢占管线。

窗口动画修复已完成：Windows 主窗口的外壳、DSH 与文件面板为独立合成层，真正最小化时隐藏并恢复，最大化/还原时保持内容可见、仅延后布局，避免出现半透明、图标栏压缩和错位中间帧；本轮记录见 [最小化动画收敛](../01-当前工作/I023-前后端UI全项目审核/36-最小化动画收敛.md)。

重启与底部连接状态排版已完成：顺序为“重启中 / 连接中 / 设置”，三项贴底对齐并保持同一行；窄栏下重启状态文案不再截断，独立 Electron 窗口与针对性测试通过。全局 UI 基线仍受本轮之外的 `dsh-better-sidebar/lib/client.js` 漂移影响，详见 [本轮记录](../01-当前工作/I023-前后端UI全项目审核/34-重启连接状态提示收敛.md)。

黑洞方案2高级视觉升级已验收：深色成长横幅、知识密度仪表、最近吸收/复用潜力、状态筛选、明亮列表/详情、轻量提醒条；真实 Loader/窗口、缩放/窄栏与全测通过，见 [本轮记录](../01-当前工作/I026-黑洞空间/03-光年档案UI.md) 与 [Design QA](../../design-qa.md)。

黑洞对话相关资料提醒增强已完成：支持中文自然表达匹配，提醒展示资料摘要，可直接引用到当前对话；源码、真实黑洞窗口与全量门禁通过，记录见 [本轮记录](../01-当前工作/I026-黑洞空间/05-对话相关资料提醒增强.md)。

黑洞首次打开时首页活动分布已修复：黑洞面板首次出现即卸载活动卡片，关闭后恢复，重复打开不重复挂载，见 [本轮记录](../01-当前工作/I026-黑洞空间/04-首次打开布局时序.md)。

底部重启/设置已改同行：展开等宽，收起保留图标，缩放/点击及全测通过，见 [本轮记录](../01-当前工作/I023-前后端UI全项目审核/32-底部重启设置同行.md)。

定时任务入口已收敛：只留左侧，原管理页直接可用；移除底部/右侧自动化和第二张源码卡片，旧构建入口已退役，实机/全测通过，见 [本轮记录](../01-当前工作/I023-前后端UI全项目审核/31-定时任务与源码入口收敛.md)。

会话右上工具组边界已整理：取消突兀竖线/胶囊框，统一按钮高度和留白，实机及全测通过，见 [本轮记录](../01-当前工作/I023-前后端UI全项目审核/30-会话工具组边界整理.md)。

主要入口重复复查完成：29轮发现的自动化故障已通过31轮入口收敛解决；其他同名功能分类见 [审查记录](../01-当前工作/I023-前后端UI全项目审核/29-主要入口重复功能复查.md)。

重复原生右栏入口已移除，右卡片/底部面板开关保留；实机和全测通过，见 [本轮记录](../01-当前工作/I023-前后端UI全项目审核/28-移除重复原生右栏入口.md)。

会话顶栏分组对齐：已验证，见 [本轮记录](../01-当前工作/I023-前后端UI全项目审核/27-会话顶栏分组对齐.md)。

右栏拖动线居中：已验证，见 [本轮记录](../01-当前工作/I023-前后端UI全项目审核/26-右栏拖动线居中.md)。

黑洞卡片菜单图标与对齐：已验证，见 [本轮记录](../01-当前工作/I023-前后端UI全项目审核/25-卡片菜单黑洞图标对齐.md)。

双右侧栏互斥：已修复并通过19组实机检查，见 [本轮记录](../01-当前工作/I023-前后端UI全项目审核/24-双右侧栏互斥.md)。

# 功能清单

**首页「任务最近活动分布」卡片已整卡下线**：任务快照只带每个任务的 `updatedAt`，12 个月里通常只有 1 个月有数据，热力图与柱状图都撑不起这块版式；卡片、数据函数、文案常量与全部 `.dss-activity*` 样式一并删除，只保留清理旧宿主的退场逻辑。输入卡继续沿用 768 px 内容列与 120 px 高度变量。47 项离屏真实几何断言（真 Chrome 152）通过，见 [下线记录](../01-当前工作/I023-前后端UI全项目审核/42-首页活动分布卡片下线.md)。

黑洞入口已移到首页标题右侧并收束为轻量动作组：真实 Electron 通过宽屏、缩放与窄屏验收，见 [本轮记录](../01-当前工作/I026-黑洞空间/06-首页标题黑洞入口对齐.md)。

**首页排版与快捷任务已核验**：取消标题与隐藏区域的大块占位，四卡改为紧凑布局，四类八个预填入口和全测通过，新基线已登记，见 [本轮记录](../01-当前工作/I023-前后端UI全项目审核/23-首页排版与快捷任务核验.md)。

**黑洞文本记忆版已接入**：创建、保存、去重、刷新恢复、会话收纳、关联提示和草稿追加引用已实测；独立模型与知识中心双向联通未开放，全仓尚有并行 UI 基线失败，详见 [I026 实机落地](../01-当前工作/I026-黑洞空间/01-实机落地.md)。

**底部终端四角已修复**：内容按内侧圆角显式裁切，拖动条端点内收；真实命中/拖动/菜单/缩放和全测通过，已保存新基线，见 [四角记录](../01-当前工作/I023-前后端UI全项目审核/22-底部终端四角裁切.md)。

**首页紧凑高度已同步**：双卡默认 120 px 等高，输入工具栏贴近底边；缩放、多行输入和最终全测通过，新基线及完整修改记录已保留，见 [本轮记录](../01-当前工作/I023-前后端UI全项目审核/21-首页紧凑高度与工具栏贴底.md)。（**已作废**：活动卡 2026-09-13 整卡下线，"双卡"只剩输入卡；高度变量与 120 px 继续生效，见 [42](../01-当前工作/I023-前后端UI全项目审核/42-首页活动分布卡片下线.md)）

**UI 维护固化已落地**：当前要求、源码/证据快照、逐次修改记录、漂移与本地链接校验已接入全量测试，见 [维护契约](../03-技术架构/UI定制维护契约.md) 及 [本次固化记录](../01-当前工作/I023-前后端UI全项目审核/20-UI固化与交接记录.md)。

**首页双卡等高已同步**：活动分布和输入框共用默认高度，真实缩放及侧栏组合验证通过，见 [双卡等高](../01-当前工作/I023-前后端UI全项目审核/19-首页双卡等高.md)。（**已作废**：活动卡 2026-09-13 整卡下线，见 [42](../01-当前工作/I023-前后端UI全项目审核/42-首页活动分布卡片下线.md)）

**工作区条底部试排已同步**：已移到输入框下方并去掉边框，工作区与模式菜单及缩放验证通过，见 [底部试排](../01-当前工作/I023-前后端UI全项目审核/18-工作区条移到底部.md)。

**底部面板开关已恢复**：适配新版对话区插槽，恢复面板定位与主区让位，真实开关、拖动、刷新和缩放验证通过，见 [开关恢复](../01-当前工作/I023-前后端UI全项目审核/17-底部面板开关恢复.md)。

**文件预览最小宽度已修复**：预览区最小与文件树等宽，主区同步让位，真实拖动、文件打开、开关及缩放验证通过，见 [宽度约束](../01-当前工作/I023-前后端UI全项目审核/16-文件预览最小宽度.md)。

**首页三块统一已同步**：工作区条、活动分布与输入框同宽同圆角，见 [首页三块统一](../01-当前工作/I023-前后端UI全项目审核/15-首页三块统一.md)。

**工作台四角及分隔条端点已同步**：外框统一留白与小圆角，右侧取消重复套框，见 [四角整理](../01-当前工作/I023-前后端UI全项目审核/14-工作台四角整理.md)。

**底部导航对齐已同步**：统一自动化、重启、设置的图文起点与间距，见 [底部导航对齐](../01-当前工作/I023-前后端UI全项目审核/13-底部导航对齐.md)。

**知识中心视觉整理已同步**：统一内容列、卡片层级及操作区，真实页面与 Electron 缩放检查通过，见 [视觉整理记录](../01-当前工作/I023-前后端UI全项目审核/12-知识中心视觉整理.md)。

**重启入口显示已修复**：消除 SVG 只读属性异常，改用正式底部插槽；真实 Loader/Electron UI 与 preload IPC 验证通过，未触发用户桌面重启，三项对齐与证据见 [重启入口恢复](../01-当前工作/I023-前后端UI全项目审核/11-重启入口恢复.md)。

**圆角板块底边已纠正**：文件预览与文件树取消额外底部留白，与左栏、主区底边对齐，实际客户端复验见 [圆角边框记录](../01-当前工作/I023-前后端UI全项目审核/10-板块圆角边框.md#底边对齐纠正)。

**2026-09-12 圆角板块外观已同步**：左侧工作栏、主内容区、右侧工作面板及独立卡片统一细边框和圆角；12 项真实页面检查与 4 项 Electron 缩放检查通过，刷新生效。见 [10-板块圆角边框](../01-当前工作/I023-前后端UI全项目审核/10-板块圆角边框.md)。

**2026-09-12 分栏修复完成**：当前 Harness 0.1.5-rc.2 / Codex UI 1.1.2；统一右侧文件面板与首页让位宽度，消除覆盖。11 项真实页面检查及 4 项 Electron 缩放检查通过，本地 `dsh-sidebar-spaces` 已同步实际加载路径，现有页面刷新生效；三项对齐见 [09-分栏遮挡修复](../01-当前工作/I023-前后端UI全项目审核/09-分栏遮挡修复.md)。

更新时间：2026-09-09（Asia/Shanghai）

**本次浏览器入口与布局修复**：移除外壳顶部重复入口，保留右侧浏览器卡片；修复隐藏快照覆盖标签栏和缩放坐标换算。前端、后端及实际 UI 待本次复验，见 [08-浏览器入口与顶部遮挡](../01-当前工作/I023-前后端UI全项目审核/08-浏览器入口与顶部遮挡.md)。

本文档记录 DSH-3-Portable 所有已实现功能。每增加一个功能，在对应分类下添加条目并描述。

**三项对齐登记要求**：新增、修改、下线功能时，必须执行 [质量门禁第 6 节](../01-当前工作/便携版3-变更与答复质量门禁.md#6-前后端与-ui-三项对齐强制门禁)，在受影响条目的描述中登记后端、前端、UI 各自的验收状态，并链接实际检查或验收记录。已有“✅ / 已实现”仅为历史实现登记，不自动代表三项对齐已验收；尚无证据的三项对齐状态按“未验证”处理。修改后须更新受影响部分的证据和状态，下线后同步说明入口及调用方处理结果。

**本轮审核索引**：1–74 全部编号的对应关系、分项结论、证据和未验证范围统一见 [I023 审核与修复记录](../01-当前工作/I023-前后端UI全项目审核/01-审核与修复记录.md)。本轮修复涉及 1、35、38–41、45、64、66、68；打包及实际运行状态见 [I023 总览](../01-当前工作/I023-前后端UI全项目审核/00-迭代总览.md)。其中知识库读写、工作模式保存、数据库字段及图表、设置真实主题 IPC、浏览器部分链路已实测；整个项目没有因此自动获得全量验收通过。

**当前版本说明**：本轮实际 Harness 运行时槽为 `0.1.2-rc.1`（与桌面清单一致）。2026-09-11 插件矩阵经 link 迁移的 install 物化后已实际进入 1.1.1 时代：Codex UI 1.1.1、专家 0.1.36、技能 0.1.44（其余组件随矩阵同批），并显式补入裸 `schemastery@3.18.0`（本地插件运行时依赖）；`dsh.profile.bundles` 已按 LinkMigration 备份恢复 20 项原序。社区插件表仍列 bundle 目标版本，与本说明的实际安装版本含义不同。

---

## 一、应用入口与生命周期

| # | 功能名称 | 文件 | 状态 | 描述 |
|---|----------|------|------|------|
| 1 | 主进程入口 | `src/main.ts` | ✅ | Electron 主进程启动、窗口管理、浏览器面板、托盘、通知、主题、DSH 进程生命周期 |
| 2 | 应用标识 | `src/app-identity.ts` | ✅ | APP_NAME、UserModelID、ToastActivator 定义 |
| 3 | 应用退出逻辑 | `src/app-lifecycle.ts` | ✅ | 应用关闭/退出流程管理 |
| 4 | 图标路径解析 | `src/app-icon.ts` | ✅ | 托盘/任务栏/通知/ICO 图标路径解析 |
| 5 | 启动进度百分比 | `src/startup-progress.ts` | ✅ | 启动进度计算与展示 |

---

## 二、DSH 运行时管理

| # | 功能名称 | 文件 | 状态 | 描述 |
|---|----------|------|------|------|
| 6 | 运行时路径解析 | `src/runtime.ts` | ✅ | DSH 运行时路径计算 |
| 7 | A/B 运行时槽管理 | `src/runtime-slots.ts` | ✅ | 激活/提交/回滚运行时槽 |
| 8 | 预构建运行时复制 | `src/runtime-prebuilt.ts` | ✅ | 官方预构建运行时复制到本地 |
| 9 | pnpm 布局解析 | `src/runtime-pnpm-layout.ts` | ✅ | pnpm 目录结构解析 |
| 10 | 运行时归档解压 | `src/runtime-archive.ts` | ✅ | 运行时压缩包解压（GNU/BSdTar 兼容） |
| 11 | 子进程运行时提取 | `src/extract-runtime.ts` | ✅ | 子进程方式提取运行时 |
| 12 | 运行时候选构建 | `src/harness-runtime-candidate.ts` | ✅ | DSH 运行时候选版本构建 |
| 13 | 启动阴影验证 | `src/harness-shadow.ts` | ✅ | 启动过程阴影验证 |
| 14 | 运行时更新策略 | `src/harness-update.ts` | ✅ | DSH 运行时更新逻辑 |

---

## 三、插件与 Profile 管理

| # | 功能名称 | 文件 | 状态 | 描述 |
|---|----------|------|------|------|
| 15 | 插件清单定义 | `src/bundled-plugins.ts` | ✅ | 所有官方+社区插件版本定义 |
| 16 | 插件首次补种 | `src/plugin-seed.ts` | ✅ | 插件首次安装到 profile |
| 17 | pnpm 工具链路径 | `src/plugin-toolchain.ts` | ✅ | pnpm 工具链/商店路径解析 |
| 18 | Profile 自修复 | `src/profile-repair.ts` | ✅ | Profile 损坏自动修复 |
| 19 | Profile 隔离检疫 | `src/profile-quarantine.ts` | ✅ | 问题 Profile 隔离 |
| 20 | 待应用插件更新 | `src/profile-updates.ts` | ✅ | 插件更新管理 |
| 21 | Profile 文件监控 | `src/profile-watch.ts` | ✅ | Profile 文件变化监控 |

---

## 四、桌面桥接与 IPC

| # | 功能名称 | 文件 | 状态 | 描述 |
|---|----------|------|------|------|
| 22 | 桌面端宿主服务 | `src/desktop-host.ts` | ✅ | 插件安装/卸载 pnpm 桥接 |
| 23 | 桌面桥接插件 | `src/desktop-bridge.mts` | ✅ | 向 DSH 注入桌面端能力 |
| 24 | 桥接客户端注入 | `src/desktop-bridge-client-source.ts` | ✅ | 桥接客户端注入代码；双击 DSH Logo 切换侧栏 |
| 25 | DSH 子进程管理 | `src/dsh-process.ts` | ✅ | DSH 子进程启动/停止 |
| 26 | DSH 引导脚本 | `src/dsh-bootstrap.mts` | ✅ | DSH 引导逻辑 |
| 27 | 会话 Cookie 清理 | `src/dsh-session-cookies.ts` | ✅ | DSH 会话 Cookie 清理 |
| 28 | WebContentsView 预加载 | `src/dsh-view-preload.cts` | ✅ | DSH WebContentsView 预加载脚本；侧栏最大宽度固定 252 px |
| 29 | 进程树终止 | `src/process-control.ts` | ✅ | 进程树安全终止 |

---

## 五、窗口与导航

| # | 功能名称 | 文件 | 状态 | 描述 |
|---|----------|------|------|------|
| 30 | 窗口状态持久化 | `src/window-state.ts` | ✅ | 窗口位置/大小记忆 |
| 31 | 窗口导航协调 | `src/window-navigation.ts` | ✅ | 窗口内导航协调 |
| 32 | 窗口图标替换 | `src/window-icon.ts` | ✅ | 窗口/favicon 图标替换 |
| 33 | URL 白名单 | `src/navigation.ts` | ✅ | URL 白名单/导航策略 |
| 34 | 外链逃生路由 | `src/escape-routing.ts` | ✅ | 外链/逃生路由处理 |

---

## 六、外壳 HTML 面板

| # | 功能名称 | 文件 | 状态 | 描述 |
|---|----------|------|------|------|
| 35 | 外壳 IPC 契约 | `src/shell-contract.ts` | ✅ | 外壳状态/动作类型定义 |
| 36 | 外壳动作定义 | `src/shell-actions.ts` | ✅ | 外壳动作与本地化 |
| 37 | 外壳 IPC 安全策略 | `src/shell-ipc-policy.ts` | ✅ | 外壳 IPC 安全控制 |
| 38 | 外壳预加载脚本 | `src/shell-preload.cts` | ✅ | 外壳预加载 |
| 39 | 浏览器面板布局 | `src/browser-panel-layout.ts` | ✅ | 卡片与顶部入口共用原生浏览器；挂载、定位、隐藏、设置/菜单遮挡已实测，过小卡片显示尺寸提示；见 [替换记录](../01-当前工作/I023-前后端UI全项目审核/05-浏览器卡片替换.md) |
| 40 | 浏览器面板预加载 | `src/browser-panel-preload.cts` | ✅ | 浏览器面板预加载 |
| 75 | 功能板块面板 | `src/feature-panels.ts` + `assets/feature-panels.html` | ✅ | 「视图 → 功能板块」打开 74 项条目索引；按视觉板块（侧栏/外壳/设置/浏览器/卡片/启动更新/本地插件/Profile）8 组聚合；条目带名称/文件路径（点击复制走主进程剪贴板）/描述。开发态与打包态均可见（2026-09-09 修订，原仅开发态）；见 [I024 实施记录](../01-当前工作/I024-功能板块开发面板/00-迭代总览.md) |

---

## 七、桌面功能

| # | 功能名称 | 文件 | 状态 | 描述 |
|---|----------|------|------|------|
| 41 | 全局桌面主题 | `src/desktop-theme.ts` | ✅ | 浅色主界面、侧栏、设置与外壳底色统一白色，交互状态仍可区分；已移除 Qoder 工作台预设，兼容降级与本轮记录见 [移除 Qoder 工作台主题](../01-当前工作/I023-前后端UI全项目审核/38-移除Qoder工作台主题.md) |
| 42 | Windows Toast 通知 | `src/desktop-notifications.ts` | ✅ | Windows 通知集成 |
| 43 | 桌面应用更新 | `src/desktop-updater.ts` | ✅ | 桌面应用更新检查；托盘对缺兼容契约的阻止状态展示说明且不提供下载（2026-09-09）。对齐：后端 `buildDesktopTrayItems` 分支由 `dist/test/desktop-updater.test.js` 覆盖通过；设置页徽标/反馈分支见 45 条 |
| 44 | 便携模式路径重定向 | `src/portable-paths.ts` | ✅ | 便携模式路径计算 |
| 45 | 便携版 A/B 候选更新 | `src/portable-desktop-update.ts` | ✅ | 便携版 A/B 更新机制；2026-09-09 增强：①缺便携契约的上游新版本进入独立 `incompatible`（已阻止）状态，设置页显示琥珀色「已阻止」徽标与诊断信息，不再渲染成红色「更新失败」；②更新源抽象 `PortableDesktopReleaseSource`（API/资产域/制品名按构建期配置解析，非法配置回退默认源）；③`prunePortableDesktopSlots` 在健康提交与本地构建暂存两个提交点回收历史槽与下载缓存（指针引用槽永保留，未引用槽默认保留最近 3 个，审计写 `events.jsonl` source=slot-gc）。**2026-09-11 发布源三级解析**：`resolvePortableReleaseSource` 按 `<便携根>/Data/config/desktop-release-source.json`（机器本地覆盖）→ `resources/release-source.json`（`prepare-runtime` 把 `DSH_PORTABLE_RELEASE_SOURCE` 烘焙进打包资源，CI 发布构建注入自有仓库）→ 内置默认（上游仓库，缺契约被拦成已阻止）依次解析，配合 GitHub Actions 在 `v*` 标签自动生成便携兼容契约并发布 Release，应用内「检查更新」即可对自有仓库自动下载并 A/B 部署。对齐：后端状态机与回收由 `dist/test/portable-desktop-update.test.js` 覆盖（0 fail）；真机证据（2026-09-09 晚 events.jsonl）：上游 1.0.51 无契约→`incompatible` 保持当前槽、本地构建 1.0.51 双击启动器路径槽切换→`completed`、slot-gc 自动回收、启动检查→`none` 已是最新、startup-error.log 零新增；设置页徽标分支已实现，截图留档 `%LOCALAPPDATA%\desktop-control\captures\20260910_000453_241306478.png` |
| 45b | DSH 运行时更新（A/B 候选 + 官方运行时升级） | `src/harness-update.ts`、`src/harness-runtime-candidate.ts`、`src/plugin-seed.ts` | ✅ | 官方 `@deepseek-ai/dsh` 运行时的发现、受信核对、候选槽装配、影子验证与 A/B 切换。2026-09-11 修复三缺陷：①官方运行时统一按发布时间解析（`resolutionMode: time-based` 同时写入生成的 `pnpm-workspace.yaml`、安装参数与既有运行时目录），修复「候选家族版本不对齐」导致的 `0.1.5` 升级必然失败；②插件「关于」页点更新不再原地改写活动运行时槽（会留下「清单新版本 + node_modules 旧版本」的坏槽且 Windows 下必然半途失败），改由桥接发 `request-harness-update` 转交 A/B 更新器；③`next` 加入发现通道并受信 `0.1.5-rc.2`，使「关于」页展示（读 `next`）、官方 HEAD、外壳可切换目标三处一致；另修复失败点击遗留的官方 pending 幽灵条目。**同日补部署失败退避闸门**：按目标版本记忆失败次数（`maxAutomaticDeployAttempts` 默认 2）与冷却（`deployRetryCooldownHours` 默认 24），自动升级超限后置 `blocked` 并写明恢复时间，用户手动检查（`interactive`）始终放行——影子验证只用官方包的一次性 profile，社区插件崩溃只在真实切换时暴露，无闸门会陷入「构建 → 切换 → 回滚 → 重启」循环。用户入口：插件「关于」页「更新」按钮、桌面设置页「检查更新」。**同日补活动槽清单自愈**：指纹槽只被验证「入口与 peer 存在」，指纹又只覆盖 lock 与家族清单，因此被旧实现半改写的坏槽（清单 `0.1.5-rc.2` + `node_modules` `0.1.2-rc.1`）不会被任何启动检查发现；新增 `reconcileOfficialRuntimeManifest`，在家族版本单一可读时把已存在的根清单与 `resolutionMode` 拉回实际安装版本（不动 lock 与物化依赖，指纹语义不变），家族混用时保持原样交由 A/B 门禁拒绝。对齐：后端与前端分支由 `dist/test/desktop-host.test.js`、`dist/test/harness-update.test.js`、`dist/test/plugin-seed.test.js` 覆盖（0 fail）；证据与未完成项见 [I025 迭代总览](../01-当前工作/I025-官方运行时更新修复/00-迭代总览.md) |
| 46 | 原子文件写入 | `src/atomic-file.ts` | ✅ | 原子文件写入避免损坏 |
| 47 | 会话日志路径修复 | `src/session-path-repair.ts` | ✅ | 会话日志路径修复 |
| 48 | DSH Market 批量操作 | `src/dshmarket-batch.ts` | ✅ | 插件市场批量操作节流 |

---

## 八、官方 DSH 运行时插件

| # | 功能名称 | 包名 | 版本 | 状态 | 描述 |
|---|----------|------|------|------|------|
| 49 | DSH 核心运行时 | `@deepseek-ai/dsh` | `0.1.2-rc.1` | ✅ | DSH 核心框架 |
| 50 | Cordis 插件分组 | `@deepseek-ai/cordis-plugin-group` | `1.0.2` | ✅ | 插件分组管理 |
| 51 | 会话作用域 | `@deepseek-ai/dsh-scope` | `0.1.2-rc.1` | ✅ | 会话作用域隔离 |
| 52 | 超时控制 | `@deepseek-ai/dsh-timeout` | `0.1.2-rc.1` | ✅ | 请求超时控制 |
| 53 | 不变量检查 | `@deepseek-ai/dsh-invariants` | `0.1.2-rc.1` | ✅ | 运行时不变量检查 |

---

## 九、社区/官方功能插件

| # | 功能名称 | 包名 | 版本 (bundle) | 状态 | 描述 |
|---|----------|------|---------------|------|------|
| 54 | Codex 风格侧栏 UI | `@michengai/dsh-codex-ui` | `0.2.102` | ✅ | 侧栏重构、扩展页、技能包展示 |
| 55 | IM 即时通讯连接 | `@michengai/dsh-im-connect` | `0.1.34` | ✅ | 即时通讯集成 |
| 56 | 定时任务自动化 | `@michengai/dsh-automation` | `0.1.27` | ✅ | 定时任务管理 |
| 57 | 技能包管理 | `@michengai/dsh-skills-manager` | `0.1.38` | ✅ | 技能包安装/管理 |
| 58 | 归档管理 | `@michengai/dsh-archive-manager` | `0.1.29` | ✅ | 会话归档管理 |
| 59 | 专家/代理 | `@michengai/dsh-agency-agents` | `0.1.30` | ✅ | 专家代理系统 |
| 60 | 上下文管理 | `dsh-context` | `0.41.2` | ✅ | 上下文窗口管理 |
| 61 | 侧边卡片与右侧工作台 | `dsh-better-sidebar` | `0.18.0-alpha.0`（本地维护副本） | 已恢复启用 | 保留文件、终端、源码、任务等卡片；浏览器已替换为桌面原生面板，移除 iframe 实现和专属设置；见 [替换记录](../01-当前工作/I023-前后端UI全项目审核/05-浏览器卡片替换.md) |
| 62 | MCP 连接器 | `dsh-mcp-connector` | `0.2.32` | ✅ | MCP 协议连接 |
| 63 | 插件市场 | `dshmarket` | `1.41.0` | ✅ | 插件安装/更新市场 |

---

## 十、本地定制插件

Codex UI 更新后左侧工作栏恢复淡绿色的问题已修复：颜色约束移至便携版主题层，页面每次重载都恢复主题。官方 0.2.113 客户端保持原样，最终候选 14 项真实界面检查通过；详见[插件更新后主题保持](../01-当前工作/I023-前后端UI全项目审核/07-插件更新后主题保持.md)。

首页活动分布卡片与输入框已统一宽度、压缩留白，窗口与界面缩放复验通过。属于展示布局变更，沿用活动统计与消息输入服务，不增加后端接口；38 项真实界面检查及正式 Profile 同步证据见[首页卡片尺寸对齐](../01-当前工作/I023-前后端UI全项目审核/06-首页卡片尺寸对齐.md)。

| # | 功能名称 | 路径 | 状态 | 描述 |
|---|----------|------|------|------|
| 64 | 知识库搜索 | `local/dsh-sidebar-spaces` | ✅ | 知识库/数据库跨库搜索、表格与 ECharts 分析、数据库总览仪表（v1.0.66+：填充率/唯一值/数值范围 per 字段，只读不改存储）、Inventor 检测；P3 Tiny 二手行情监控命令入口已装实机（dsh-p3-tiny-watch，Loader ACTIVE，来源 403 见 I027 验收报告）；侧栏底栏重设计（v1.0.67+：移除常驻重启按钮降级为顶栏入口，改为「DSH 工作台」标识 + ⚙ 设置图标 28px 命中）；保留右侧卡片入口，按用户要求移除左侧底部“知识与数据”快捷入口；首次打开时自动预置两条使用说明 |
| 65 | 重启应用按钮 | `local/dsh-restart-button` | ✅ | 左侧栏底部「重启应用」按钮 |
| 66 | 工作模式配置 | `local/dsh-work-mode` | ✅ | 设置内保留默认模式及会话档位记录；按用户要求移除左侧“编程/通用”切换和“全部/编程/通用”筛选，并清理旧筛选状态；见 [框选入口移除](../01-当前工作/I023-前后端UI全项目审核/03-侧栏框选入口移除.md) |
| 67 | 订阅额度查询 | `local/plan-quota` | 未交付 | 历史在途原型；当前 Profile 未安装，登记路径无源码。后端、前端及 UI 均未验证，详见 I023 |
| 76 | 执行方工具门 | `local/dsh-agent-boundary` | ✅（见下注） | 宿主侧 `tools/pre-execute` 门禁：`ask_user_question` 必须一次问全（≤4 问）、每题 ≥2 个选项、并标注推荐项（推荐项放第一 + `(Recommended)`）；凭据/身份类提问（密码、验证码、登录、扫码）豁免。与用户全局 `Data/DSH/AGENTS.md`、仓库根 `AGENTS.md` 的行为边界同批落地（文本层 + 工具层双保险）。**真实端到端已实证**：激活后活体探针（故意无选项提问）被当场拒绝，`startup-error.log` 零新增，live `--dump-config` 见插件行与 config。证据、GCC/GIR 与门禁红项界定见 [I024 迭代总览](../01-当前工作/I024-执行方工具门/00-迭代总览.md)。注：最终门禁 **450 tests / 438 pass / 0 fail / 12 skip（EXIT=0，tsc 0 错误）**。收尾前曾出现的 2 个 fail 属 `dsh-black-hole` 待补基线（`scripts/ui-baseline.mjs` 比对清单为硬编码、不含本插件），已由原作者于 2026-09-13 01:32:43 补录（见 `docs/01-当前工作/I023-前后端UI全项目审核/14-重启入口UI基线补记.md`），非本插件引入，本人未重录基线掩盖 |

---

## 十一、浏览器面板

| # | 功能名称 | 文件 | 状态 | 描述 |
|---|----------|------|------|------|
| 68 | 浏览器资料库 | `browser-library.cjs` | ✅ | 支持多标签、历史、书签、凭据、扩展导入；卡片与顶部入口共用同一资料库，收藏共享及页面导航已在新候选实测 |

---

## 十二、构建与部署

| # | 功能名称 | 文件 | 状态 | 描述 |
|---|----------|------|------|------|
| 69 | 一键构建脚本 | `Build-DSH-Portable.ps1` | ✅ | 类型检查+测试+打包+候选暂存 |
| 70 | 启动脚本 | `Start-DSH-Portable.ps1` | ✅ | 候选验证+健康检查+启动 |
| 71 | 便携环境配置 | `Portable-Environment.ps1` | ✅ | 便携环境变量设置 |
| 72 | 健康检查 | `Check-DSH-Health.cmd` | ✅ | 应用健康状态检查 |

---

## 十三、测试覆盖

| # | 功能名称 | 文件数 | 状态 | 描述 |
|---|----------|--------|------|------|
| 73 | 单元测试套件 | 71 | ✅ | 覆盖插件系统、运行时、Profile、桌面功能、外壳、便携特性等；新增侧边卡片启用状态与服务延迟加载/重载回归，最新结果见恢复记录 |

---

## 十四、本地推理与模型接入

| # | 功能名称 | 文件 | 状态 | 描述 |
|---|----------|------|------|------|
| 74 | 本地 Ollama 模型接入 | `Data/DSH/settings.yaml` | ✅ | 本机 Ollama 0.33.3 + `qwen3:8b` 注册为 `llm-pi-ai` provider 路由 `ollama-local`（`http://127.0.0.1:11434/v1`，openai-completions）；`reasoningEfforts.off: none` 关闭思考链，否则回复正文为空。显存不足时模型 CPU 分流，速度从 ~53 tok/s 降到 ~9 tok/s |

---

## 变更记录

| 日期 | 功能编号 | 变更类型 | 说明 |
|------|----------|----------|------|
| 2026-09-07 | - | 新建 | 初始功能清单，共 73 项 |
| 2026-09-07 | 74 | 新增 | 接入本地 Ollama 推理：DSH 新增 `ollama-local` provider 路由；Ollama 侧持久化 `OLLAMA_KEEP_ALIVE=2h`、`OLLAMA_FLASH_ATTENTION=1`、`OLLAMA_KV_CACHE_TYPE=q8_0`、`OLLAMA_CONTEXT_LENGTH=8192` |
| 2026-09-07 | 64 | 增强 | `dsh-sidebar-spaces` 知识库默认种子新增「Qoder 知识中心（QMind）使用说明」条目，版本升至 0.6.1 |
| 2026-09-07 | 24、28 | 增强 | 侧栏全局固定 252 px，禁用拖拽宽度调整；双击 DSH Logo 与外壳左上角面板图标均可切换侧栏 |
| 2026-09-09 | 75 | 新增 | 「视图 → 功能板块」开发用面板；按视觉板块聚合 74 项条目（侧栏/外壳/设置/浏览器/卡片/启动更新/本地插件/Profile），每条带名称、文件路径（点击复制走主进程剪贴板）、一句话描述；仅 `!app.isPackaged` 可见；实现见 [I024 实施记录](../01-当前工作/I024-功能板块开发面板/00-迭代总览.md) |
| 2026-09-09 | 75 | 修改 | 「功能板块」从仅开发态可见放开为全实例可见（用户日常用打包版，仅开发态等于用不上）；拆除 `devOnly` 机制（`src/shell-actions.ts`、`src/main.ts` isActionEnabled/showFeaturePanelsWindow 守卫）；测试改为打包态可见防回滚断言 |
| 2026-09-09 | 43、45 | 增强 | 统一更新器三增强：缺便携契约的上游新版本改为「已阻止」状态（不再误报红色更新失败）；更新源构建期配置化（`PortableDesktopReleaseSource`，去 `MichengAI/dsh-codex-desktop` 硬编码）；新增槽位/下载缓存回收（提交点触发，指针槽永保留）；见 [源码更新系统方案](../03-技术架构/DSH便携版3-源码更新系统方案.md) |
| 2026-09-09 | 全局 | 同步 | 上游认祖 `v1.0.51`（merge -s ours）并按分组吸收内容增量：移植子代理未读修复（v1.0.50）；版本升至 1.0.51、electron 44.1.1；恢复模式硬化系列经审阅不移植（与便携自研桥接闭包架构冲突，决策记录见 CHANGELOG 1.0.51 节）；桌面宠物/Mica/插件矩阵暂缓 |
| 2026-09-11 | 45b | 修复 | 官方运行时更新链路三缺陷：「关于」页显示 `0.1.5-rc.2 可更新` 但点击失败。①候选家族钉版失效（pnpm 11 忽略 `package.json` 的 `pnpm.overrides`，通配 overrides 不命中）→ 官方运行时统一 `resolutionMode: time-based`；②「关于」页原地改写活动运行时槽 → 改发 `request-harness-update` 走 A/B 更新器；③「关于」页读 npm `next`、外壳只发现 `[alpha, latest]` → `next` 入发现通道并受信 `0.1.5-rc.2`；顺带清掉失败点击遗留的官方 pending 幽灵条目。实机活动槽被旧实现半改写为 `0.1.5-rc.2` 清单，已恢复并复算指纹一致。见 [I025](../01-当前工作/I025-官方运行时更新修复/00-迭代总览.md) |
| 2026-09-11 | 45b | 增强 | 部署失败退避闸门：影子验证只装官方包（一次性 profile、空 patch），社区插件从未参与验证，候选可通过影子验证却在真实 profile 切换时崩溃并回滚。新增按目标版本的失败记忆（`deploymentFailures`：次数 / 时间 / 原因，含版本号白名单与 20 条上限防原型污染与膨胀）与 `evaluateDeploymentRetryGate`；自动升级连续失败达 `maxAutomaticDeployAttempts`（默认 2）后置 `blocked` 并写明恢复时间，冷却 `deployRetryCooldownHours`（默认 24）；用户手动检查（`interactive`）始终放行。接入点：`runHarnessUpdateCycle` 闸门 + `deployHarnessCandidate` 失败记录 + 切换回滚记录 / 提交清除 |
| 2026-09-11 | 45b | 修复 | 活动槽描述性清单自愈：`isOfficialRuntimeLaunchable` 只看入口与 peer、指纹只覆盖 lock 与家族清单（根清单被排除）、指纹槽在 `seedOfficialRuntime` 直接早返回——三重盲区让「清单新版本 + node_modules 旧版本」的坏槽永远不会被修，错误版本号一路传到「关于」页与更新器。新增 `reconcileOfficialRuntimeManifest`（家族版本单一可读时拉回实际版本并幂等确保 `resolutionMode`；只在文件已存在时改写，不在指纹槽造文件；家族混用则跳过交由 A/B 门禁）。两条新用例覆盖两个分支，全量 420 tests / 419 pass / 0 fail |
| 2026-09-11 | 43、45、全局 | 增强 | 认祖上游 v1.0.53（`merge -s ours`，增量按组审阅：bundled 运行时/插件矩阵跳版不移植——便携版运行时走自研 A/B 通道到 0.1.5-rc.2；上游主题同步修复不移植——便携外壳钉住「桥不注入 theme」且原生主题取解析后明暗，缺陷不存在；宠物修复针对暂缓功能）。**自助更新闭环**：`resolvePortableReleaseSource` 三级解析（Data/config 机器覆盖 > CI 烘焙 `resources/release-source.json` > 内置默认），GitHub 仓库 `hly252397-tech/DSH-3-Portable` 建立，`v*` 标签触发 CI 构建并自动生成便携兼容契约发布 Release——此后用户在应用内点「检查更新」即可自动下载、验证并 A/B 部署，无需本地构建。附带修复：9 个测试文件对实机 `Data/` 产物的依赖改为 `existsSync` 守卫 + `t.skip`（干净检出 422 tests / 0 fail / 72 skip） |
| 2026-09-12 | 43、45 | 修复 | **自助更新闭环首次真实演练成功**：v1.0.54 CI 发布（五平台+契约全绿）后，在运行中的 1.0.53 上走完整用户路径「检查更新 → 发现 v1.0.54（契约校验通过）→ 下载并构建 → 部署并重启」，应用自动切到 CI 构建的 `1.0.54-0a8de5bc061b7b06` 槽，启动自检「已是最新」，startup-error.log 零新增——远程发布→自动下载→A/B 部署全程无需本地构建。同版修复两处用户报告：①对话宽度把手命名空间漂移（`data-width-handle`→`data-dcu-width-handle`）致 32px×100% 隐形把手复活覆盖工作区——theme.css 两代同退役；②侧栏顶排「收缩侧边栏」按钮冗余退役（收起归双击 Logo，展开保留顶栏按钮）；均以调试协议对实机 DOM 验证，防回归测试钉住选择器与文案契约（427 tests / 426 pass / 0 fail） |
| 2026-09-12 | 46、全局 | 修复 | **任务管理面板挤压对话列**：better-sidebar 的面板拖宽只钳到视口宽（`clampWidth` 上限 = `innerWidth`），拖宽一次或恢复旧宽度后叠加 `autoOpenSubagent` 自动重开，对话列被压到最小内容宽度（文字一列一字竖排）。theme.css 双层兜底：工作台面板 `max-width: 70vw`、对话列 `min-width: 420px`（`workbench-panel-clamp` 用例钉住）；实机 DOM 以持久化 2500px 面板宽度复现并验证。同版处置市场矩阵升级的连锁：codex-ui 升至 1.1.2（本地 0.2.x 消失，≥1.1 变量映射已就位；深度内容契约测试改跳过、退役选择器契约双布局解析）；dsh-automation 升至 0.1.40（四锚点审阅通过后工作台适配器重钉 9319c634… 并写入实机 client）。v1.0.56 发布后自动下载链路再次零点击走到 ready，部署确认留用户（428 tests / 0 fail） |
| 2026-09-12 | 45 | 修复 | 两轮窄窗反馈的最终收敛：1.0.57 把面板钳制从 70vw 改为三段式（900px 绝对上限）后，用户反馈文件可视化窗口「固定死了」——1.0.58 改为**硬底线式**：唯一不变量是对话列 420px，面板可占「视口 − 740px」内任意宽度（大屏 ~1820px），拖多宽由用户决定。同版修复 GitHub API 限流的误报：未认证 `releases/latest` 返回 403/429 时不再渲染红色「更新失败」，而是保持上一结论（检查前快照 phase/release 原样保留）并记 `RELEASE_RATE_LIMITED`、附「稍后自动重试」说明（新增两场景用例）。v1.0.58 发布后经 CDP 驱动设置页完成部署，指针切至 `1.0.58-9220b6e6`（429 tests / 0 fail） |
| 2026-09-12 | 46 | 修复 | **对话内容列裁切根因**：容器变窄时内容仍按宽度把手时代内联持久化的像素值渲染（`--dsh-conversation-column-width: 1035px`），标题/卡片被切一半。主题层响应式覆盖 `min(100%, 900px)`（用户气泡 `min(100%, 640px)`）——内容列任意宽度下填满容器、宽屏封顶可读行宽。实机验证：容器 623px 时内容 623px、零裁切（429 tests / 417 pass / 0 fail）。v1.0.59 发布后自动下载并在本次演练中首次实现「构建完成 → 自动部署」零确认切换 |
| 2026-09-12 | 46 | 增强 | 窄窗比例按用户参照（WorkBuddy）重调：对话列保底 420→**560px**，工作台面板上限 = 视口 − 1000px、窗口窄于 ~1040px 时面板整个隐藏对话独占——面板先让位、对话底线最后让位。v1.0.61 发布后经 CDP 驱动完成部署（指针 `1.0.61-721520d8`），启动验证通过 |
| 2026-09-13 | 76 | 新增 | **执行方工具门（禁止智能体指挥用户）**：用户要求"防止 DSH 指挥用户去做事情"，先落两层文本约束——新建用户全局 `Data/DSH/AGENTS.md`（官方 `dsh-agent-instructions` 最先加载的全局位，覆盖本机全部会话，写入后即被运行时以 `user-global` 域注入验证）、仓库根 `AGENTS.md` 增「智能体行为边界」一节；再落**工具层硬门** `local/dsh-agent-boundary`：在 `tools/pre-execute` 上校验 `ask_user_question`。门禁非冗余有实读依据：官方 schema 中 `options` 是可选字段（`dsh-tool-ask-user/lib/index.js` L40-L58 无 required/minItems）、`questions` 无数量上限，模型可合法抛出开放式甩问。实现遵守铁律 1（deny 经既有 tool/result 落盘，不扩展 SessionEventMap）、3（先 `await next()`，上游 deny/ask 单调透传）、5（仅 `ctx.on`，无手动清理）、7（策略全走 Schemastery 配置）；挂载沿用 house pattern（`link:` 依赖 + bundles 条目 + junction，不跑 pnpm）。验证：策略 8 组用例、waterfall 单调性、真实 profile 组合 dump-config，并做**对照实验**证明 `--dump-config` 不能作为"可加载"证据；tsc 0；450 tests 中本改动新增用例全绿（8 pass / 0 skip）。**激活与端到端**：`link:` 依赖 + bundles 条目使 profile 指纹变化，DSH 经 `recycleAfterDshMarketBatch` 自动热重载（无需手动重启）；随后 live `--dump-config` 见插件行与完整 config、`startup-error.log` 零新增，并在真实实例发起故意无选项的提问被**当场拒绝**——门在真实链路生效。见 [I024](../01-当前工作/I024-执行方工具门/00-迭代总览.md) |
| 2026-09-13 | 76、全局 | 修复 | **两处旧红灯收尾（用户指示"去把两处旧红灯收干净"）**，并纠正本人两次误判。① `ui-baseline` 2 项失败：根因是「重启入口恢复」迭代改了受保护 UI 文件后漏登记基线，原作者已于 01:32:43 用 `--record` 补记（note `14-重启入口UI基线补记.md`）；本轮复验单测 3/3 + 独立脚本 PASS，受保护文件与快照逐字一致 —— **本人未重录基线/未改哈希/未删测试**。② 官方基线脚本 `--online` 失败：我先误报"打包回退槽缺 `@deepseek-ai/dsh`"，实为当时 `workspace-write` 受限沙箱禁止子进程管道 stdio，`spawnSync('tar')` 起不来（`status=null`）被渲染成缺包——策略放开后离线检查立即 PASS，**打包制品无问题**；随后真红灯是 `api.github.com` 间歇性卡顿撞 15s 硬超时（连 3 次误报后连 4 次全绿，复刻同参 3/3 成功、60s 副本实测仅 654ms）。唯一代码改动：`scripts/verify-dsh-official-baseline.mjs` 两处联网超时 15s→`onlineTimeoutMs = 30_000` 并注释实测依据（dev-only 脚本，不随应用打包；`quality-gate` 测试只钉官方 API/raw 地址，不断言超时值）。加固后连跑 3 次 PASS/online=verified；最终门禁 `tsc` 0 + **450/438 pass/0 fail/12 skip EXIT=0**。见 [I024](../01-当前工作/I024-执行方工具门/00-迭代总览.md) §4 |
| 2026-09-13 | 全局 | 文档 | **官方兼容基线纠偏（官方无变化）**：原表把「便携版实现基线」写作 `0.1.2-rc.1` 且称"与 npm latest 稳定线一致"，与实际不符——`0.1.2-rc.1` 是**打包内置／回退**运行时（须与 `package.json.config.bundledDshVersion` 一致，门禁强制），而**活动运行时槽自 2026-09-11 17:09 起为 `0.1.5-rc.2`**（`Data/Runtime/Harness/current.json`，previous 为 `0.1.2-rc.1`），npm `latest` 也已到 `0.1.5-rc.1`。现拆为「实现基线（活动运行时槽）」与「打包内置运行时（回退）」两行，「编译／运行／API 依据」明确指向活动槽；并按脚本真实行为重述强制动作第 1 条（它校验的是**打包回退运行时**与清单一致、历史回退目录仅在无活动槽时强校验，**不检查活动槽**）。复核：`--online` **PASS / online=verified**，官方 HEAD 未变（仍 `c291e79…`），npm 标签仍 `latest=0.1.5-rc.1`／`next=0.1.5-rc.2`／`alpha=0.1.5-alpha.2`。历史按日审阅记录**不改写**，以新增的「2026-09-13 复核与文档纠偏」节为准；机器可读基线 JSON 本身正确、未动。同日另清理 `Data\Temp` **1.25 GB**（1632 项测试/诊断垃圾；旧 `1.0.51` 安装包归档至 `Data/Development/archived-installers/`） |
| 2026-09-13 | 73 | 缺陷 | **测试临时目录泄漏（本轮仅定位与清积压，未改代码）**：29 个使用 `mkdtemp` 的测试文件中**仅 2 个含清理**，每跑一轮全量测试即残留约 270 个临时目录，累积至 `Data\Temp` **1.58 GB / 40,782 文件**。已定位泄漏主体：`test/portable-desktop-update.test.ts`（11 处 mkdtemp → `dsh-desktop-update-*` 855、`dsh-local-*` 386、`dsh-physical-asar-*` 140）、`test/session-path-repair.test.ts`（2 处 → `dsh-session-repair-*` 284）；另有 `codex-history-*` 两目录计 234 MB（来源未在 `test/`、`scripts/` 中定位到）。**已修复（同日，机制化而非逐个打补丁）**：① 新增 `scripts/run-tests.mjs`——每次运行创建独立目录 `<TEMP>\dsh-test-runs\run-XXXX` 并把子进程 `TEMP/TMP/TMPDIR` 全部指向它，因此**所有**测试（含其 spawn 的子进程）产生的临时文件都落在本次运行目录内；全绿即整目录删除、失败则保留并打印路径、每次启动顺手清理超过 24 小时的旧运行目录（防强杀残留）；支持 `DSH_TEST_KEEP_TMP=1`/`--keep` 保留现场，也支持只跑单个测试文件。② 新增 `test/helpers/tmp.ts`（`makeTrackedTempDir` 与 `mkdtemp` 同签名，导入即登记、文件结束时由 `after` 钩子清理），两个主要泄漏文件改为从它导入、调用点不动。③ 新增防复发测试 `test/test-tmp-hygiene.test.ts`：断言运行器的收口/清理/过期清理契约、断言 `AGENTS.md` 与代码地图的门禁命令指向运行器（防止回退到泄漏命令），并以**子进程实证**助手确实会在测试文件结束时删除目录。④ 门禁命令已在 `AGENTS.md`、`docs/05-系统认知/02-代码地图.md`、`docs/03-技术架构/UI定制维护契约.md` 统一为 `scripts/run-tests.mjs`。选择"收口到运行器"而非逐个改 27 个测试文件的原因：逐个改既不彻底（新写的测试照旧会漏），又会与正在改这些文件的其他会话冲突（实测最近 6 小时有 11 个测试文件被改动）；收口方案对**任何**测试文件都生效。⑤ `package.json` 的 `test` 脚本（`pnpm test`，也是 CI 入口）同步改为 `node scripts/run-tests.mjs`。⑥ 实证：修复前每轮全量测试给 `Data\Temp` 净增约 270 个条目，修复后**净增 1**（仅新增空的 `dsh-test-runs` 容器），连续两轮全量（**457 tests / 445 pass / 0 fail / 12 skip，EXIT=0**）后残留运行目录为 **0**。⑦ 未迁移项：其余约 25 个仍直接用 `mkdtemp` 的测试文件已被运行器收口覆盖，不再泄漏；如需拔掉最后的绕过风险（有人裸跑 `node --test`），可随后续改动顺手改用 `test/helpers/tmp.ts`，不单独排期 |
| 2026-09-12 | 45、46 | 修复 | **挤压问题的最后一层**（用户实机持续反馈后定位）：壳的原生浏览器工作台面板（`browserPanelView`，宽度 = `browserWidthRatio` × 视口，页面 CSS 触不到）此前上限 0.75——页面内钳制全部生效时它仍可占 75%。收口：`maximumRatio` 0.75→**0.55** + `capBrowserWorkspacePanelWidth`（`视口 − 900px` 绝对保护）双保险，历史遗留超宽比例加载时自动收敛。v1.0.62 部署完成（指针 `1.0.62-881d78e8`）。教训登记：双层视图（页面内面板 + 原生卡片）的宽度约束必须在两层同时钳制，只修 CSS 可达层对原生层无效 |
| 2026-09-13 | 73 | 修复 | **临时目录删除的 Windows 竞态（EBUSY）收口**：全量测试偶发红在「起挂死子进程 → 立刻删其工作目录」的用例上——先命中 `plugin-seed` 的 pnpm 超时用例，同一轮修完后又命中 `desktop-host` 的市场 pnpm 超时用例，报 `EBUSY: resource busy or locked, rmdir …`；单文件连跑 3 次**不复现**，只在并发全量时出现。根因：被 `taskkill /t /f` 终止的进程异步释放目录句柄，紧跟其后的裸 `rm()` 瞬时失败（`extract-runtime.test.ts` 里早已存在的内联 `maxRetries: 5` 补丁是同一根因的第三处实证）。修法照 2026-09-13 泄漏条目的原则——机制化而非逐例打补丁：① `test/helpers/tmp.ts` 的清理统一带 `maxRetries: 10 + retryDelay: 50`，并新增可重试的 `removeTempDir()`（内层复用 Node 自带重试、外层再兜 5 轮，best-effort 不抛错，覆盖杀毒/索引扫描这类更长占用）；② 三个会起挂死子进程的测试文件（`plugin-seed`、`desktop-host`、`extract-runtime`）改用受跟踪的 `makeTrackedTempDir` + `removeTempDir`，`extract-runtime` 的内联补丁一并收敛；③ `scripts/run-tests.mjs` 回收运行目录的两处 `rmSync` 也带 `RM_RETRY`；④ `test/test-tmp-hygiene.test.ts` 新增契约用例钉住「助手必须带 `maxRetries`／必须导出 `removeTempDir`／运行器必须带 `RM_RETRY`」并给出行为断言。证据：修前 1 例 EBUSY 红，修后**连续 3 轮全量 459 tests / 446 pass / 0 EBUSY**；`tsc` 0 错误。未改任何产品代码（`src/` 未动，无需重新打包） |
| 2026-09-13 | 全局 | 重构 | **宏建云 ERP 技能归一 + 脚本路径自解析 + 侧边栏入口**（本轮不含 UI 变更，故无三项对齐登记）。① **技能归一**：4 个旧技能（`bom-entry-system`、`hongjian-code-dims`、`hongjian-material-buy`、`hongjian-seg-value-finalize`）合并为单一技能包 `<盘根>\.workbuddy\skills\hongjian-erp\`（`SKILL.md` 四域路由 + `references\` 四份操作页），正文**搬移非复制**；旧技能 5 处副本（用户级 4 + 项目级 1）已归档到 `宏建云系统\_legacy\skills-20260913\`（含恢复说明），项目级 `宏建云系统\.workbuddy\skills\` 随之清空移除。② **修失效引用**：旧技能共引用 8 份已于 2026-09-12 并入手册后删除的编号文档（07/13/14/17/24/26/27/28）+ 1 个从未存在的脚本 `tools/probe_finance2.cjs`，全部改指 `31-宏建云操作手册.md` 对应章节或删除。③ **脚本路径自解析**：新增 `宏建云系统\tools\_root.cjs`（从脚本自身位置推导 `REPO`/`PORTABLE`/`EXPLORE`/`CODE_DISCOVER`，并按 `HJ_CHROME` → 本机浏览器缓存 → 常规安装 → Edge 顺序发现浏览器）；**31 个写死盘符/用户名的 .cjs 已迁移**（含 2 个 .mjs 的 `import.meta.url` 推导、1 个 .py 注释），`tools/` 残留硬编码 **0 处**（仅 `_root.cjs` 文档串述历史问题）；`tools/bom_manual_add.cjs` 按「单文件可拷」契约保持内联探测。迁移前整目录备份于 `宏建云系统\_legacy\tools-before-pathfix-20260913\`。④ **用户级链接**：`~\.workbuddy\skills\hongjian-erp` = Junction 指向盘内实体（**非副本**，写穿透已实测），附盘根 `重建技能链接.cmd` + 包内 `scripts\fix-link.cjs` 应对换盘符断链。⑤ **界面入口**：`Data\DSH\settings.yaml` 新增 `custom-spaces` 段（宏建云 ERP → `https://login.hongjian.com/login/login.jsp`），走 `dsh-custom-spaces` 插件呈现在侧边栏底部（备份 `Data\DSH\settings.yaml.bak-before-hongjian-space-20260913`）。**证据**：35 个 .cjs/.mjs 全部 `--check` 语法通过；30 个受迁移脚本逐个求值常量并核实 `CHROME`/`OUT`/`BASE` 路径**全部可达**，2 个 .mjs 的 `PORTABLE` 与 `bom_manual_add` 的 Chrome 均解析正确；`tsc` 0 错误；全量 **467 tests / 453 pass / 12 skip**。**遗留 2 例红**（`ui-baseline.test.js` 446/447）纯属**其它会话**在 14:16–16:05 对 `assets/theme.css`、`assets/shell.html`、`dsh-sidebar-spaces`、`dsh-better-sidebar`、`dsh-restart-button`、`dsh-black-hole` 的 UI 漂移（本人未触碰，时间戳与改动清单双向排除），按契约不代他人补记基线；**无 EBUSY**，本轮 0 新增失败。⑥ `宏建云系统\AGENTS.md` 的 BOM 章节改写为 `hongjian-erp` 四域路由 + 「禁止再写死盘符」硬规则 |
| 2026-09-13 | 全局 | 记录 | **本轮验收边界与外部漂移的收束过程**：上述修复进行中，`customizations/black-hole/lib/client.js` 于 13:01–13:02 被**外部会话**改写（内嵌横幅重出，122,499 → 132,321 字节，属 I026 黑洞空间视觉在途改动），使连续 3 轮全量各出现 **2 例红**（`ui-baseline.test.js` 439/440，`UI drift: customizations/black-hole/lib/client.js`）；同期 `test/black-hole.test.ts` 的资产一致性用例通过，判定为合法资产更新而非损坏。按 [UI 定制维护契约](../03-技术架构/UI定制维护契约.md) 与 AGENTS.md「漂移必须调查、完成真实 UI 验证后才可 `--record`」，**不代他人补记基线**（本人未改哈希、未删测试、未重录基线）。13:05:04 外部会话自行 `--record` 生成新基线（`customizations/ui/history/2026-09-13T05-05-04-701Z.json`）后，`ui-baseline.mjs` PASS，全量复验 **459 tests / 448 pass / 0 fail / 11 skip，EXIT=0**。本轮修复不含 UI 变更，不涉及三项对齐登记 |
| 2026-09-13 | 全局 | 重构 | **三大运行时依赖全部进便携盘**（用户指令「必须进便携盘」）—— 此前 `puppeteer-core`、Chrome、Python 三处都指向 `C:\Users\96551\...`，换电脑/换盘符即全线失效，直接违背「整盘可搬移」的设计前提。① **puppeteer-core@25.10.0 + 依赖树（23 MB / 2240 文件）** → `宏建云系统\tools\node_modules\`：Node 从脚本所在目录向上查找，33 个活跃脚本的裸 `require("puppeteer-core")` **原生命中，`NODE_PATH` 彻底弃用**；溯源文件 `tools\package.json` + `package-lock.json` 随迁（供 `npm ci` 复原）。② **Chrome 152.0.7977.64（428 MB / 308 文件）** → `宏建云系统\browsers\`：`_root.cjs` 发现顺序改为 `HJ_CHROME` → **盘内 `browsers/`** → 用户缓存 → 常规安装 → Edge（顺序不可反，曾实测漂移到 msedge）；`bom_manual_add.cjs` 的内联探测同步加入盘内目录。③ **Python 3.13.14 + venv（414 MB / 14808 文件）** → `宏建云系统\python\{base,venv}\`：`pyvenv.cfg` 的 `home`/`executable` 改写为盘内路径（原文件留档 `pyvenv.cfg.orig-before-portable`），openpyxl 3.1.5 / pandas 3.0.5 / markdown 3.10.3 齐备。④ **新增自检器 `tools\_check_deps.cjs`**（`--smoke` 可真启浏览器）：一条命令核验三链是否**全部解析在盘内**（按相对 `PORTABLE_ROOT` 判定，不只看存在性）；`_root.cjs` 导出新增 `BROWSERS`。⑤ 文档同步：`31-宏建云操作手册.md`（6.3 运行方式 + B·生成 PDF 的 Chrome 路径）、`宏建云系统\AGENTS.md`（依赖表 + 「禁止指回用户目录」硬规则）、技能包 `SKILL.md` 与 references 01–04、`宏建云系统\.workbuddy\memory\MEMORY.md`；新增 `browsers\README.md`、`python\README.md`。**证据**：干净环境（`NODE_PATH` 未设、cwd 在 `C:\`）下自检三链全 `[盘内]`；`--smoke` 真启 Chrome 152 加载 about:blank 成功；盘内 Python 实跑 `analyze_screw_dims.py` 读实测数据输出完整分析（EXIT=0）；`tsc` 0 错误；`ui-baseline.mjs` PASS；全量 **467 tests / 456 pass / 0 fail / 11 skip，EXIT=0**。盘内净增约 **865 MB**。未触碰 `src\`、DSH 插件目录与黑洞 I026（无 UI 变更，不涉三项对齐登记，无需重新打包） |
| 2026-09-13 | 全局 | UI | **外壳顶栏改版（重启右移 + 中段搜索胶囊 + 分组竖线）**。用户截图问「这里的UI你可以修吗？」，两轮 AskUserQuestion 后拍板「整条顶栏选A、左段按照你的来、中段选A、右段选3」。**根因**：改前顶栏 x=315 之后连续空白 2099px（占 82%）不是设计，而是成品被关掉——`assets/shell.html` 里 `.command-center{display:none}`、`.bar-right{display:none}`、`.divider{display:none}` 三条，而后端 `find`/`desktop-settings`/`check-updates` 与更新按钮进度环**早已实现**。**改动**：① `.bar-right` 恢复为 flex 并 `margin-left:auto` 贴右；② `#command-center` 改 `position:absolute;left:50%;top:50%;transform:translate(-50%,-50%)` + 定宽 320px 胶囊（中心恒为窗口中心，`≤1000px` 自动收起防窄窗相撞）；③ `#restart-btn` 由 `.bar-left` 首位移入 `.bar-right`（`#status` 之后、竖线之前），图标改指**自绘** `assets/shell-icons/power.svg`；④ `.bar-left` 在菜单前插入分组竖线，`.menu` 14px→13px、padding 0 10px→0 9px。**刻意用 `<img>`+新图标文件而非内联 SVG**：`.nav img`/`.nav.restart.confirming img`/`.nav.restart.spinning img` 三条既有规则只匹配 `img`，用文件可一行不改继续生效；`extraResources` 对 `shell-icons` 用 `**/*` 通配，**无需改 package.json**。**测试同步**：`test/main-safety.test.ts` 原断言「重启必须是 `bar-left` 第一个、且不得在 `bar-right`」编码的是上一轮"重启放最左"的布局决定，按维护契约「最新用户决定覆盖旧图」改写为「切出两段分别断言 + 唯一入口计数 + 胶囊居中定宽 320 钉死」，保护意图（唯一入口/紧凑图标/两步确认）不变；契约表新增「外壳顶栏（整条）」行并显式标注覆盖 33 的旧决定。**证据**：真实 `assets/shell.html` 离屏渲染（便携盘 Chrome 152 + puppeteer-core 25.10.0，2560/1280 双宽度 × 深浅双主题），**15/15 断言 PASS**，改后实测 `back 17~47 / forward 49~79 / divider 87 / menus 100~276 / command-center 1120~1440（宽 320，中心 1280=窗口中心）/ restart 2273~2303 / divider 2311 / find 2320 / settings 2352 / update 2384~2414`，**最长连续空白 2099px → 844px**；1280 窄窗无重叠（左余量 844px、右余量 826px）。见 [本轮记录](../01-当前工作/I023-前后端UI全项目审核/39-外壳顶栏改版.md)、证据 `artifacts/shell-topbar-20260913/results.json`、对比页 `artifacts/shell-topbar-20260913/顶栏改版-验收对比.html`。**门禁**：`tsc --noEmit` 与 `tsc` 均 **0 错误**；全量 **468 tests / 454 pass / 3 fail / 11 skip**，3 处红**全部为外部 `theme.css` 回退的连带、0 处由本改动引入**（`#447`/`#448` ui-baseline 漂移、`#468 便携侧栏使用官方紧凑图标轨` 读 `theme.css` 缺 `body .dcu-root{width:56px}`）；本改动触及的 `#215 顶栏移除重复浏览器入口且重启确认保持紧凑图标态`（已按新布局改写）**通过**，同族 `#214/216/218~221/223` 全绿，`main-safety` 单文件 **18/18 pass**。**未覆盖**：原生弹出菜单真实展开、重启两步确认实机点击、候选槽启动验证、缩放实测。**门禁收口与构建结果（17:4x~18:2x 追记）**：原先那 3 处外部漂移已由原会话自行 `--record` 收口，改后再检只剩 `assets/shell.html` 一处（本人改动），遂按契约补记 `ui-baseline.mjs --record --note docs/…/39-外壳顶栏改版.md --evidence artifacts/shell-topbar-20260913/results.json` → 新基线 `customizations/ui/history/2026-09-13T09-48-24-560Z.json`，**逐文件比对仅 `assets/shell.html` 项哈希变化**（未替他人改动背书），复核 **PASS**；`pnpm test` 仍余 1 条红（`#468 便携侧栏使用官方紧凑图标轨`，因 `theme.css` 已移走 `body .dcu-root{width:56px}`，**属他人在途、未动**）。**构建两次均失败、未产出候选槽**：`Build-DSH-Portable.ps1 -SkipTests` 走到 `prepare-runtime` 即永久静默（19 分钟零 CPU 增长、无子进程、无网络连接、无磁盘写入），根因锁定为 `scripts/prepare-runtime.ts:75-78` 删除 `runtime-plugins` 时**整目录递归删除永久阻塞**（`fs.rmSync` 120 秒不返回、不抛异常；目录内无符号链接/Junction、8779 节点遍历正常、采样期无进程写入）⇒ **卷级 I/O 卡死**，非代码缺陷、非权限问题。**因此运行中窗口仍是旧顶栏**：当前槽 `1.0.66-local-f08cc431b294ee27` 内 `shell.html` = `dd1e03eb`（改前值），本改版 `32502ef4` **尚未进入任何槽**；`runtime-plugins`/`runtime-dsh` 残留目录未删（下次构建仍会先尝试删它，卷 I/O 未恢复则再次卡同处）。善后校验：`ui-baseline.mjs` PASS、`Data/DSH/profiles/web/local/` 七个本地插件完好。详见 [39 号文档 §9](../01-当前工作/I023-前后端UI全项目审核/39-外壳顶栏改版.md)。`src/`、DSH 插件目录、黑洞 I026 均未触碰 |
| 2026-09-13 | 68 | 优化 | **浏览器导航条精简（右侧面板优化 2）**：导航条常隐「资料/下载/外部」三个文字按钮（`.browser-tool.optional` 由"仅窄屏 620px 隐藏"改为常隐），地址栏加宽约 100px；功能不丢——「更多」溢出菜单新增「在系统浏览器打开」等价项并接线 `browser.openExternal()`（资料/下载本有等价项）。仅动纯素材 `assets/browser-panel.html` + `assets/browser-workspace.js`，不改行高（导航条仍 42px）、不碰 `BROWSER_TABS_BAR_HEIGHT` 几何常量，零测试断言改动。走界面快通道（在途 src 改动拦截门禁，按其 `-Force` 语义只推素材）：候选槽 `1.0.66-local-536047aaadd3f01a`，源码=release=候选三方 `cmp` 校验一致；用户重启后指针已切换到该槽、`startup-error.log` 无新增（现存错误为 09-11 旧账）、gateway 20:10 boot `apply 完成`。方案与效果页：`docs/01-当前工作/I023-前后端UI全项目审核/40-浏览器面板优化方案.md`、`artifacts/browser-panel-20260913/浏览器面板优化-验收对比.html`。用户截图目视确认通过（导航条右端仅「已收藏/更多」、地址栏显著加宽；「更多」菜单含 资料/下载内容/在系统浏览器打开，证据 `artifacts/browser-panel-20260913/after-real.png`）；优化 1（需 src 几何同步）与优化 3（better-sidebar 侧卡默认宽度）**未实施** |
| 2026-09-13 | 68 | 修复 | **顶栏工具组全部失效（上一条界面快通道的连带故障）**。用户截图（顶栏条 2411×52）报「这些功能现在都没办法使用」：菜单区空白，返回/前进/搜索胶囊/重启/搜索/桌面设置/检查更新全部点不动。**根因**：上一条（浏览器导航条精简）在 `assets/browser-workspace.js` 的 `create()` 里**无条件**新增 `elements.menuExternal.addEventListener(...)`，而该 id 只加进了 `assets/browser-panel.html`，**`assets/shell.html` 里没有**；`shell.html` 第 227 行在顶层调用 `create()`，抛 `Cannot read properties of null (reading 'addEventListener')` ⇒ 其后所有 `addEventListener`（菜单填充 + 全部控件）一行未执行；HTML/CSS 与 `<head>` 主题脚本照常执行，所以顶栏「长出来了但按不动」（用户截图是浅色 = 主题脚本已跑过）。19:45 该素材进槽 `536047`、20:10 切换后暴露。**改动**：① `shell.html` 的 `#browser-overflow` 补回「在系统浏览器打开」入口（与浏览器面板文档同字）；② 该绑定改 `elements.menuExternal?.` 空安全；③ `test/main-safety.test.ts` 新增用例：解析 `browser-workspace.js` 全部 `byId('…')`（62 个）并断言在**它加载的两个文档**里都存在，钉住「少一个 id 拖垮整条外壳」这类问题。**证据**：便携盘 Chrome 152 离屏渲染**真实 assets** + 真实点击，**14/14 断言 PASS**，含对照组——同一 harness 加载活动槽 `536047` 的旧组合**复现** `pageErrors=[Cannot read properties of null (reading 'addEventListener')]`、`menusCount=0`、点返回零 action（与用户截图逐项一致），修复后 `menusCount=4` 且 back/forward/find/desktop-settings/check-updates 各发对应 action、重启两步确认（首击 0 action、二击 `app-restart`）。见 [41 号记录](../01-当前工作/I023-前后端UI全项目审核/41-顶栏工具组失效修复.md)、证据 `artifacts/shell-topbar-fix-20260913/{results.json,fixed-topbar-2560.png,broken-topbar-2560.png}`。**门禁**：`tsc` 0 错误；`test/main-safety.test.ts` **19/19 pass**；全量 **470 tests / 457 pass / 1 fail / 12 skip**，唯一红 `#450` 是**另一会话**重建 `dsh-sidebar-spaces/lib/{client.js,client.src.js}` 造成的 UI 基线漂移（由其自行登记，按契约不代记）；本轮登记的基线 `customizations/ui/history/2026-09-13T12-52-19-972Z.json` 与上一版逐文件比对**只有 `assets/shell.html` 一项变化**——20:50:08 首次登记时恰好把对方在途的两个文件也快照了进去，已用 `artifacts/shell-topbar-fix-20260913/trim-external-drift.mjs` 恢复为上一版哈希（把不该绿的恢复为红）后重记。**部署**：只落 `assets/**` ⇒ 界面快通道 `Build-UI-Only.ps1 -Force`（-Force 依据：`release\win-unpacked\resources\app.asar` 与活动槽 asar 同哈希 `6596A68ED9861BFD`、槽内 `dist/src/main.js` 与仓库 `dist/` 同哈希 `0592201E08DEE007` ⇒ 不存在混血槽），候选槽 `1.0.66-local-6686f376011cb406`（20:47），**切换需用户下线重启**。**未覆盖**：真机外壳窗口点击、原生菜单展开、候选槽启动验证 |

| 2026-09-14 | 全局、45 | 修复+增强 | **修复陈旧随包插件清单并对齐上游 v1.0.60（插件矩阵 + 运行环境）**。触发：用户问"v1.0.60 那两条要点更新了吗"，实测两条都没做，且发现清单长期陈旧（Codex UI 记 `0.2.102` 而实机 `1.1.x`），会让离线包与全新安装把插件装回老版本。① **清单修复**：`BUNDLED_PLUGINS` 由 10 项陈旧版本改为 16 项对齐，补齐 codex-pet/btw/simplify/code-review/pua/usage-billing；并补上让清单**真正生效**的机制——`planBundledPluginSeed` 过去只补缺失、不升低于基线的已装件，现按基线升级并加**两道护栏**（只升不降；`link:`/`file:`/`portal:` 本地定制包永不参与比较，否则会把定制件换成 npm 包）。② **运行环境**：Electron 44.1.1→44.3.0、Node 24.20.0→24.21.0、pnpm 11.24.0→11.26.0、@types/node 26.5.1、electron-builder 26.16.1（五平台 `bundledNodeSha256` 取上游官方值，不自造哈希）。③ **automation 补丁重钉**（**经用户明确授权**改定制文件）：实机已是 0.1.42 而补丁锚定 0.1.40，导致补丁拒绝生效 + UI 基线漂移（两条红灯同源；实测 0.1.40=9319c634…、0.1.42=b4839315…）；逐项核对 0.1.42 三锚点各出现 1 次、`import_react11` 别名未漂移、退役标记零残留后重钉。④ **真机验证**：候选槽 `1.0.66-local-54d1c533b96a3297`（事务 `aee4c198…`）部署 `completed`；槽内实测 Node v24.21.0 / pnpm 11.26.0 / Electron 44.3.0；**16/16 插件与目标一致**；8 个 `link:` 定制声明与版本原样未动；`startup-error.log` / `plugin-update.log` 零新增；设置页「配置管理插件」12 项全部「已安装」、无任何「可更新」。⑤ **发现并处置一处隐患**：重启后启动补种因 `ERR_PNPM_UNEXPECTED_STORE` 失败（环境 `PNPM_HOME` 令 pnpm 默认仓库指向 `Data\Development\pnpm-home\store\v11`，与 profile 链接的 `G:\.pnpm-store\v11` 不同；命令未显式带 `--store-dir` 即必然报错），已用同一路径带显式仓库装上三项；"那一次为何没带上"未复现，留作下一轮（建议：解析不到仓库时不要发出不带仓库的 pnpm 命令）。**门禁/证据**：`tsc` 0；全量 **486 tests / 474 pass / 0 fail / 12 skip**；`ui-baseline` PASS（漂移经调查 + 实际 UI 验证后 `--record`，证据 `artifacts/automation-0.1.42-repin-20260914/results.json` 16/16 含反向对照）。开工前完整备份 profile 与 customizations（`Data/Development/backup-before-v1060-<时间戳>`）。见 [I030](../01-当前工作/I030-随包清单修复与上游v1.0.60对齐/00-迭代总览.md) |

---

## 待新增功能

<!-- 新功能请添加到此区域 -->
