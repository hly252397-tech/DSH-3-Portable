# 待办与阻塞

更新时间：2026-09-11（Asia/Shanghai）

| 事项 | 负责人 | 优先级 | 下一步 | 解除条件 |
|---|---|---|---|---|
| I023 三项对齐剩余验收 | 当前审核任务/使用者 | 高 | 正常重启候选并按 [I023 详细记录](../01-当前工作/I023-前后端UI全项目审核/01-审核与修复记录.md) 复验未覆盖范围 | 候选健康提交、相应真实业务通过；外部账号/服务及未交付额度功能不计入已有通过项 |
| 0.1.2 → 0.1.5 运行时真实切换与回滚演练 | 开发维护者 | 高 | **前置①生态适配已到位**（MichengAI 六件套 peer 声明 `0.1.5-rc.1 \|\| 0.1.5-rc.2`）；**前置②受信已就绪**（2026-09-11 `0.1.5-rc.2` 入受信清单，`next` 入发现通道，两个目标版本候选各 231/231 家族对齐）；**前置③失败循环已加闸**（同版本连续部署失败 2 次后自动升级退避 24 小时，手动重试始终可用）。剩余：重启后观察真实 A/B 切换（候选槽 → 影子验证 → 空闲切换 → 观察窗口）→ 复核升级后真实 Profile 插件矩阵（历史阻断 `cannot get property webServer without inject`）→ 跑一次回滚演练 | 切换后主界面与 DSH readiness 正常、`state.json` 进入 `succeeded`/`completed`、startup-error.log 零新增；回滚演练能在观察窗口内退回旧槽 |
| 官方桌面双技术评估（源自 09-11 审阅） | 开发维护者 | 已完成 | ①目录链接替代双拷贝：**已采纳实施**（2026-09-11 完成 4 插件 junction 迁移，静态验证通过，见 [评估文档](../03-技术架构/评估-目录链接替代插件双拷贝.md)；真实 DSH 加载随下次应用重启验证，异常按文档回滚）；②启动页/错误页拆分：**暂缓**（现有单页阶段渲染已真机验证，收益不显著） | 重启后侧栏知识库/重启按钮/工作模式功能正常即为通过 |
| 工作台钉版锚点复核 | 开发维护者 | 已完成（2026-09-11） | ~~核对 `Customize/Automation-Workbench/build.mjs` 的 `upstreamHash`/图标常量与 0.1.38 重钉声明~~ → 已核对：`upstreamHash = 21fefb6c…` 与实装 `@michengai/dsh-automation@0.1.38` 的 `lib/client.js` 实测 sha256 完全一致；两处锚点各命中一次；`IconAlarmClockOutline16` 已消失、`IconListPenOutline16` 存在，与重钉声明相符，无需改动 | 锚点与声明版本一致，构建产物可复现 ✅ |
| 自有发布渠道（origin 远端 + 独立版本线 + tag 发布） | 发布维护者 | 低 | 用户提供目标 GitHub 仓库后：`git remote add origin`、推送、tag 触发 CI（契约生成已就绪）、更新源常量改为自有仓库 | 自有 Release 可被便携版检测、下载、入槽并通过 A/B 验证 |
| 配置正式发布签名凭据 | 发布维护者 | 低 | 后续配置 Windows 证书以及 Apple 签名、公证 Secrets | 签名产物通过 Windows 与 macOS 系统校验 |

2026-09-09 晚已完成并真机验证：上游认祖 v1.0.51 + 分组吸收（子代理未读修复、版本/electron 对齐）、统一更新器三增强（阻止态/更新源配置化/槽位回收，事件链证据见 [源码更新系统方案](../03-技术架构/DSH便携版3-源码更新系统方案.md)）、基线门禁离线转绿（打包 tgz 一致性 + GNU tar 探测）、构建器 electron 镜像回退。桌面 1.0.51 已在真实便携盘完成槽切换与健康提交；另有同源码二次候选 `1.0.51-local-360bd95ad6eb709f` 处于 pending，下次自然重启自动部署（与运行版本功能等价，仅构建签名时间戳不同）。

2026-09-10 完成官方 `0.1.5-alpha.2@b2e3b2a` 审阅并更新双基线（审阅记录见 [官方兼容基线](../03-技术架构/DeepSeek-Harness-官方兼容基线.md)）；受信清单按 0.1.5-alpha.1 除名裁决维持不变。

2026-09-11 完成 `b2e3b2a..c291e79`（160 提交，含官方桌面 runtime-tree 架构收敛）审阅，基线锚定 `0.1.5-rc.2@c291e79`，`verify-dsh-official-baseline --online` 首次全绿（`online=verified`）；官方桌面「目录链接共享包」「启动页拆分」两项技术记入评估待办。

2026-09-11 晚修复官方运行时更新链路三缺陷（候选家族钉版失效 /「关于」页原地改写活动槽 /「关于」页与外壳版本口径不一致），并清理失败点击遗留的幽灵 pending 条目；被旧实现半改写的活动槽已恢复，指纹复算与 `current.json` 一致。详见 [I025](../01-当前工作/I025-官方运行时更新修复/00-迭代总览.md)。

2026-09-11 晚补「部署失败退避闸门」：影子验证只用官方包的一次性 profile，社区插件从未被验证，候选可能通过影子验证却在真实 profile 切换时崩溃并自动回滚；按目标版本记忆失败次数（默认 2 次）后暂停自动升级 24 小时，用户手动重试始终放行，避免「构建 → 切换 → 回滚 → 重启」反复循环。真机切换若失败，`state.json` 应出现 `blocked` 与 `retry-gate` 审计事件。

同日再补「活动槽描述性清单自愈」：`0.1.5-rc.2` 提示被点击后留下的「清单新版本 + node_modules 旧版本」坏槽，此前**没有任何启动检查能发现**（`isOfficialRuntimeLaunchable` 只验入口与 peer，指纹只覆盖 lock 与家族清单，指纹槽在补种时直接早返回）。现由 `reconcileOfficialRuntimeManifest` 在家族版本单一可读时把已存在的根清单与 `resolutionMode` 拉回实际安装版本；家族混用则不动清单，交给 A/B 门禁拒绝。实机活动槽当前健康（清单 = 安装 = `0.1.2-rc.1`，指纹 `2c82a3ef…` 与 `current.json` 一致），自愈属预防性修复——**下一个候选槽通过启动验证后才会带上它**。

2026-09-11 深夜自助更新闭环落地：认祖上游 v1.0.53 后，公开仓库 `hly252397-tech/DSH-3-Portable` 建立，`v*` 标签触发 CI 构建五平台制品并自动生成便携兼容契约发布 Release；`resolvePortableReleaseSource` 按「Data/config 机器覆盖 → CI 烘焙 resources → 内置默认」解析发布源。**已验证（run 34623286216）**：五平台构建全绿，Release `v1.0.53` 已标记 latest，未认证 API/下载可达（更新器视角），`dsh-portable-contract-1.0.53-win-x64.json` 与 Windows ZIP 的 sha256（`17304a57…`）绑定一致、五项能力齐全。**余下观察**：①用户重启后 pending 槽 `1.0.53-local-5832cc93cb304a80` 部署为 current；②下个上游版本认祖发布 v1.0.54+ 后，设置页「检查更新」应对自有仓库走「下载 → 验证 → 构建 → A/B 部署」全程无需本地构建。首次运行还需注意：上游同版本标签名认祖后归便携仓库（本地 `git tag -f` 声明过），`git fetch upstream --tags` 会对 v1.0.53 报冲突，属预期。CI 全新检出暴露的实机 `Data/` 测试依赖已全部改为守卫跳过（422 tests / 0 fail / 72+ skip），win32 盘符路径用例限 win32 验证。

2026-09-12 凌晨完成自助更新闭环首次真实演练（见功能清单 2026-09-12 行）：v1.0.54 经 CI 发布后，1.0.53 通过应用内「检查更新 → 下载并构建 → 部署并重启」自动切到 CI 构建槽，全程无本地构建。v1.0.55 再进一步：策略切为「自动下载，安装前提醒」后，重启即**零点击**完成检查→下载→构建，仅部署确认需要一次点击（设计使然）。**遗留观察收窄**：窗口白屏只出现在带 `--remote-debugging-port`/`--disable-gpu` 或强杀后立即重启的实例上；正常启动器循环（v1.0.55 部署后实测）渲染完全正常。若实机再现白屏，优先排查窗口呈现层（WebContentsView 挂载时序/合成），内容侧无需怀疑。

本次按用户要求发布未签名测试版；签名凭据全部留空时 CI 放行，部分配置时仍会失败，避免产生错误的签名预期。
