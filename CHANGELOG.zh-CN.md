# 更新日志

[English](CHANGELOG.md)

以下记录最近发布的五个版本。

## 1.0.65 便携集成版 — 2026-09-12

- 修复 rc.2 新前端（dsh-web-frontend，模块 17p4l 系列）工作台标签菜单越界盖住侧栏的问题：菜单为 fixed 定位、JS 按锚点右对齐计算 left，按钮在标签条左端时菜单左缘伸进侧栏区。主题层用模块前缀匹配把菜单左对齐到侧栏之外（260px）。改动前核对了新前端 CSS 源（index-DPX2bQLO.css 的 _menu_17p4l_444 规则）。

## 1.0.64 便携集成版 — 2026-09-12

- 修复原生浏览器面板「页面报告 bounds」路径绕过钳制的问题：`normalizeBrowserPanelBounds` 此前只裁剪到视口范围，没走 `viewport − 900px` 对话保护——页面侧的占位矩形报告太宽时原生视图也跟着宽（页面内钳制修了占位，但原生视图用的是裁剪前值）。现在两条路径（比例推算 + 页面报告）都经 `viewport − 900px` 约束、对称执行。同时把壳的 ratio 上限从 0.55 收到 0.48 并命名为 `MAXIMUM_BROWSER_WIDTH_RATIO`。

## 1.0.63 便携集成版 — 2026-09-12

- **对话布局对齐官方 Harness 模型**（按用户指示停止打补丁、回归正源）：官方源码定义 `--dsh-chat-content-width = var(--dsh-chat-user-width, clamp(680px, 列宽×64%, 920px))`——官方默认本就是响应式。便携侧两处破坏了它：历史拖拽偏好（`--dsh-chat-user-width: 640px` 固定像素）盖掉了响应式钳制；对话列底线（420→560px）低于官方 680px 内容下限。主题层现在清除历史偏好（`unset`）交还官方模型、对话列底线提到官方的 680px、面板上限移至 `calc(100vw − 1040px)`（视口窄于 1100px 隐藏）。改动前先核对了官方源码（`dsh-client-ui-conversation`）。

## 1.0.62 便携集成版 — 2026-09-12

- 把对话保护钳制补到**壳的原生浏览器工作台面板**上（页面 CSS 触不到的一层）：宽度比例上限从 0.75 降到 0.55，比例换算出的宽度再叠加 `视口 − 900px` 的绝对保护（`capBrowserWorkspacePanelWidth`）——无论比例被拖到多大，原生面板都吃不掉对话区；历史遗留的超宽比例在加载时自动收敛。至此补齐了 1.0.56–1.0.61 只钳页面内面板、没钳原生卡片的缺口。

## 1.0.61 便携集成版 — 2026-09-12

- 按用户提供的参照（WorkBuddy 窄窗布局）重新调配比例：对话列保底从 420px 提到舒适的 560px，工作台面板拿余量（上限 = 视口 − 1000px），窗口窄于 ~1040px 时面板整个隐藏、对话独占——面板先让位，对话列底线最后让位。

## 1.0.60 便携集成版 — 2026-09-12

- 按用户窄窗截图再收敛一档：上一版的 `100vw − 740px` 上限在 2000px 窗口仍给面板 1260px，对话列只剩 420px 底线、首页卡片被切一半。面板上限改为 `clamp(340px, calc(100vw − 900px), 1100px)`——绝对上限 1100px（拖拽的 1075px 在宽屏仍放行）、窄窗优先让位（1450px 窗口实测面板 460、对话 508、零溢出）。首页卡片行同时允许换行（`flex-wrap: wrap`），窄窗降级为 2×2 而不是被裁切。

## 1.0.59 便携集成版 — 2026-09-12

- 修掉窄宽度下对话内容被裁切（标题、卡片切一半）的根因：对话根元素上还挂着宽度把手时代内联持久化的像素值（`--dsh-conversation-column-width: 1035px`、`--dsh-chat-user-width: 640px`），容器一变小内容就溢出裁切。主题层现在响应式覆盖（`min(100%, 900px)` / `min(100%, 640px)`）：内容列在任意宽度下填满容器，宽屏上限 900px 保证可读行宽。实机验证：容器 623px 时内容 623px、零裁切。

## 1.0.58 便携集成版 — 2026-09-12

- GitHub API 限流（未认证 `releases/latest` 返回 HTTP 403/429）不再渲染成红色「更新失败」：检查会保持上一结论（已是最新 / 发现新版本，以最近一次确认为准），只刷新检查时间并附「限流，稍后自动重试」说明，错误码记为 `RELEASE_RATE_LIMITED`。高频检查（重启 × 检查周期 × 手动）很容易打满匿名 60 次/小时的配额——这是暂时状态，不是发布物坏了。
- 放宽工作台面板钳制：1.0.57 的 900px 绝对上限把文件可视化窗口锁死了（用户反馈「固定死了」）。现改为单一硬底线——面板可占「视口 − 740px」（图标轨 + 侧栏 + 对话 420px 底线 + 间距）以内的任意宽度，大屏最多约 1820px；对话列 420px 底线是唯一不变量，拖多宽由你决定。

## 1.0.57 便携集成版 — 2026-09-12

- 收紧上一版的工作台面板钳制：纯 `70vw` 上限在窄窗口下仍放行 70% 宽度（对话列压到 420px 底线，首页卡片被切一半）。现改为 `clamp(320px, calc(100vw - 740px), 900px)`——面板绝对高度不超过 900px、窗口变窄时优先让位（1250px 窗口实测面板缩到 494px）、最低 320px 兜底。在 2560/1984/1234 三档视口实机验证，水平溢出均为 0。

## 1.0.56 便携集成版 — 2026-09-12

- 修复子代理「任务管理」面板把对话挤成竖排细条的问题：better-sidebar 的面板拖宽只钳到视口宽（`clampWidth` 上限 = `innerWidth`），拖宽一次（或恢复旧状态）再叠加 `autoOpenSubagent` 自动重开，对话列就被压到最小内容宽度。`theme.css` 现在把工作台面板钳到 70vw、给对话列保底 420px；已通过持久化 2500px 面板宽度在实机 DOM 上验证。
- 插件市场把 `@michengai/dsh-codex-ui` 升到 1.1.2（本地 0.2.x 布局消失，包迁入 `node_modules`）。≥1.1 侧栏变量映射此前已就位；钉本地 0.2.x 的内容契约测试在无该布局时跳过，退役选择器契约改为双布局解析。
- 自动化工作台适配器重钉到市场升级后的 `@michengai/dsh-automation` 0.1.40（新增 `maxConcurrentRuns`、调度粒度 5→1 分钟、日程表单重构）：四个锚点（apply/runtime/设置返回行/`IconListPenOutline16`）逐一核对未变，适配器已重建并写入实机 client。

## 1.0.55 便携集成版 — 2026-09-12

- 移除桌面设置窗口页内的冗余关闭按钮：该窗口本就有原生标题栏，页内再画一个 X 属于重复（Esc 关闭保留；无框的「关于」窗口保留页内关闭按钮，因为那是它唯一的关闭方式）。
- 键盘快捷键窗口同为原生标题栏，做同样清理。
- 其余无改动；发这一版是为了让 1.0.54 上的自助更新闭环再端到端演练一次。

## 1.0.54 便携集成版 — 2026-09-12

- 修复侧栏拖拽把手覆盖对话工作区的问题：实装对话 UI 把宽度把手命名空间从 `data-width-handle` 改成了 `data-dcu-width-handle`，便携退役规则失配后，把手以 32px×100% 全列高的隐形条复活——既挡住工作区点击，拖动时又把输入区拉宽到盖住整个对话列。`theme.css` 现在同时退役新旧两代把手，并已通过调试协议对实机 DOM 验证生效。
- 按用户决定移除侧栏顶排冗余的「收缩侧边栏」按钮：收起入口归双击 DSH Logo（desktop 桥接），对话顶栏保留「展开侧边栏」按钮作为恢复入口。中英两语言的 aria 文案均已覆盖。
- 新增防回归测试：把两代退役选择器与实装 codex-ui 的文案契约钉进门禁——插件再改名时门禁先红，不再静默复活。

## 1.0.53 便携集成版 — 2026-09-11

- 以 `merge -s ours` 认祖上游 `v1.0.53`，并按组审阅了 `v1.0.51..v1.0.53` 的全部增量。
- 有意不移植——随包运行时/插件矩阵跳版（上游 `00515df`）：便携版继续随包 `@deepseek-ai/dsh` `0.1.2-rc.1`，`0.1.5-rc.2` 通过自研 A/B 运行时更新器送达（家族钉版解析、影子验证、空闲切换、失败自动回滚与按版本部署退避）。上游为 0.1.5+ 入口加的 `dsh-bootstrap` `runCli` 守卫此前已吸收。注意：上游提示 `0.1.5-rc.2` 起会话改用 Session V3 格式——新版写入的会话旧版读不了；运行时更新器的回滚只保护程序槽，接受运行时更新前请先备份重要会话。
- 有意不移植——原生主题同步修复（上游 `8748b33`）：它向桌面桥注入 `theme` 服务以感知主题「偏好」变化、驱动原生 Mica 材质。便携外壳有回归测试钉住「桌面桥不注入 theme」，原生主题取自解析后的明暗结果，该修复针对的「偏好切回 system 时 Mica 材质锁旧主题」缺陷在本架构下不存在。桌面宠物窗口修复针对的是便携版暂缓的宠物功能。
- 桌面更新源真正自助化：`prepare-runtime` 打包时把 `DSH_PORTABLE_RELEASE_SOURCE` 烘焙进 `resources/release-source.json`，`<便携根>/Data/config/desktop-release-source.json` 可按机器覆盖，打包后的更新器按两者依次解析、都没有才回落内置默认。CI 在 `v*` 标签上发布带兼容契约的 Release，应用内「检查更新」即可自动下载并 A/B 部署新版本，不再需要本地重新构建。
- 桌面版本升至 `1.0.53`。

## 1.0.51 便携集成版 — 2026-09-09

- 祖先关系对齐上游 `v1.0.51`（`merge -s ours` 认祖），此后与上游的每次同步都以该版本为三方基准，不再重放 1.0.42–1.0.46 已移植内容的历史冲突。
- 移植上游 `v1.0.50` 子代理未读修复：`origin === 'subagent'` 的会话不计入任务栏角标，覆盖初始基线、完成转换通知与迟到元数据三个路径。
- 桌面版本号升至 `1.0.51`，electron 升至 `44.1.1`、`@types/node 26.4.1`；不引入仅被上游桥接迁移模块使用的 `yaml` 依赖。
- 经审阅不移植上游 1.0.47–1.0.51 的恢复模式硬化系列：该系列与上游 `desktop-bridge` 迁移架构深度耦合，而便携版按既有决策保留自研桥接闭包（`dsh-process.js`/`profile-bundle-health.js`/`profile-quarantine.js`）与单插件检疫事务；桌面宠物、Mica 原生背景与插件矩阵批量升级（codex-ui 0.2→1.1 等）同样暂不采纳，插件升级将走 Profile 事务单独评估。
- 统一更新器增强：缺便携兼容契约的上游新版本进入独立「已阻止」状态（不再误报红色更新失败）；更新源构建期配置化（`PortableDesktopReleaseSource`）；新增候选槽与下载缓存回收（指针槽永保留，未引用槽保留最近 3 个，审计写 `events.jsonl`）。
- 开发门禁修复：官方基线核对改为校验当前指针槽打包 `dsh-runtime.tgz` 的真实版本（GNU tar 探测 `--force-local` 规避盘符坑），历史回退运行时目录在存在活动运行时槽时降级为提示，离线门禁由永久红转绿；构建器在未显式配置时回退 npmmirror 的 electron 镜像，消除直连 GitHub 下载超时导致的构建失败。

## 1.0.46 便携集成版 — 2026-09-04

- 将内置官方 DSH 运行时及其启动依赖从 `0.1.2-alpha.3` 升级到 `0.1.2-rc.1`，与受信发布清单对齐，消除「配套管理插件」持续提示可更新的状态。
- 合入上游 `v1.0.46` 的未读完成角标修复，并将十个内置社区组件同步到该版本的官方固定版本矩阵。
- 保留便携版的完整版内嵌浏览器、单插件事务隔离、便携路径和桌面/Harness 双 A/B 更新能力，不使用会覆盖这些能力的原始上游二进制包。
- 新增发布级便携兼容契约；客户端在下载前校验契约自身 SHA256、制品 SHA256 和必需能力，阻止不兼容 Release 覆盖定制功能。
- 修复 Electron ASAR 物理文件误判、已验证下载缓存复用，以及失败进度、错误码和事务号的可视化保留。
- 桌面更新改为切换前后台预热共享运行环境，压缩包另附不受打包时间戳和 pnpm 易变 SQLite mtime 索引影响的逻辑内容摘要，同时保留确定性仓库锁文件；内容未变化时跨版本复用缓存，内容变化时也在旧桌面仍可使用时完成准备，重启只执行槽切换和健康验证。启动阶段解压降级为断电或缓存损坏后的自愈路径。
- 强化 Windows 冷启动冒烟门禁：本机验收数据可固定在便携盘，HTTP 401 改用带硬超时的 `HttpClient`，清理前验证路径边界、归一化只读项并有限重试；只有离线启动、鉴权、插件闭包和清理全部成功后才输出 PASS。

## 1.0.43 — 2026-09-01

- 将内置官方 DSH 运行时及其启动依赖升级到 `0.1.2-alpha.3`。
- 将内置生态组件更新到 Codex UI 0.2.97、IM Connect 0.1.30、Archive Manager 0.1.22 和 MCP Connector 0.2.32。
- 强化离线运行时初始化：Windows 冒烟复用共享插件校验器，鉴权与未鉴权路径都等待桌面就绪标记，并在取消或超时解压时清理完整进程树。

发布标签：[`v1.0.43`](https://github.com/MichengAI/dsh-codex-desktop/tree/v1.0.43)。

## 1.0.41 — 2026-08-31

- 修复离线包缺少官方 DSH 启动所需 peer 依赖的问题，确保随包运行时无需联网即可完整启动。
- 将便携版首次运行时的校验、解压和大量文件复制移入独立子进程，并展示分阶段进度，避免初始化期间窗口长时间未响应。
- 将运行时与插件仓库的解压完成标记绑定到随包归档 SHA256；覆盖升级或复用旧便携目录时，空标记和陈旧标记会自动触发重新解压，离线首启不再缺少插件。
- 将 Windows、macOS 与 Linux 打包冒烟测试统一为强制离线，并在隔离 Profile 中核对全部十个随包插件及其固定版本。

发布标签：[`v1.0.41`](https://github.com/MichengAI/dsh-codex-desktop/tree/v1.0.41)。

## 1.0.40 — 2026-08-31

- 将内置官方 DSH 运行时及其启动依赖升级到 `0.1.2-alpha.2`，适配启动 Token 鉴权与 alpha.2 所需的原生安装脚本。
- 将内置生态组件更新到 IM Connect 0.1.27、Skills Manager 0.1.32、Archive Manager 0.1.21、Agency Agents 0.1.23、`dsh-context` 0.38.5、DSH Better Sidebar 0.18.0-alpha.0、MCP Connector 0.2.31 和 `dshmarket` 1.38.1。
- 将 `electron-builder` 升级到 26.15.7，并刷新供全新安装和缺包自修复使用的离线运行时与插件仓库装配流程。
- 让 Windows、macOS 与 Linux 打包应用冒烟测试适配 alpha.2 鉴权：在隔离 Profile 中校验预期的未鉴权响应、进程存活、启动诊断以及主窗口创建后的就绪标记。

发布标签：[`v1.0.40`](https://github.com/MichengAI/dsh-codex-desktop/tree/v1.0.40)。

## 1.0.39 — 2026-08-30

- 为 Ubuntu、Debian 及其他兼容发行版新增原生 Linux ARM64 / aarch64 安装包，同时提供 `.deb` 与 `.AppImage` 制品。
- 新增 GitHub 托管的原生 ARM64 打包任务与打包应用冒烟测试，不依赖未经验证的交叉编译。
- 新增经过校验的 Node.js 24.20.0 Linux ARM64 可执行文件哈希，并在发布前检查架构专属的 `latest-linux-arm64.yml` 更新元数据。
- 更新中英文下载说明、系统要求与开发文档，使 Linux x64 / ARM64 支持保持一致。

发布标签：[`v1.0.39`](https://github.com/MichengAI/dsh-codex-desktop/tree/v1.0.39)。

## 1.0.38 — 2026-08-30

- 将内置的六个 MichengAI 产品更新到 Codex UI 0.2.94、IM Connect 0.1.26、Automation 0.1.22、Skills Manager 0.1.31、Archive Manager 0.1.19 和 Agency Agents 0.1.22。
- 将内置生态组件更新到 `dsh-context` 0.38.3、DSH Better Sidebar 0.17.1、MCP Connector 0.2.29 和 `dshmarket` 1.38.0。
- 刷新供全新安装和缺包自修复使用的离线插件目录；已有 Profile 可通过插件市场升级到相同版本，桌面端不会静默覆盖用户主动选择的插件版本。
- 官方 DSH 运行时继续使用 npm 当前版本 0.1.1-rc.2；仅提供源码的 0.1.2 Alpha 暂不进入桌面稳定通道。

发布标签：[`v1.0.38`](https://github.com/MichengAI/dsh-codex-desktop/tree/v1.0.38)。
